# Cross-Cutting Architectural Decisions

Decisions made during feature-by-feature elicitation that apply platform-wide, beyond any single feature doc. Recorded here so later feature docs stay consistent without re-deriving them. Several of these resolve open questions from `docs/pdd.md`.

- **Backend framework: .NET.** Chosen specifically to ease FedRAMP acceptance and NIST/FIPS (crypto module) compliance for DOE/federal tenants. Resolves PDD Open Question #1 (backend language/framework). API surface pairing (REST/GraphQL conventions, versioning approach) should follow whatever is idiomatic for .NET/ASP.NET Core.
- **API surface: both REST and GraphQL, different audiences.** GraphQL for the internal web/mobile frontend; versioned REST for external integrations and webhook-adjacent tooling. Both sit on the same underlying business logic/permission layer. (Established in [api-messaging-layer.md](0-platform-core/api-messaging-layer.md).)
- **Offline sync strategy: idempotent append-only outbox; offline is a minimal capture subset, never 1:1 parity.** *(Revised — originally specified as hybrid CRDT + field-level LWW; superseded during the platform design review, see the revision entry at the end of this file.)* Offline writes are limited to creating new records (client UUID, locally editable until first sync) and appending rows to existing records' timelines; shared server-side records are never mutated offline — the Dispatcher on-behalf-of (radio relay) path is the platform's primary disconnected workflow. Under single-writer + insert-only + unshared-until-sync, conflicts are impossible by construction, so no conflict-resolution engine exists. Resolves PDD Open Question #5. (See [offline-data-sync.md](0-platform-core/offline-data-sync.md).)
- **GIS/map provider: adaptor architecture, tenant-selectable.** No single hardcoded vendor; tenants pick from enabled adaptors (e.g., a self-hostable default stack, Esri, Google), with DOE/air-gapped tenants restricted to self-hostable adaptors. Resolves PDD Open Question #4. (See [gis-mapping-services.md](0-platform-core/gis-mapping-services.md).)
- **Authorization model: RBAC baseline + ABAC overlay.** RBAC grants are feature + action + data-scope. ABAC policies (subject/resource/environmental attributes) can only narrow an RBAC grant, never widen it; failed ABAC conditions hard-deny with a visible reason and an audit entry. (See [authentication-authorization.md](0-platform-core/authentication-authorization.md).)
- **MFA/authenticator model: method catalog tagged by NIST 800-63B AAL.** Passkeys, hardware keys, and CAC/PIV are strong enough to serve as standalone primary login (no separate password); TOTP app, SMS OTP, email OTP, and magic link always pair with a password as a second factor. Tenants set a minimum required AAL that gates which methods are even offered. (See [authentication-authorization.md](0-platform-core/authentication-authorization.md).)
- **Tenant isolation: one tier per Company-level tenant.** Full database isolation vs logical isolation is set once at the top of a tenant's org hierarchy (Platform Super Admin, based on client classification) and inherited by everything beneath it — not configurable per-site. (See [authentication-authorization.md](0-platform-core/authentication-authorization.md).)
- **Notifications: internal-only scope for the Platform Core Notifications Engine.** It is strictly for internal platform-user notifications (alerts, task assignments, approvals). Module 17 (Mass Notification & Crisis Communications) is a separate system for external/occupant broadcasts and may reuse channel infrastructure but not this engine's category/ack/escalation model. (See [notifications-engine.md](0-platform-core/notifications-engine.md).)
- **CQRS = Command + Query + Event, explicitly differentiated.** [event-command-bus-architecture.md](0-platform-core/event-command-bus-architecture.md) now treats the Command bus (write intent → handler → event), Query bus (read intent → read-model, RBAC/ABAC-filtered, no side effects), and Event bus (fan-out to audit/notifications/push/projections) as three distinct pillars rather than collapsing into "just an event bus."
- **Two new Platform Core features added, split out from the raw bus:** [domain-events.md](0-platform-core/domain-events.md) (business-meaningful events configured to trigger cross-module side effects) and [command-action-bus.md](0-platform-core/command-action-bus.md) (reusable, invokable action/automation catalog). Both build on top of the low-level Command/Query/Event bus infrastructure rather than being part of it.
- **Unified action registry powers every invocation surface.** The Command/Action Bus catalog is the single source of truth for "what can happen" — UI buttons, context menus, drag-and-drop, command palette, CLI-style commands, and a future true CLI all invoke the same registered actions, never separate per-surface implementations of the same effect.
- **Command Palette and CLI-Style Input specified as two separate features.** [command-palette.md](0-platform-core/command-palette.md) (fuzzy search/navigate/invoke, any user) and [cli-style-input.md](0-platform-core/cli-style-input.md) (typed `verb noun --flag value` syntax, power users). The "CLI" is explicitly an in-app command bar, not a standalone external/scriptable tool — a true external CLI remains a separate, deferred, not-yet-scoped future possibility.
- **Command/Action Bus parameters support a "default resolver."** A parameter can declare a lookup (e.g., "this site's scheduled default patrol route") that fires only when no explicit value, context, or alias/playbook template value supplies one. Final parameter resolution precedence, uniform across every invocation surface: **explicit > context > alias/playbook template > default resolver**. Added to [command-action-bus.md](0-platform-core/command-action-bus.md); consumed by [cli-style-input.md](0-platform-core/cli-style-input.md) aliases.
- **Command system "standout feature" additions (Command/Action Bus + CLI-Style Input + Command Palette).** To make the command system a genuine differentiator, several mechanisms were added, all enforced server-side and shared across every manual invocation surface:
  - **Confirmation gate**: any manual invocation of an action with no compensating action, or flagged for step-up auth, must pass an explicit confirmation (surface-appropriate UI — chip in CLI, dialog in Palette/buttons) before executing; step-up is folded into the same confirmation step. Automated (Domain Events) invocations are exempt but cannot target step-up-required actions at all.
  - **Undo history**: generalized beyond a fixed timer — an invocation with a compensating action stays undoable as long as it's the most recent invocation against its specific target entity (not global-most-recent), scoped independently per entity, with a platform backstop max age. This is what lets a Dispatcher juggling several incidents undo the last action on any one of them independently. Each surface adds its own fast/convenience presentation (e.g., a 5-second toast) on top of this shared mechanism.
  - **AI-assist mode** (CLI-Style Input only): an explicit per-entry toggle (never blended into strict parsing) translates natural language into a proposed structured command requiring confirm/edit before running; includes optional off-by-default voice input reusing the same transcription capability as AI-Assisted Incident Report Writing. Tenant-disable-able.
  - **Proactive alias suggestion**: detects a user's repeated command pattern and offers to save it as an alias.
  - **Alias sharing & promotion**: personal alias → shared to a browsable team library (opt-in adopt, no auto-behavior-change) → optionally promoted by a Site/Tenant Admin into an auto-applied site/tenant-scope default.
  - **Explicitly rejected**: physical-world (NFC/QR checkpoint scan) context-seeding for CLI-Style Input — mobile field scanning isn't a natural fit for a desktop-oriented typed-command surface. Also rejected as a headline feature: voice as a primary field-work input (minor/optional only) and a fully separate third AI input surface (folded into CLI-Style Input as a mode instead).
- **CLI-Style Input supports chained commands with implicit, type-aware context.** `;`-separated commands on one line share context automatically (e.g., `create-incident ...; assign-unit U-01 --role IncCom` assigns to the incident just created). Context is tracked per entity type (not a single most-recent-wins slot), can be ambiently seeded from the currently open record, is shown as removable chips, and explicit flags always override it. Enter executes and leaves context active; rapid double-Enter executes and clears it. A failed step halts the chain with no auto-rollback of prior steps (lighter-weight than a formally authored Command/Action Bus playbook's full saga behavior).
- **API & Messaging Layer vs internal Event/Command Bus vs Module 19 Integration Gateway.** API & Messaging Layer is the external-facing client↔server and third-party-webhook layer. The (upcoming) Event & Command Bus Architecture feature is the internal server-side CQRS mechanism those APIs publish to/read from. Module 19's Physical Security Integration Gateway owns hardware protocol adapters (BACnet, Modbus, Wiegand, RTSP) and may reuse this feature's webhook/API infra for outbound notification only. (See [api-messaging-layer.md](0-platform-core/api-messaging-layer.md).)
- **Settings & Preferences is a shared hierarchical config registry, not just a bundle of platform preferences.** Every feature registers its tenant/site/user-configurable settings against one resolution engine rather than reinventing override logic. Two independent chains: a **location chain** (Platform → Tenant → configurable sub-units → Site → configurable sub-sites → Device) and an **identity chain** (User → UserDevice). Narrowest-set-wins with upward inheritance; personal (identity chain) beats location by default for identity-anchored settings, unless an admin **locks** a setting at some level (generalizing the admin-pinned-non-mutable pattern from Notifications Engine) to block every narrower override. Device-anchored settings (e.g., a kiosk's pinned Network Profile) win over a logged-in user's personal value on shared hardware; identity-anchored ones (e.g., language) don't. Setting changes are versioned and audit-tier. Retroactively, earlier docs' ad hoc policy models (Authentication's Tenant Auth Policy, GIS's GPS Retention Policy, Notifications' Tenant/User Notification Policy) are Setting Definitions registered against this engine, not standalone mechanisms. See [settings-preferences.md](0-platform-core/settings-preferences.md).
- **Entity modeling aligns with NIEM Core as an internal discipline, not full conformance (yet).** Entity/extension type naming and structure follow NIEM Core's taxonomy (`nc:Person`, `nc:Location`, `nc:Item`, `nc:Organization`) so a future NIEM-conformant exchange is a mapping exercise, not a rework — motivated by a named national-lab (DOE) client and plausible future CAD-to-CAD mutual aid interoperability with external fire/police departments (many CAD systems use NIEM-based standards) and CJIS-adjacent law enforcement BOLO sharing. Full IEPD/XML schema conformance is explicitly deferred until a concrete external exchange partner is identified. (See [entity-registry-core.md](0.5-master-records/entity-registry-core.md).)
- **Entity Registry Core is generic/extensible, base-entity-plus-extension pattern.** Any feature can register an Entity Type (Person, Vehicle, Location today) or an Extension Type (e.g., Employee/Visitor/Contractor/BOLO-Subject/Incident-Participant extending Person) against a shared core registry — mirroring the registration pattern already used for Settings and Command/Action Bus. Extensions add both fields and their own independent lifecycle; one base entity can carry multiple simultaneous extensions. Entity IDs are strictly tenant-scoped (never shared across the isolation boundary); a subcontractor's cross-tenant identity is linked via Authentication's existing multi-tenant identity bridge, not by merging Entity Registry data. Deduplication is always human-reviewed — no auto-merge at any confidence level, given the liability risk of misattributing history (e.g., BOLO status) to the wrong person. Merges redirect references to a canonical survivor, tombstone the loser, and are reversible with full audit trail.
- **Entity Registry Core is a service layer, not a generic data table — and Activity is a fourth base Entity Type.** Retrofitted after the user pushed on "why not just Person/Item/Conveyance/Location/Activity as concrete bases": Entity Registry Core now explicitly provides shared *behavior* (ID issuance, dedup, merge/tombstone, BOLO flag, tenant-scoping) via a lightweight cross-reference record (`Entity`: entity_id + type + status + a pointer to the real record), while Person/Item/Location each own their own concrete, strongly-typed schema with the actual fields — avoiding the entity-attribute-value anti-pattern a literal generic table would create. **Activity** (`nc:ActivityType` — calls, incidents, citations, accidents, alarms, inspections, dispatches, drills) joins Person/Item/Location as a fourth base Entity Type, established now (thin registration pattern only) specifically because it's what makes Entity Relationships & History's cross-module timeline a single uniform join instead of a patchwork of ad hoc foreign keys — and because Activity subtypes are, per the user, "the whole point of the platform." Individual modules (Dispatch/CAD, Security Operations, Physical Security Integration Gateway, Safety Management, etc.) will register their own Activity extensions with rich fields when those modules are specified. (See [entity-registry-core.md](0.5-master-records/entity-registry-core.md).)
- **Master Records base type set rounded out to five: Party, Item, Location, Activity, Document.** Prompted by the user pointing out NIEM Core has real, distinct `nc:EntityType` (Person-or-Organization), `nc:OrganizationType`, and `nc:DocumentType` bases that were missing. Key structural changes:
  - **Party** (renamed from NIEM's "Entity" to avoid colliding with "Entity Registry Core," the platform's own service-layer name — "Party" is standard enterprise-data-modeling terminology for exactly this Person-or-Organization duality) becomes a genuine base Entity Type with its own identity/dedup/merge, registered directly against Entity Registry Core. **Person is retrofitted to extend Party** rather than being a directly-registered base type — a real architectural change, not just a rename: Person Registry no longer owns `contact_information`/`identifications[]` (inherited from Party), and extension chains can now nest more than one level (Employee → Person → Party).
  - **Organization** (new) is Party's second extension, uniform/no-category like Vehicle, with basic `parent_organization_ref` self-nesting. A tenant can designate one Organization record as itself, letting fields like Vehicle's owner resolve to a real Party reference instead of a three-way person/tenant-fleet/external-org branch — Vehicle/Conveyance Registry's `owner_type` enum was retired in favor of `owner_party_ref` (nullable, defaults to tenant-owned).
  - **Document** (new) is the fifth base type, `nc:DocumentType`-aligned, with hash/integrity verification and version history built at the base level — directly serves Investigation Management's future "Digital Evidence Locker" (already named in MODULES.md) rather than that module reimplementing file-integrity tracking.
  - **Entity Relationships & History synthesizes the whole Master Records architecture into two views**: an **Interaction Timeline** (chronological — Activity participation/attachment/location, EntityAssociation transitions, BOLO Flag lifecycle) and a **Relationship Snapshot** (current-state — only active EntityAssociation rows, grouped by kind). Both are CQRS read-model projections (not live queries), require a dedicated Interaction Timeline Viewer permission layered on top of (never bypassing) each entry's own ABAC/sensitivity filtering, and need no special-case merge handling since Entity Registry Core's merge already redirects historical *and* active association rows to the survivor. Scheduled shifts and access-control events (named in MODULES.md) are an explicitly deferred integration point, not built now, since Personnel and Access Control aren't specified yet. This is the last Master Records feature. (See [entity-relationships-history.md](0.5-master-records/entity-relationships-history.md).)
- **Final architecture: Table-Per-Type (TPT) inheritance for both Entity and EntityAssociation, one shared PK per hierarchy.** Third and final refinement of the entity/association architecture, prompted by the user proposing TPT directly. Two independent TPT hierarchies: **Entity** (`Entity` → `Party` → `{Person, Organization}` → `{Employee, Visitor, ...}`, and `Entity` → `{Item, Location, Activity, Document}` → their own extensions) and **EntityAssociation** (`EntityAssociation` → named concrete kinds like `ConveyanceOwnerAssociation`, `CustodyAssociation`). At every level, the row's primary key **is** `entity_id` (or `association_id`) — shared with and FK'd to the parent level — eliminating the earlier redundant `extension_id` + `entity_id` pointer pair, and giving associations real named types instead of a string `association_type` discriminator alone (the discriminator is kept only as a practical query aid, not the source of truth for type). This also resolved the user's sharp challenge to the earlier "generic vs. concrete" framing: the `Entity`/`EntityAssociation` root tables ARE generic tables, and that's fine specifically because they hold only uniform metadata (id/type/status, or two-refs/role/audit) — genericity earns its keep exactly where heterogeneity is the modeled feature (identity, arbitrary relatability) and costs you exactly where it isn't (the actual field data, which stays in concrete per-level tables).
  - **Full sweep**: every direct entity-reference field across Master Records was re-evaluated and converted to a named EntityAssociation subtype where the relationship benefits from independent history/audit: `ConveyanceOwnerAssociation` (Vehicle ownership, replacing `owner_party_ref`), `CustodyAssociation` (Item custody, replacing `current_holder_ref`+`custody_history[]` — current holder is just the active row, history is the active/removed sequence), `EmergencyContactAssociation` (Person-to-Person, replacing the dual-mode embedded field — the contact must now be a real, possibly minimal, Person entity, no free-text fallback), `HierarchyAssociation` (one reusable subtype for both Location's and Organization's parent/child nesting, replacing two separate `parent_*_ref` fields), `ActivityLocationAssociation` (replacing Activity's `location_ref`, gaining location-correction history for free), `DocumentAuthorAssociation` (replacing `author_party_ref`, and now supports multiple co-authors for free), `FacilityManagerAssociation` (replacing Location's `facility_manager_contact_ref`), `ActivityParticipantAssociation`/`ActivityAttachmentAssociation` (named subtypes replacing the earlier discriminator-only rows).
  - **Display labels are universal, owned by Entity Registry Core** (corrected after an initial misread): every registered Entity Type/Extension Type, at every TPT level — Party, Person, Organization, Item, Vehicle, Location, Activity and every Activity extension, Document — must declare a `display_label_strategy`, either `template` (a simple field readout, e.g. Person → full name, Vehicle → plate+make/model, Location → location_name, Document → document_title) or `computed` (synthesized summary logic, needed by Activity extensions specifically, since a useful label there draws on several fields, not one). The requirement explicitly does **not** extend to non-entity system/governance records (`EntityAssociation` rows, `PotentialDuplicate`, `MergeRecord`, `BOLOFlag`, audit-log entries) — those are metadata *about* entities, not entities, and render using the labels of whatever entities they reference. Consumed by Command Palette, CLI-Style Input context chips, and Entity Relationships & History's timeline uniformly, with no per-surface type-switching logic.
  (See the retrofitted [entity-registry-core.md](0.5-master-records/entity-registry-core.md) and every other Master Records doc, all updated to match.)
- **Associations: one real base table anchored at the Entity layer, refined twice.** First pass specialized associations into bespoke concrete tables per pairing (`ActivityPartyAssociation`, etc.) to avoid a polymorphic-FK problem. User pointed out a better resolution: anchor both sides of a relationship (`entity_id_a`, `entity_id_b`) to Entity Registry Core's own lightweight `Entity` cross-reference record — not to the N concrete schemas directly — which makes "points to a valid Entity" a real, single-table, enforceable constraint. This yields one genuinely generic, physical **Entity Association** table (`entity_id_a`, `entity_id_b`, `association_type`, `role`, `added_by`, `added_at`) that isn't the EAV anti-pattern, because unlike a field-blob table, an edge-list (two refs + a role) is uniform data regardless of what it connects — a standard graph/join-table pattern. Specialization now means exactly what it means for entities: an **Entity Association Extension** (its own small table, FK'd to the base association row) only when a specific `association_type` needs more than role+audit (e.g., `employment` needing start/end dates) — mirroring Employee-extends-Person-extends-Party. Activity Registry's three bespoke tables were retired in favor of Entity Association rows with `association_type` = `activity_participant` / `activity_attachment`. Confirmed NIEM-faithful throughout: `nc:AssociationType` is itself a generic role+entity-reference pattern used across NIEM, not something Person-specific.
  (See [party-registry.md](0.5-master-records/party-registry.md), [organization-registry.md](0.5-master-records/organization-registry.md), [document-registry.md](0.5-master-records/document-registry.md), retrofitted [entity-registry-core.md](0.5-master-records/entity-registry-core.md), [person-registry.md](0.5-master-records/person-registry.md), [activity-registry.md](0.5-master-records/activity-registry.md), [vehicle-conveyance-registry.md](0.5-master-records/vehicle-conveyance-registry.md).)
- **Activity Registry promoted to its own full first-class doc**, giving the fourth base Entity Type (established thin in Entity Registry Core) the same depth as Person/Item/Location. Owns three generalized mechanisms: (1) a **generic Participant association** (any Person or Item entity, tagged with a role, on any Activity) — confirmed NIEM-faithful via `nc:AssociationType`'s generic role+entity-reference pattern, used throughout NIEM for exactly this purpose; this **retires Person Registry's separate "Incident Participant" extension**, which is removed in favor of the shared mechanism (same generalization instinct as BOLO Flag). (2) **Offline-safe identification** reusing Offline Data Sync's existing client-UUID + server-assigned-sequential-number pattern unmodified. (3) **Dedup/merge** using the same tombstone + redirect + reversible model as Person/Item/Location, explicitly designed to power Dispatch/CAD's future "Incident Merging" feature (already named in MODULES.md) rather than that module reimplementing it. This doc establishes only the base mechanics; individual modules (Dispatch/CAD, Security Operations, Physical Security Integration Gateway, Safety Management, Emergency Management) register their own rich Activity extensions (Call, Incident, Citation, Accident, Alarm, Inspection, Drill) when specified. (See [activity-registry.md](0.5-master-records/activity-registry.md).)
- **Location Registry grounded in `nc:LocationType`, with a base-level `location_type` field** (unlike Vehicle's deliberate no-category pattern) since geometry needs genuinely differ by kind (address for a Site, polygon for a Zone, floor/room identifiers for a Room) — structural, not purpose-driven. Carries basic `parent_location_ref` self-nesting as an intrinsic structural fact (Room→Floor→Building), with the rich hierarchy-building UI deferred to Facility & Zone Management's future Location Hierarchy Designer. "Utility & Contact Directory" gets a light quick-reference surface here (facility manager contact, shutoff pointers, local dispatch number); the full directory is deferred to Facility & Zone Management's future Utility Control Tracking — same light-reference/deferred-lifecycle pattern as Vehicle's `associated_permits[]`. **Location geometry vs. GIS Geofence** are kept as distinct concepts (a location's footprint = intrinsic identity; a geofence = a purpose-built alerting overlay that doesn't need 1:1 correspondence) but a Geofence may now optionally set `derived_from_location_ref` to reuse a location's own geometry instead of requiring a redundant independently-drawn duplicate shape (touched up in [gis-mapping-services.md](0-platform-core/gis-mapping-services.md)). (See [location-registry.md](0.5-master-records/location-registry.md).)
- **Vehicle/Conveyance Registry promoted to its own full first-class doc**, sitting on top of Item Registry rather than being a thin stub within it — mirroring Person Registry's depth, since Vehicle (like Person) is a cross-cutting core concern consumed by many modules (Dispatch/CAD, Access Control, Executive Protection, Supply Chain, Special Event) with no single owner. Base Vehicle record is deliberately **uniform, no category enum** — a vehicle's purpose (fleet/personal/visitor/vehicle-of-interest) is expressed by which further extensions consuming modules attach later, exactly like Person's Employee/Visitor/Contractor pattern, not a flat field. Introduces **owner** (who the vehicle is registered to — person, tenant fleet, or a future Organization type) as explicitly distinct from Item Registry's existing **custody** (who currently has/is operating it) — a fleet car is owned by the tenant but custody rotates shift to shift. Permit/parking-pass/authorization-tag association is a lightweight reference here (`associated_permits[]`); full lifecycle deferred to Access Control. (See [vehicle-conveyance-registry.md](0.5-master-records/vehicle-conveyance-registry.md).)
- **"Vehicle Registry" generalized into "Item Registry" (base `nc:ItemType` + Vehicle as its first `nc:ConveyanceType`-aligned extension).** Same two-layer pattern as Person Registry (not a third infrastructure layer) — Item Registry defines the base Item entity directly against Entity Registry Core. Item carries generic **custody/possession tracking** (current holder + transfer history, atomic and audit-tier) at the base level, anticipating that Weapons Inventory, Key Custody, Lost & Found, Evidence Tracking, and K9 Equipment will all register their own Item extensions later and inherit this rather than rebuilding chain-of-custody logic each time. Vehicle's own fields (plate, jurisdiction, model year, body style) are additive to the base Item's make/model/color/photo, never duplicating them. (See [item-registry.md](0.5-master-records/item-registry.md).)
- **BOLO Flag generalized to Entity Registry Core, applicable to any entity type.** Originally built Person-specific in Person Registry, then retrofitted: one governed mechanism (Supervisor+ permission, mandatory justification, mandatory expiration, step-up auth to create/clear, audit-tier) lives at Entity Registry Core and is reused unmodified by Person's BOLO/Trespass Subject and Vehicle's BOLO/Violation Flag — consuming entity types add only their own context field (e.g., trespass_notice_ref, citation_ref). Same generalization instinct as the Command/Action Bus confirmation gate and undo history (put shared governance at the lowest common layer, not duplicated per consumer). (See [entity-registry-core.md](0.5-master-records/entity-registry-core.md).)
- **Person Registry's base fields are structurally grounded in `nc:PersonType`, not an invented field list.** Structured name with aliases, NIEM-style physical description (sex, race/ethnicity, height/weight/eye/hair color, distinguishing features), a generic repeatable Identification structure (id value + category, covering SSN/DL/passport/badge/etc.), associated Contact Information, and Person-to-Person emergency-contact associations. This is a materially better fit for BOLO/witness-description use cases than a generic HR-style profile. Medical alerts and language fluencies have no NIEM Core equivalent and are explicitly labeled as platform-specific augmentations. Race/ethnicity and citizenship are on-by-default nullable fields (tenant-disableable, sensitivity-classified) per explicit user direction: "this platform is about security not political correctness," matching real BOLO/witness-description practice and NIEM Core itself. These live once on the base Person entity (never duplicated per extension — they're intrinsic characteristics of the person, not the relationship) with **field presence decoupled from workflow display**: each extension type's owning module decides whether its own create/edit UI surfaces these fields by default (e.g., Personnel's Employee flow need not prompt for race; Access Control's BOLO flow should). This is the primary control against US-domestic employment-law exposure (EEOC disparate-treatment risk, state sensitive-PI statutes) without weakening the data model or the BOLO use case; GDPR was considered and correctly ruled not operative given the platform's US-commercial/DOE-national-lab target market. Exact NIEM version/element-name verification remains a technical-spec task. This same grounding approach (checking the real NIEM Core type before inventing fields) should be applied when specifying Vehicle Registry (`nc:Item`/vehicle extension) and Location Registry (`nc:Location`). (See [person-registry.md](0.5-master-records/person-registry.md).)
- **Terminology: "Tenant" is reserved exclusively for the SaaS multi-tenancy concept.** MODULES.md's Person Registry profile type "tenants" (building occupants/residents) is renamed **Occupant** platform-wide to avoid collision — consumed primarily by Module 9's Tenant & Occupant Registry feature (which itself should probably be read/renamed with this in mind when specified). (See [person-registry.md](0.5-master-records/person-registry.md).)
- **Person Registry extension types get only a core/shared skeleton here; richer fields deferred to owning modules.** Employee, Visitor, Contractor, and Occupant extensions each have minimal identifying fields defined in Person Registry; Personnel, Access Control, Subcontractor Management, and Facility & Zone Management respectively add their own richer fields when those modules are specified — consistent with Entity Registry Core's extensible registration pattern.
- **BOLO/Trespass Subject is a governed, high-liability extension type**: elevated permission (Supervisor+) required to create/activate, mandatory justification, mandatory expiration (statutory or tenant-configured default), and step-up authentication required for both creation and clearance.
- **Sensitive Person fields (medical alerts, emergency contacts) use emergency-aware visibility**: restricted to a specific permission under normal operations, automatically broadened to EOC/muster-coordinating roles during an active emergency event, then reverted — a pattern other sensitive-field features may want to reuse.
- **Network Profiles are a Settings & Preferences concept**: bundled bandwidth behaviors (media sync deferral, compression, tile pre-cache, sync frequency, push verbosity), platform presets + tenant-customizable, auto-selected by connection quality with manual pin override at any eligible hierarchy level.
- **First Module 1 (Security Operations) feature: Daily Activity Reports (DAR).** Established three reusable patterns for any future "report" feature (SITREPs, After-Action Reports, and others are likely candidates):
  - **A "report" is a filtered read over Activity Registry, snapshotted into a Document — not its own container entity.** DAR itself stores almost nothing: a **DAR Entry** is a thin Activity extension (tenant-configurable category + narrative + optional location/attachment, same mechanism as any other Activity), and "the report" is a person/site/time-window filter over Activity Registry that pulls in DAR Entries *and* every other Activity extension in scope (Incidents, Dispatches, etc., once those modules exist) automatically, with zero DAR-side change required when a new Activity extension is added elsewhere. **Generate Report** snapshots that filtered result into an immutable Document (Document Registry's hash/version model), fixed at generation time and never retroactively updated even if a pulled-in Activity later changes — a saved report is explicitly not guaranteed 1:1 with an Activity's eventual final disposition. Report-generation mode (auto-generate on sign-off vs. ad hoc-only vs. both) is Settings & Preferences-registered, tenant-configurable.
  - **A review/sign-off gate is its own governance record, never a status field on the underlying Activity.** DAR's **Shift Review** references a scoped set of Activities and records per-item approve/kick-back/exclude decisions without ever mutating the Activity records themselves — same instinct as BOLO Flag being a separate governed record rather than a field on Person/Vehicle. This matters specifically because Activities are shared across modules (an Incident pulled into a DAR keeps its own independent lifecycle, owned by the future Incident Reporting module, regardless of how a given shift's DAR review judged it). Any future feature needing a "did a human sign off on this set of records" gate over Activity Registry data should likely reuse this pattern rather than adding an approval field to Activity itself.
  - **Shift Window is a deliberately interim, DAR-owned scoping record** (clock-in/out for a Guard, or a Supervisor-defined time block for a team roster) standing in for Post Schedule Builder's (Module 8, not yet built) real scheduled-shift data — flagged for reconciliation once that module exists, same deferred-integration pattern used for Entity Relationships & History's scheduled-shifts note.
  (See [daily-activity-reports.md](1-security-operations/daily-activity-reports.md).)
- **Shift Passdowns & Handover Notes reuses DAR's filtered-view/Document-snapshot pattern with a different inclusion rule, and adds two more reusable pieces:**
  - **Rule-or-flag inclusion, not blanket inclusion.** Unlike DAR (everything in a person/site/time filter), a passdown only pulls in Activities included by a tenant/site-configured **Pass-On Rule** (by activity_type/category, Settings & Preferences-registered) or an explicit per-Activity **Pass-On Flag** (manual "add to pass on"). Any future report-shaped feature needing "curated subset" rather than "everything" should follow this rule-or-flag split rather than reinventing filtering.
  - **Pass-On Flag is BOLO-Flag-shaped but deliberately kept feature-local, not promoted to Entity Registry Core** — a unary marker on an Activity with its own `open`/`resolved` lifecycle, independent of the referenced Activity's own status (reusing the "governance record doesn't mutate what it references" discipline from DAR's Shift Review). Kept local because, unlike BOLO Flag, nothing else currently needs a cross-entity-type version; promote later only if that changes.
  - **Configurable-by-role, configurable-by-scope acknowledgment** (which roles must explicitly read/acknowledge which passdown scope, hard-gating only those roles' shift start) is introduced here as a feature-local mechanism, explicitly flagged as a strong candidate for Digital Acknowledgment Logs (Module 14, not yet built) to generalize — same deferred-integration posture as DAR's Shift Window pending Post Schedule Builder.
  (See [shift-passdowns-handover-notes.md](1-security-operations/shift-passdowns-handover-notes.md).)
- **Guard Tour & Checkpoint Verification introduces a "triad" entity pattern and a multi-place assignment-plan pattern, both likely reusable:**
  - **Checkpoint is a Location extension plus an associated, independently-replaceable physical Item (its scan tag), linked via a single-current-value EntityAssociation** — a checkpoint's own identity/history survives tag replacement, exactly like Vehicle's `ConveyanceOwnerAssociation` pattern applied to hardware instead of ownership. Any future "a place with an attached, swappable physical asset" concept (an access-control reader, an alarm sensor mount) should likely follow this same triad shape rather than conflating the place and the hardware into one record.
  - **A Route is an assignment plan composed of independent Tour Definitions that can target entirely different places under one assignment**, each with its own required occurrence count and minimum spacing (e.g., "2 tours of Building 2, at least 4 hours apart, alongside single tours of three other buildings"). This is a materially richer shape than a single ordered checkpoint list, and is the pattern any future multi-site or multi-occurrence assignment feature should default to rather than assuming one assignment = one place.
  - **Tour Run and Checkpoint Scan both register as Activity extensions** (Tour Run per-execution, Checkpoint Scan per-event), continuing the "everything trackable is an Activity" discipline established since Activity Registry, feeding Module 12's future Tour Completeness Logs / Officer Performance Logs with zero DAR/Passdown-side changes needed.
  - Missed/late tour detection is deliberately left as a published automation-eligible domain event rather than a hardcoded alert path, consistent with Domain Events owning all "when X, do Y" behavior platform-wide.
  (See [guard-tour-checkpoint-verification.md](1-security-operations/guard-tour-checkpoint-verification.md) — note several specifics in that doc's Open Questions are best-guess defaults pending confirmation, not fully elicited, due to interrupted question rounds.)
- **Patrol Management establishes itself as the umbrella assignment layer over Guard Tour & Checkpoint Verification**, via a lightweight interim **Post** concept (`post_type`: fixed | mobile) — same deferred-integration posture as DAR's Shift Window, pending Post Schedule Builder (Module 8). Guard Tour's Route Assignment was **retrofitted** with an optional `post_ref` (see both docs) so a Fixed Post's local patrols, or a Mobile Patrol Unit's optional checkpoint coverage, are ordinary Route Assignments underneath a Post — no parallel assignment mechanism. Two more reusable pieces:
  - **Graceful location-source fallback chain** for anything that needs a "current position" without assuming GPS is always available: `gps_continuous` → `scan_checkin` → `radio_manual`, all writing to one `last_known_location` (+ source + timestamp), never fabricating a position and always surfacing staleness by age. Any future feature tracking a mobile asset's live position should default to this chain rather than assuming GPS.
  - **Ad hoc Patrol Request built now, shaped to become a Dispatch/CAD target later** — deliberately scoped as request → assign → fulfill on top of Activity Registry, rather than either skipping ad hoc tasking until Module 2 exists or over-building real dispatch-queue mechanics prematurely. Future Dispatch/CAD should extend/consume this Activity type, not replace it.
  (See [patrol-management.md](1-security-operations/patrol-management.md).)
- **Guard Tour & Checkpoint Verification retrofitted: "Tour Run" renamed to Patrol, plus two scan-triggered mechanisms added.** Per the user's own framing ("patrol is the activity of completing a tour"), the Activity extension for one tour execution is named **Patrol**, giving a genuinely nested Activity chain: Route (a plan, not an Activity) → Patrol (a tour run, an Activity) → Checkpoint Scan (an event within a Patrol, itself an Activity) → optionally a Patrol Finding or other Activity spawned from a scan. Route Assignment stays a non-Activity plan/scope record (same role as DAR's Shift Window), not promoted to a fifth nested layer, since it has no discrete "occurrence" of its own distinct from the Patrols underneath it. Explicitly disambiguated in both docs from Patrol Management's "Mobile Patrol Unit"/"Patrol Request" (assignment/post layer) — same root word, different layer, deliberately. Two additions, both likely reusable:
  - **Checkpoint carries `inspection_instructions`, surfaced automatically on scan** — "what to look for" lives on the checkpoint itself, not a separate reference doc; must be offline-cached since scanning often happens without connectivity.
  - **A Checkpoint Scan is a generic launch point for follow-on Activities, with location auto-seeded from the checkpoint.** A lightweight built-in **Patrol Finding** (its own Activity extension, tenant-configurable category + narrative, independent open/concluded lifecycle that never mutates its originating scan) covers minor observations (an unlocked door, a leak); any richer future Activity type (e.g., Incident) can be launched the same way via ordinary Command/Action Bus context-seeding (location passed as explicit context) — no scan-side change needed as new Activity extensions are added later. Any future "event record that should let you spin off a fuller record with location pre-filled" should follow this same launch-point shape.
  (See the retrofitted [guard-tour-checkpoint-verification.md](1-security-operations/guard-tour-checkpoint-verification.md).)
- **Correction: Route Assignment is itself an Activity extension, not a plain scope/plan record.** The prior retrofit (above) had modeled Route Assignment as a bare plan/scope row, the same role as DAR's Shift Window — the user corrected this: "there needs to be an activity of each time a route is assigned and completed." Route Assignment now registers as an Activity extension with its own lifecycle (`open` → `in_progress` → `concluded`/`cancelled`) and a rolled-up `completeness_pct` across every Patrol underneath it, giving a clean four-level nested Activity chain with only **Route** itself (the plan/build) as the non-Activity layer: Route → Route Assignment → Patrol → Checkpoint Scan. General lesson for future docs: a "scope/window" record (like Shift Window) is the right shape only when nothing distinguishing actually *happens* at that level beyond bounding time — the moment it has its own real start/complete lifecycle and rollup metrics, per this correction, it should be an Activity instead.
  (See [guard-tour-checkpoint-verification.md](1-security-operations/guard-tour-checkpoint-verification.md).)
- **Guard Tour's structure crisped into two explicit parallel hierarchies, confirmed by the user: a plan hierarchy and an execution hierarchy, one-to-one.** Plan (definition, versioned, no lifecycle beyond active/archived): Route → Tour Definition → Checkpoint. Execution (Activity extension, one per real occurrence): Route Assignment → Patrol → Checkpoint Scan — each execution level maps to exactly one plan level above it. "Route Assignment" keeps its name rather than adopting a Patrol/Scan-style word swap (considered and rejected: "Detail," "Deployment"). This plan/execution split is a clean template for any future feature with the same "define once, occurs many times, occurrence needs its own nested audit trail" shape.
- **Big-picture review of Guard Tour surfaced five real gaps, all resolved by the user, worth carrying forward as general lessons:**
  - **A checkpoint doesn't belong to any one tour.** A Checkpoint can be referenced by multiple Tour Definitions (even across Routes) — so anything that's a property of the physical checkpoint (required verification method, inspection instructions) belongs on the Checkpoint, never on whichever plan level currently references it. General lesson: when a "child" record can legitimately have multiple "parents," don't let parent-owned config leak onto the child from just one of them.
  - **Starting an execution-level occurrence needs to be an explicit, trackable moment** ("the system needs to know when Unit 1 starts a patrol of building 37"), not inferred — resolved as explicit start (self, or on-behalf-of via radio, reusing Patrol Management's existing radio-check posture) OR implicit start via first scan **only when unambiguous**; an ambiguous match (a checkpoint shared by multiple currently-eligible tours) prompts the user rather than guessing. General lesson: never let the system silently disambiguate against a real-world decision a human actually needs to make.
  - **Plan mutability during active execution needs versioning, not live references.** Route/Tour Definition edits create a new version (same discipline as Settings & Preferences); Route Assignment/Patrol pin the version in effect when they started, so an in-progress or completed occurrence's requirements never shift retroactively under a later plan edit. General lesson: any plan/execution split needs an explicit answer to "what happens to in-flight executions when the plan changes" — a live FK reference is usually the wrong default.
  - **Verification/compliance checks should degrade gracefully, never block completion.** A guard can always manually override a failed/unavailable verification method, recorded as unverified rather than blocking the work. General lesson: a compliance-tracking mechanism must not become an operational blocker — track the gap, don't prevent the job.
  - **Dedup is for identity, not events.** Checkpoint Scan doesn't need Entity Registry Core's full dedup/merge/human-review machinery — only a narrow write-time debounce against an accidental immediate double-read. Flagged for Activity Registry to eventually formalize a "high-volume event-type Activities may opt out of dedup" allowance. General lesson: don't inherit a shared mechanism's full weight by default onto a type it wasn't designed for — a routine timestamped event has no "duplicate identity" the way a Person or Incident does.
  - **A negative/failure outcome should still be a real, queryable record, not a silent absence.** A missed required occurrence auto-creates a `missed` Patrol row (with an optional context-only resolution note) rather than just leaving a gap in the data — "missed points are missed points," no waiver state that softens the record. General lesson: when reporting/analytics need "did X happen," model the non-occurrence as a real row too, not an implicit absence a query has to infer.
  (See [guard-tour-checkpoint-verification.md](1-security-operations/guard-tour-checkpoint-verification.md).)
- **Three-hop evolution of a bounded EAV exception: Courtesy Patrol (local field) → Entity Registry Core (`extended_fields` on the TPT roots) → Tenant-Defined Types & Custom Fields (Module 0, new Platform Core feature owning the whole governance model).** Started as a `category_details` JSONB column local to Courtesy Patrol, holding a few category-specific optional fields (Jump Start battery type, Tire Change tire position) without forcing a full Activity-extension TPT level per small category. Promoted to Entity Registry Core so `extended_fields` lived once on both TPT roots (`Entity`/`EntityAssociation`, inherited by every level via the shared primary key) instead of per-feature. Promoted again, on the user's suggestion, to its own Platform Core feature — [tenant-defined-types-custom-fields.md](0-platform-core/tenant-defined-types-custom-fields.md) — so the concept isn't limited to things downstream of Entity Registry Core at all: **Entity Registry Core now only carries `extended_fields` as physical storage** (the default/primary "carrier"); the new feature owns the Custom Field Definition registry, write-time validation, reserved-key (`custom_*`) namespace protection, per-tenant caps, and Platform Super Admin audit/override — plus a further generalization the user also asked for: **fully tenant-defined new Types**, not just fields. A Tenant-Defined Type is a real Entity/Extension/Association Type registration (`is_tenant_defined = true`) with **no developer-built concrete table at all** — every field lives in `extended_fields`, keeping the self-service path free of runtime DDL/schema-migration risk while still getting full standard treatment (dedup, merge, BOLO Flag eligibility, display labels, timeline inclusion) for free, since it's a real registered type. Two non-entity carriers (Domain Events **Rule**, CLI-Style Input **Alias**) were wired up immediately to prove the mechanism generalizes beyond Master Records, per the user's explicit ask. Courtesy Patrol itself was retrofitted a second time to consume the final mechanism, its Category Definition now acting as a **carrier subdivision** (`courtesy_patrol:category:<id>`, finer-grained than a whole registered type) rather than owning any storage or governance.
  - **Concrete TPT subtype columns remain the default and expected way to add fields, unconditionally** — `extended_fields` is reserved for (a) a handful of optional, low-value-to-formalize fields where a dedicated column/table is disproportionate, or (b) genuine tenant-defined custom fields/types, explicitly NOT a dynamic-DDL system (no new physical table is ever created at tenant request).
  - **Schema-governed, not free-form**, with a reserved namespace: every custom field key must be `custom_`-prefixed, guaranteeing a future platform-added concrete column can never collide with a tenant's self-service field.
  - **Self-service, but capped, not gatekept**: Tenant Admin defines freely within platform-enforced per-tenant caps; Platform Super Admin gets audit visibility and override (disable, never destructive) without pre-approval friction.
  - **Explicit, documented tradeoff**: never guaranteed indexed/queryable like a concrete column — the deliberate price of self-service without a schema migration; a **graduation path** (promoting a popular tenant-defined type/field into a real developer-built concrete column later) is flagged as a future technical-spec undertaking, not solved now.
  - General lesson: when a locally-drafted escape hatch keeps looking useful outside its original feature, promote the *mechanism* (not just the pattern) to the smallest shared layer that's still generic enough — first Entity Registry Core, then further up to a dedicated Platform Core feature once even Entity Registry Core turned out to be too narrow a home. Any future "many small categories/contexts, each with a few optional fields" or "let a tenant self-serve a whole new record type without code" need should register against this one governed mechanism rather than reinventing it.
- **Tickets, Citations & Traffic Safety fulfills Activity Registry's already-anticipated `citation` activity_type**, modeled as one Activity extension (category distinguishes courtesy notice through formal fine-bearing citation, not two separate mechanisms) plus a second, independent **Traffic Control Activity** extension for traffic-direction work unrelated to any citation — no forced cross-reference between the two; both just surface together in the same guard's DAR/timeline by time/location. Citation issuance is the first concrete use of Authentication & Authorization's RBAC-baseline-plus-ABAC-overlay model for a category-gated professional authority requirement (a tenant-configurable `requires_citation_authority` flag per category), with the actual certification data deferred to Personnel/Armed Qualifications Registry, not yet built — same deferred-cross-module-dependency posture used throughout Module 1.
  - **Ticket Book introduces a reusable "checked-out numbered range must fully reconcile" pattern**: a physical book/pad (optional, tenant/site-configurable, since not every site uses paper) tracks a number range assigned to a person; every number must resolve to either a real record (a Citation) or an explicit voided entry before the book can be marked reconciled, with `lost` as a distinct terminal state given the compliance weight of a missing pad. This sits *alongside*, never instead of, each record's own digital offline-safe identity (client UUID + server sequence number) — a physical reference number is optional extra metadata, not a replacement identity. Any future feature needing to track a physical numbered-pad/tag range with full accountability (e.g., a future Evidence tag range, a Lost & Found tag book) should reuse this same checkout → void-or-issue → reconcile shape rather than inventing its own.
  (See [tickets-citations-traffic-safety.md](1-security-operations/tickets-citations-traffic-safety.md).)
- **Incident Reporting & Management validates DAR's report architecture as a genuinely reusable platform pattern, not a one-off.** Incident directly reuses "report = filtered Activity Update timeline snapshotted to an immutable Document," DAR's Shift Review governance-record pattern (renamed Supervisor Review here, same "references but never mutates what it judges" discipline), and DAR's three-way report-generation-mode toggle (auto-on-sign-off / ad hoc-only / both) — the third consumer (after DAR and, more loosely, Passdown) confirms this is the platform's standard shape for "evolving record with an update timeline that needs both live tracking and a point-in-time official document," not something worth re-deriving per feature.
  - **New refinement carried back for future reuse: report *scope mode* as a first-class, tenant-configurable axis independent of report *generation* mode.** Incident Report can be `master` (one Document per Incident) or `per_responder` (one Document per authoring responder) — orthogonal to *when*/*how* a report generates. Any future multi-contributor evolving-record report should consider both axes independently rather than assuming one document per record is the only shape.
  - **Review-reopening on new information**: a Supervisor Review that already signed off reopens automatically when a new Incident Update arrives on a `concluded` Incident, rather than requiring a manual reopen action or blocking further updates post-signoff — a refinement of DAR's kickback-only reopening, worth considering wherever a review gate sits in front of a record that can keep receiving new information after approval.
  - **Escalation linkage (`escalated_from_ref`) formalizes the launch-point pattern's audit trail**: every doc that already established itself as an Incident launch point (Guard Tour's Checkpoint Scan, Patrol Finding, DAR Entry, Courtesy Patrol, Citation) required zero changes — the pattern was deliberately built generic enough upstream that Incident simply consumes it.
  (See [incident-reporting-management.md](1-security-operations/incident-reporting-management.md).)
- **AI-Assisted Incident Report Writing closes out Module 1, establishing the platform's human-in-the-loop AI discipline for generated narrative content and a reusable interim-timeline pattern.** Response Timeline Event (call received/dispatched/en route-from-where/arrived/on scene/cleared) is modeled as a thin Activity extension per the user's own framing ("they are activities related to the incident") — self-logged by a responder or logged on-behalf-of by a Supervisor, directly reusing Patrol Management's Mobile Patrol Unit location posture. It's explicitly interim, built now only because elicitation proceeds in MODULES.md order rather than implementation order — Dispatch/CAD (Module 2) will eventually own the canonical version.
  - **AI Draft Narrative is always a mutable working record, never auto-published.** Only human approval commits it as a real, immutable Incident Update (`source = ai_generated`, referencing its source draft and synthesized timeline data) — the same "AI proposes, human confirms" discipline already established for CLI-Style Input's AI-assist mode, now applied to narrative generation. General lesson: any future AI-generation feature touching a liability-weight record should default to this same propose-then-approve shape, never direct-to-record generation.
  - **Voice-to-text transcription is established here as the platform's shared capability**, fulfilling CLI-Style Input's own forward-reference to it — confirms that doc's earlier design correctly anticipated where the dependency would actually be built.
  - **Canned Quick Phrase (tenant-configurable, instant-log, zero-typing) is a new low-friction input pattern** worth reusing anywhere else "keep the user out of a typing posture in the field" matters.
  - AI Draft Narrative generation is the platform's first explicitly **online-only** step inside an otherwise offline-first flow — disclosed as a deliberate, bounded exception rather than compromising the general offline guarantee everywhere else.
  (See [ai-assisted-incident-report-writing.md](1-security-operations/ai-assisted-incident-report-writing.md) and the light retrofit to [incident-reporting-management.md](1-security-operations/incident-reporting-management.md)'s Incident Update.)
- **AI / LLM Services added as a new Module 0 Platform Core feature, resolving the "assumed AI capability" open question from AI-Assisted Incident Report Writing.** Owns three things every AI-generation consumer plugs into rather than reimplementing: (1) **provider abstraction** — SaaS-pooled tokens or tenant BYO API key, across Anthropic, OpenAI, Google Gemini, and Microsoft Azure OpenAI Service (a distinct provider from OpenAI's own API, relevant for DOE/federal Azure Government hosting) — with a BYO key encrypted at rest and never redisplayed in plaintext to any role, including Platform Super Admin; (2) **Prompt Templates** with `{placeholder}` substitution, schema-governed the same way Tenant-Defined Types & Custom Fields validates `extended_fields` (undeclared placeholders rejected at save time); (3) **Custom Instructions resolved by a category × location lookup** — a genuinely new scoping shape, deliberately distinct from Settings & Preferences' single hierarchical chain, resolving narrowest-match-wins across two independent axes (incident category, site) down to an always-present platform default.
  - **AI Context Registration formalizes "declare a use-case and its placeholders, never talk to a provider directly"** — AI-Assisted Incident Report Writing's `incident_narrative`/`voice_transcription` and CLI-Style Input's `cli_assist_translation` are the first three contexts, retrofitted to consume this registry instead of an unspecified capability. Any future AI-generation need should register a new context here rather than building its own provider/prompt plumbing.
  - General lesson: this is the fourth promotion-to-Platform-Core in this session (after Domain Events/Command Bus/Palette, Tenant-Defined Types & Custom Fields) — a strong recurring signal that when two or more features independently need the same non-trivial capability, the right move is a shared registry-and-resolution feature, not parallel bespoke implementations.
  (See [ai-llm-services.md](0-platform-core/ai-llm-services.md) and light retrofits to [ai-assisted-incident-report-writing.md](1-security-operations/ai-assisted-incident-report-writing.md) and [cli-style-input.md](0-platform-core/cli-style-input.md).)
  - **Requestor vs. recipient kept distinct, and requestor deliberately allowed to stay unregistered free text** — contrasted explicitly against `EmergencyContactAssociation`'s requirement of a real minimal Person entity: that mechanism is for a durable, dedup-worthy formal relationship, while a one-off courtesy caller isn't, so forcing an Entity record for every anonymous caller was rejected as the wrong tradeoff here. Recipient and any subject Item, when identifiable, use the ordinary `ActivityParticipantAssociation` mechanism — no new pattern needed there.
  - **Courtesy Patrol is its own Activity extension, only optionally linked from Patrol Management's Patrol Request** (`courtesy_patrol_ref`, retrofit) — a self-initiated courtesy act needs no dispatch step at all, while a dispatched one keeps Patrol Request as the generic wrapper and Courtesy Patrol as the richer fulfillment detail, the same launch-point relationship Checkpoint Scan has to Patrol Finding.
  - **Courtesy Patrol explicitly does NOT reuse Checkpoint Scan's dedup-debounce exception** — two independently-logged courtesy interactions are meaningfully distinct occurrences (unlike routine scans), so standard Activity dedup applies. Reinforces the general lesson from Guard Tour's review: the dedup exception is for genuinely event-shaped, near-zero-distinct-identity records, not a default to reach for whenever volume is high.
  (See [courtesy-patrol.md](1-security-operations/courtesy-patrol.md).)
- **Module 2 (Dispatch/CAD) opens with Call Intake & Logging, which generalizes/supersedes Patrol Management's ad hoc Patrol Request as the CAD front door**, exactly as that doc anticipated. Call is its own Activity extension (fulfilling Activity Registry's already-anticipated `call` activity_type) — the universal intake record for phone/radio/walk-up, self-service portal/app, and (deferred, flagged as forward references only) future automated-sensor and 911/PSAP/CAD-to-CAD ingest. A Call is deliberately not an Incident: many calls resolve without ever escalating. Three reusable refinements:
  - **A third distinct rationale for opting a record out of Entity Registry Core's heavy tombstone/merge dedup.** Checkpoint Scan's carve-out was "no real duplicate-identity risk"; Call's is different — multiple calls about the same event are individually valuable reporting data (call volume, first-report timing), so merging away duplicates would destroy signal rather than reduce noise. Call instead gets a lightweight, real-time, dispatcher-facing `related_call_ref` link: grouped for response purposes, but every linked call keeps its own full record. General lesson: before inheriting a shared mechanism's default weight, check *why* it exists, not just whether the record type looks similar to one that already opted out.
  - **Caller identity reuses Courtesy Patrol's Requestor shape** (free text by default, optional `caller_party_ref` link), not Incident's mandatory-Party-association pattern — reinforces that the right identity-strength choice depends on whether the relationship is durable/dedup-worthy or a one-off.
  - **Call Type and Call Priority are their own tenant-configurable Definitions, deliberately independent of Incident's Severity Definition** — response urgency in the moment (Call Priority) and seriousness/liability after the fact (Incident Severity) are related but distinct axes; a future feature needing both an "in the moment" and an "in hindsight" classification should keep them as two Definitions, not one.
  - Patrol Request is retrofitted with an optional `source_call_ref` (additive, existing lifecycle unchanged) so a triaged Call can result in direct-unit tasking; Patrol Request keeps its own simpler lifecycle for tasking that bypasses full call intake. A Call reaching `dispatched` hands off to a Dispatch Activity extension owned entirely by the next doc, Unit Dispatch & Proximity Routing (which will set its own `source_call_ref` back to Call) — no Dispatch-side fields speculated here.
  (See [call-intake-logging.md](2-dispatch-cad/call-intake-logging.md) and the retrofitted [patrol-management.md](1-security-operations/patrol-management.md).)
- **Unit Dispatch & Proximity Routing establishes Dispatch as the canonical response-phase timeline, formally superseding AI-Assisted Incident Report Writing's interim Response Timeline Event, exactly as that doc flagged it would.** Dispatch registers as its own Activity extension (fulfilling Activity Registry's already-anticipated `dispatch` activity_type), one row per assigned officer per Call — a multi-unit response to one Call is multiple independently-phase-tracked Dispatch records sharing one `source_call_ref`. Three reusable refinements:
  - **The dispatchable "unit" is an on-duty officer, not a pre-configured resource record.** Per explicit user direction, Dispatch draws its candidate pool from any currently on-duty Person (using GIS & Mapping Services' existing on-duty GPS gating), optionally carrying Patrol Management Post/vehicle context (`assigned_post_ref`, `assigned_vehicle_ref`) when one exists — a Post is never a prerequisite for dispatchability. General lesson: when a module needs "an assignable person," check whether an existing assignment/scheduling construct (Post) is actually required first, or whether the person themselves, on-duty status alone, is the real dependency.
  - **Proximity routing is scoped honestly against GIS's own architecture**: straight-line nearest-available-officer ranking (reading GIS Position Record directly, no separate position store), Dispatcher always confirms, true driving-distance/ETA routing explicitly deferred pending a routing-capable adaptor — avoids committing this doc to a GIS capability that doesn't exist yet.
  - **Availability is a minimal derived signal** (on-duty + no open Dispatch), explicitly not a full status system — deferred to **Status & State Monitors** (later in this module's elicitation queue), which this doc flags as the intended owner of richer real-time unit-status states (break, out of service, training, etc.).
  - **Retrofit**: Response Timeline Event (AI-Assisted Incident Report Writing) gains `source_dispatch_ref` — for an Incident escalated from a Call with a matching Dispatch record, four of its six phases (`dispatched`/`en_route`/`arrived`/`on_scene`/`cleared`) now derive from that Dispatch rather than being independently logged a second time; `call_received` continues sourcing from Call's own `received_at`. A self-initiated Incident with no upstream Call/Dispatch is entirely unaffected — the original mechanism stays fully live for exactly the case it was built for. General lesson: an explicitly-flagged "interim, pending module X" mechanism should be revisited and reconciled the moment module X is actually specified, not left to silently duplicate the real thing indefinitely.
  (See [unit-dispatch-proximity-routing.md](2-dispatch-cad/unit-dispatch-proximity-routing.md) and the retrofitted [ai-assisted-incident-report-writing.md](1-security-operations/ai-assisted-incident-report-writing.md).)
- **Active Incident Queue (CAD Console) confirms Activity Registry's universal-mechanism payoff a second time, now for live/current state rather than history.** Per explicit user framing ("part of the reason for doing the activity base type was so a dispatcher could see a list of all the activities currently in progress"), the console is a CQRS read-model over Activity Registry itself — every `open`/`in_progress` Activity of any registered type platform-wide, not a bespoke Call/Dispatch/Incident list — mirroring Entity Relationships & History's Interaction Timeline (the historical view) with this as its live/current-state counterpart. A future module's Activity extension automatically appears in both, zero console-side change required. Two reusable refinements:
  - **Cross-type normalization belongs at the consuming read-model, not retrofitted onto every producer.** Call Priority and Incident Severity are deliberately independent Definitions (established when Call Intake & Logging was written); rather than adding a universal priority field to every Activity extension just so this one console can sort across types, a tenant-configurable **Queue Urgency Mapping** normalizes `(activity_type, field, value) → urgency_tier` entirely at this doc's own layer, with a graceful `unclassified` fallback for anything unmapped. General lesson: when only one consumer needs a cross-cutting normalization, put the mapping at that consumer, not a retrofit touching every producer — reserve the "promote to a shared mechanism" move (see the four earlier promotions logged above) for when two or more consumers actually need it.
  - **"Universal query, scoped default preset" is the right shape when a mechanism is intentionally broader than any one screen's default use.** The query itself stays universal (any Activity type); a Settings & Preferences-registered default filter preset (here: Dispatch/CAD + Patrol Management types) narrows what a Dispatcher sees day-to-day without limiting what's actually queryable — any future feature building a live/aggregate view over a deliberately generic base mechanism should default to this same split rather than hardcoding a narrower query.
  - Explicitly scoped apart from **Multi-Incident Console** (later, this module): this doc is the filterable/sortable/groupable list; Multi-Incident Console is a separate multi-pane workspace for working several incidents at once, built on the same underlying read-model.
  (See [active-incident-queue-cad-console.md](2-dispatch-cad/active-incident-queue-cad-console.md).)
- **Active Incident Queue retrofitted with a Card/Feed model after the user pushed back on the flat-list design** — a Patrol (or any card) should show its own high-volume children (a Checkpoint Scan, a per-officer Dispatch, an Incident Update) as a nested live status feed *inside* its card, not as separate top-level rows. Every participating Activity type now declares a **queue role** (`card`, `feed`, or `none`, default `none`), registered locally to this doc rather than retrofitting Activity Registry Core or any producing module's own schema — the same "normalize at the consumer" call already made for Queue Urgency Mapping. A `feed` type nests via its own already-established direct parent-reference field (Checkpoint Scan's `patrol_ref`, Dispatch's `source_call_ref`, Incident Update's `origin_incident_ref`) — no new field added anywhere. Three reusable refinements:
  - **Explicit opt-in, not automatic participation, is the right call for a live operational board** — a deliberate divergence from Entity Relationships & History's fully-automatic timeline participation. General lesson: "any registered type automatically participates" is right for a paginated per-entity history view (Entity Relationships & History), but wrong for a live, concurrently-viewed, unbounded-cardinality operational board, where an unvetted type appearing by default is a real clutter/performance risk, not just a completeness nicety — match the participation model to the consumer's actual exposure profile, not a blanket "always automatic" default.
  - **A parentless Feed record promotes to a standalone Card rather than vanishing** — reuses the exact same instinct behind Courtesy Patrol's own self-initiated-vs-dispatched duality (no parent Patrol Request ⇒ Courtesy Patrol already stands alone). General lesson: a "nest under a parent when one exists" mechanism needs an explicit, non-silent fallback for the no-parent case, not an assumption that a parent always exists.
  - **Nesting is deliberately capped at one level**, resolved for Guard Tour's real three-tier chain (Route Assignment → Patrol → Checkpoint Scan) by registering the middle tier (Patrol) as the card and the top tier (Route Assignment) as `none` — sidestepping, not solving, the general transitive-rollup case, flagged honestly as an open question rather than over-built now.
  (See the reworked [active-incident-queue-cad-console.md](2-dispatch-cad/active-incident-queue-cad-console.md).)
- **Status & State Monitors promotes Unit Dispatch & Proximity Routing's minimal "derived, not stored" availability signal into a real Unit State record, exactly as that doc flagged it would.** Unit State (`available`/`dispatched`/`en_route`/`on_scene`/`completed`/`out_of_service`) transitions automatically in lockstep with an officer's own Dispatch phase, plus manual transitions (`out_of_service`) no Dispatch drives. Three reusable refinements:
  - **`completed` is a deliberate resting state, not an instant bounce-back to `available`** — real dispatch practice needs a report-writing buffer; returning to service (or going `out_of_service` next) is its own explicit action. General lesson: when promoting a derived signal into a real state machine, look for the intermediate real-world states a same-shaped instant on/off flag would have hidden.
  - **Reused the Command/Action Bus confirmation gate for a new purpose**: assigning a Dispatch to an `out_of_service` officer requires passing that same existing gate rather than inventing new override-confirmation logic — the fourth-ish reuse of that mechanism beyond its original step-up-auth motivation.
  - **A missed Safety Check-in is its own permanent record, not just a notification blip** — directly reapplying Guard Tour's "a negative outcome deserves a real row" lesson (the missed-Patrol precedent) to a brand-new domain (officer welfare safety timers), confirming that lesson generalizes well beyond where it was first established.
  - Time-on-Scene Watchdog and Safety Check-in both publish automation-eligible domain events rather than hardcoding an alert path — the same trigger/effect split used everywhere else in the platform, now applied to physical officer-safety monitoring for the first time.
  - **Retrofits**: Call Intake & Logging's Call Type Definition gains `default_requires_safety_checkin` (Dispatcher-overridable per Dispatch); Unit Dispatch & Proximity Routing's proximity suggestion now filters on real Unit State instead of its own local derivation.
  (See [status-state-monitors.md](2-dispatch-cad/status-state-monitors.md) and the retrofitted [unit-dispatch-proximity-routing.md](2-dispatch-cad/unit-dispatch-proximity-routing.md) and [call-intake-logging.md](2-dispatch-cad/call-intake-logging.md).)
- **Follow-up round on Status & State Monitors, after being called out for drafting the first pass without asking.** Three refinements: (1) `completed`'s advance-to-`available` behavior is a tenant-configurable **Completed State Policy** (`manual`/`auto`/`timer`), not a fixed manual-only rule as first drafted. (2) Safety Check-in confirmation is **self- or on-behalf-of**, matching every other real-time confirmation mechanism in the platform (Dispatch phases, Mobile Patrol Unit location) — a tenant-configurable `prompt_method` decides whether the interval reminder pings the officer's device directly, prompts the Dispatcher to make contact instead, or both. (3) A missed check-in's notification carries a one-tap **Escalate** action (reusing the established launch-point mechanism to create an Incident with context pre-filled) rather than being purely passive — escalation stays a deliberate human decision, just one tap away instead of a manual chore. General process lesson: this feature's first draft skipped the AskUserQuestion round entirely, going straight from MODULES.md's bullet list to a full doc — a real deviation from this session's established process, not just a style choice. The fix isn't to only ask on "new" features; every feature gets a real elicitation round, even one with heavy existing context to draw on.
- **Silent Mobile Dispatching splits "silent" into two independent axes rather than one bundled mode: delivery style (audible vs. silent alert) and radio posture (radio callout expected vs. bypassed).** Both resolve via the platform's now-familiar explicit-beats-default chain (Dispatcher flag > Call Type default > — for delivery style only — the officer's own personal Notifications Engine preference > platform default), reusing Notifications Engine's existing push/category/preference mechanism rather than building new delivery infrastructure; this doc's own scope is narrow: the resolution rule, a dispatch-specific **Acknowledgment** receipt distinct from Dispatch phase logging, and unacknowledged-dispatch escalation via Domain Events. Two reusable refinements:
  - **Radio posture is an expectation-setting flag surfaced to the Dispatcher console, not a technical control over physical radio hardware** — the platform can't suppress an actual radio transmission (that's out of scope, adjacent to Module 19's future integration territory), so this doc is explicit that `radio_bypass` is a documented operational signal, not an enforced behavior. General lesson: when a "bypass X" requirement touches something genuinely outside the software's control (physical hardware, a human's own radio habits), scope the doc honestly as an indicator/reminder rather than implying an enforcement mechanism that doesn't exist.
  - **Acknowledgment reuses the zero-friction-tap pattern (Canned Quick Phrase, Safety Check-in) for a new purpose** — confirming "I saw this" distinct from "I'm responding" (Dispatch phase) — with an explicit fallback rule (a later phase log counts as implicit acknowledgment) so an officer who's clearly already moving is never wrongly flagged as unresponsive.
  - **Retrofits**: Dispatch gains `silent_delivery`/`radio_bypass`/`acknowledged_at`; Call Type Definition gains `default_silent_delivery`/`default_radio_bypass` — following the exact same retrofit shape as Safety Check-in's `default_requires_safety_checkin`, now proven three times as Call Type Definition's standard place for a Dispatcher-overridable per-type default.
  (See [silent-mobile-dispatching.md](2-dispatch-cad/silent-mobile-dispatching.md) and the retrofitted [unit-dispatch-proximity-routing.md](2-dispatch-cad/unit-dispatch-proximity-routing.md) and [call-intake-logging.md](2-dispatch-cad/call-intake-logging.md).)
- **Multi-Incident Console turned out much richer than "pin a few Incidents side by side" — a genuine dockable, IDE-style multi-panel workspace**, per explicit user direction: a Panel Registry (Map, Queue, Kanban, Unit Roster, Detail) feeding flexible dock zones/tabs, every panel fully interactive (a Detail panel is the pinned record's real working surface, not a preview). Three reusable refinements:
  - **A new visualization mode (Kanban) over an existing read-model, not a new data source.** The Kanban panel groups Active Incident Queue's same Card data into columns; dragging a card invokes that Activity's own already-registered status-transition action, never a new business rule. General lesson: a new *view* over data that already has correct write-paths should stay a pure presentation layer — don't let a visualization gesture (drag-to-column) grow its own parallel mutation logic.
  - **First feature to need a genuinely dockable multi-panel workspace, flagged as a likely-but-not-yet-confirmed promotion candidate.** Command Center Wallboard View (Module 3, not yet specified) looks like a strong second consumer of the same Panel Registry/dock mechanism. Consistent with the platform's established pattern (promote only once a *second* real consumer shows up, not preemptively), this doc keeps the mechanism local but explicitly flags the likely reuse so it isn't rebuilt from scratch later — a middle ground between premature promotion and forgetting the precedent exists.
  - **Agency Handoff Log always resolves to a registered Organization Party (with inline creation), unlike Call's caller or Courtesy Patrol's requestor** — a deliberate divergence from the "free text by default" identity pattern, per explicit user direction, since an external agency handoff is exactly the durable, dedup-worthy relationship `EmergencyContactAssociation`-style patterns were designed for, not a one-off anonymous interaction.
  - EOC Activation is deliberately a thin trigger record only — full EOC mechanics stay owned by Command Center/Emergency Management (neither specified yet), reusing the confirmation gate (and optional tenant-flagged step-up) rather than inventing new high-consequence-action gating.
  - **Retrofit**: Agency Handoff Log registers as a `feed`-role type under Incident in Active Incident Queue, alongside Incident Update — the second feed-role registration contributed by a doc other than the one that originally defined the Card/Feed model, confirming that registration point generalizes cleanly to new contributors.
  (See [multi-incident-console.md](2-dispatch-cad/multi-incident-console.md) and the retrofitted [active-incident-queue-cad-console.md](2-dispatch-cad/active-incident-queue-cad-console.md).)
- **Active Call Alerts & Timers generalizes Status & State Monitors' Time-on-Scene Watchdog into a reusable Duration Watchdog mechanism rather than building a second parallel timer system**, per explicit user direction — `(activity_type, watched_field, watched_value)` → duration, with Pending Call Alarm (`call`/`status`/`queued`) and Enroute Timer (`dispatch`/`phase`/`en_route`) as two more registered instances alongside the original Time-on-Scene (`dispatch`/`phase`/`on_scene`). Two more reusable refinements:
  - **Alarm delivery mode (`one_shot` vs. `persistent`, tenant-configurable per instance) is the real "sound alerts in the dispatch room" mechanism**, added to the generalized Duration Watchdog itself rather than being special-cased to Pending Call Alarm alone — a persistent alarm auto-silences on condition-resolve or explicit Acknowledge, never lingers about something already handled. General lesson: when a new requirement ("needs a real alarm, not just a notification") turns out to be a property of an *existing* mechanism rather than a wholly new one, add the property to the shared mechanism so every current and future consumer gets it, not just the one that prompted it.
  - **Critical Event Escalation Policy unifies "Supervisor Notifications" into one tenant-configurable second-tier escalation surface** over every critical-event-publishing trigger in the module (any Duration Watchdog instance, missed Safety Check-in) — a Settings-registered convenience layer over Domain Events (picks watched event types + additional delay + escalation target), not new delivery infrastructure, mirroring how Call Priority/Incident Severity are Settings-registered Definitions layered over lower-level mechanisms elsewhere. Prevents a tenant from having to hand-author and keep in sync N separate ad hoc Domain Events rules for what's conceptually one policy.
  - **Retrofit**: Status & State Monitors' "Watchdog Threshold" is renamed/generalized to "Duration Watchdog" with new `alarm_mode`/`resend_interval` fields — the first Module 2 doc to retroactively widen a sibling doc's mechanism into something explicitly reusable rather than either duplicating it or leaving it narrowly scoped.
  (See [active-call-alerts-timers.md](2-dispatch-cad/active-call-alerts-timers.md) and the retrofitted [status-state-monitors.md](2-dispatch-cad/status-state-monitors.md).)
- **Historical CAD Log Reconstruction closes out Module 2, corrected twice by the user before drafting — a genuinely useful pre-draft catch.** First correction: it's DAR-shaped (a filtered, chronological view over Activity Registry, snapshotted to Document — the fourth consumer of that architecture after DAR/Passdown/Incident Report), not a new logging system, and it explicitly does **not** source from Structured Logging & Audit Trails — that stays a separate, background, rarely-accessed compliance mechanism; high-precision timestamps are satisfied by business records' own already-established fields (Dispatch's `phase_timestamps`, Incident Update's `update_timestamp`, etc.), correcting a literal reading of MODULES.md's "audit logs" wording against the platform's actual layered architecture. Second: two filter axes, both DAR-shaped (person/site/time there → dispatcher-or-activity/time here) — **dispatcher/shift-axis** (strictly chronological, interleaved across every Activity a dispatcher touched — actions on Incident 1, 3, 1, 2 show in that literal order, never regrouped) and **activity-axis** (a specific Call/Incident's full descendant tree, multi-dispatcher, unbounded depth). Two reusable refinements:
  - **The dispatcher-axis view is the opposite grouping from Active Incident Queue's Card/Feed model, and both are correct for their own purpose.** A live board groups by parent Activity because that's what a Dispatcher needs *right now* (working one situation at a time); a shift reconstruction needs the literal temporal sequence across situations because that's what actually happened. General lesson: two views over the same underlying data can legitimately want opposite groupings — don't assume a single "canonical" shape has to serve every consumer.
  - **Unbounded-depth reconstruction resolves Active Incident Queue's own flagged open question** (genuine transitive Feed nesting) for the specific case that doesn't need solving live — a point-in-time report has no live-board clutter/performance constraint, so the activity axis just walks the full existing parent-ref chain (reusing that doc's `parent_link_field` registrations directly, no new mechanism) rather than needing the Card/Feed model's one-level cap lifted. General lesson: a constraint that's real for a live/continuous view isn't necessarily real for an on-demand/point-in-time view over the same data — check which one actually applies before assuming a limitation carries over.
  - **Honest gap flagged, not silently patched**: several Module 2 records (Call's status-transition timestamps, Dispatch's single `recorded_by`) don't yet carry per-action actor attribution the dispatcher axis ideally wants; Log Source Registration (local to this doc) degrades gracefully by excluding unregistered types rather than the doc pretending full fidelity exists today.
  (See [historical-cad-log-reconstruction.md](2-dispatch-cad/historical-cad-log-reconstruction.md) and the retrofitted [active-incident-queue-cad-console.md](2-dispatch-cad/active-incident-queue-cad-console.md).)
- **Module 2 (Dispatch/CAD) complete — 8/8 features.**
- **Further refinement: Route Assignment is a rollup Card, not a `none`-role type, and default presets are role-scoped.** The user pointed out a Dispatcher works individual Patrols while a Supervisor needs to monitor whole-Route completion — Route Assignment needed *some* visibility, just not on a Dispatcher's default screen. Resolved two ways: (1) Route Assignment registers as `card`, but a rollup-only one with no Feed children — it surfaces its own already-computed `completeness_pct` (established when Guard Tour & Checkpoint Verification was written) via its computed display label, rather than nesting every underlying Patrol as a Feed line (which would double-display Patrol, already its own independent Card, and would require the genuine two-level nesting this doc's Feed model deliberately doesn't support). (2) The console now ships **two day-one default presets, not one** — Dispatcher Default (individual actionable units) and a broader Supervisor/EOC Default (adds Route Assignment) — both just ordinary role-scoped Saved Views using the mechanism already specified, not a new concept. General lesson: when a "should this be visible" question turns out to depend on *who's asking*, look first at whether the viewing-scope mechanism you already built (role-scoped default views) solves it before reaching for a new visibility axis — and when a coarser parent record already computes a rollup field, surface that field directly instead of nesting the fine-grained children a second time.
- **Offline architecture revised during platform design review: "1:1 offline parity" rejected as a requirement; hybrid CRDT + field-level LWW retired.** Per explicit user direction, grounded in 20+ years of security-management domain experience: most facilities — including DOE underground areas — have working WiFi; the truly offline zones (SCIFs and similar) are places a device typically cannot enter at all, which is exactly why the platform's Dispatcher on-behalf-of capability was designed to cover ~95% of an officer's system actions via radio relay. Offline device capability is therefore a **minimal capture subset with accepted degraded performance**, governed by a three-class contract: (1) **create new records offline** (client UUID, freely editable locally until first sync — single-writer and unshared, so safe); (2) **append rows to existing records' timelines** (notes/updates/acknowledgments, plus state-transition events only on execution records the user is the sole assigned actor of, e.g., their own Patrol); (3) **never mutate shared server-side records offline** (Incident status/severity/assignment, plan definitions, settings — online or Dispatcher-relayed only; an offline officer can at most *append* a "requested status change" timeline event for later confirmation). Key consequences and lessons:
  - **Conflicts are impossible by construction** under single-writer + insert-only + unshared-until-sync, so the entire conflict-resolution apparatus (field-level LWW engine, supervisor reconciliation queue, conflict flags) is **deleted as a requirement, not deferred**. What remains is boring and verifiable: a durable outbox, per-device monotonic sequence numbers (no vector clocks — single writer per device), dual timestamps (client-claimed + server-received, drifting field clocks assumed), idempotent replay keyed by client UUID, and batch-internal UUID reference resolution.
  - **Multi-officer concurrent notes on one Incident are not a conflict case**: the update-timeline architecture (each Incident Update its own immutable row) already converted would-be shared mutation into independent appends — set-union merge, interleaved for display by client-claimed time with server-received tiebreak. This is a quiet payoff of the "record = timeline of updates" report architecture.
  - **Stale-context authorship is accepted, not fixed**: an offline officer appending a note without having seen concurrent entries is radio-era operational reality; the record honestly shows what each author knew and when. No merge complexity exists to paper over it.
  - **Air-gapped facilities are a deployment-model concern, not a client-sync concern** — they run a self-hosted local instance and clients there are online to it. The prior doc's multi-day fixed-terminal offline requirement is retired accordingly.
  - **Tripwire**: any future feature proposing offline *co-editing of shared mutable state* reopens this decision and must be flagged against this file before drafting; the default answer is "radio it in."
  - **The Dispatcher on-behalf-of pattern is elevated to a first-class cross-cutting design principle**, not a per-feature convenience: future modules must preserve a connected actor's ability to perform a disconnected officer's workflow actions on their behalf, because it — not device-side sync — is the platform's primary disconnected path.
  - Open question recorded for a future market/persona decision: lone-officer commercial sites with no dispatcher have no on-behalf-of path — the cached capture subset may need to grow (still strictly append-only) for solo-officer self-sufficiency.
  - **Retrofits**: [offline-data-sync.md](0-platform-core/offline-data-sync.md) rewritten around the outbox contract; [daily-activity-reports.md](1-security-operations/daily-activity-reports.md) and [guard-tour-checkpoint-verification.md](1-security-operations/guard-tour-checkpoint-verification.md) integration notes updated (CRDT → append-only outbox); `docs/pdd.md` offline constraint and risk-table row softened; `docs/MODULES.md` conflict-resolution bullet corrected.
- **Unit State concurrency defect resolved: a tenant-configurable Dispatch Stacking Policy guarantees at most one active Dispatch per officer, making single-valued Unit State well-defined by construction.** The platform design review flagged that Status & State Monitors' single-valued Unit State sat on Unit Dispatch's allowance of multiple concurrent Dispatches, with "whichever Dispatch most recently changed phase" as the tiebreak — a shrug in a safety-relevant feature. Resolved per user direction ("tenant configuration can have one call or a queue"):
  - **Dispatch Stacking Policy** (Settings & Preferences-registered, `single_active` | `queued`): under either mode, at most one Dispatch per officer is ever in the active phase range — `single_active` (default) forces an explicit confirmation-gated preempt-or-cancel to reassign a busy officer; `queued` creates additional Dispatches in a new `queued` phase (visible to dispatcher and officer, driving nothing until activated).
  - **Queue advance is always an explicit human action** (officer self, or Dispatcher/Supervisor on-behalf-of), never automatic — and is explicitly permitted from the `completed` Unit State, temporarily cutting the report-writing buffer short by deliberate choice; the Completed State Policy governs passive return-to-available, never blocks an explicit advance. Consistent with the platform's explicit-action-over-silent-automation posture (patrol starts, safety check-ins).
  - **Preemption is a real terminal state, not a cancel**: pulling an officer off a call for higher priority marks the Dispatch `preempted` (with `preempted_by_dispatch_ref`, partial phase history preserved) — reporting can distinguish "abandoned" from "pulled for higher priority," per the established negative-outcome-gets-a-real-row discipline. The interrupted call's disposition is the **Dispatcher's per-preemption choice** (back to pending pool, or re-queue to the same officer under `queued`); a resumed response is a new Dispatch with `resumes_dispatch_ref`, never a reopened row.
  - **Guard Tour retrofit — Patrol gains an `interrupted` status**: a Dispatch becoming active for a guard mid-Patrol auto-transitions the Patrol to `interrupted` (`interrupted_by_dispatch_ref`, cycles in `interruption_history[]`), resuming on next in-scope scan or explicit resume; window close while interrupted concludes with partial completeness attributable to the Dispatch. Missed points remain missed points — but the *reason* is now a queryable reference, not an unexplained skip.
  - General lesson: when a single-valued state machine coexists with a multiplicity allowance elsewhere, don't patch the state derivation (priority rules, most-recent-wins) — constrain the multiplicity itself at the source with an explicit invariant, and let the state machine stay trivially correct.
  (See the retrofitted [unit-dispatch-proximity-routing.md](2-dispatch-cad/unit-dispatch-proximity-routing.md), [status-state-monitors.md](2-dispatch-cad/status-state-monitors.md), and [guard-tour-checkpoint-verification.md](1-security-operations/guard-tour-checkpoint-verification.md).)
- **Merge/unmerge state machine fully specified in Entity Registry Core, closing the design review's largest open defect.** The prior spec was three sentences ("redirect every reference," "transactionally safe," "reversible") hiding a distributed-consistency problem and an unanswered post-merge-reference question. Resolved through elicitation (four user decisions) plus engineering drafting:
  - **Instant switchover + background rewrite** (user choice): confirming a merge executes one atomic transaction (tombstone + `merged_into_ref` + survivorship writes); from that instant the merge is behaviorally complete — reads resolve redirects transitively, writes arriving with a tombstoned reference are re-pointed at write time, and an idempotent, resumable background job physically rewrites stored references over the following minutes. "Transactionally safe" is achieved by construction (behavioral invisibility of the rewrite), not by one giant lock — the NFR was rewritten to say so honestly.
  - **Field survivorship: field-by-field pick with survivor default** (user choice, hybrid of the offered options): the confirmation flow presents only genuinely conflicting fields (all shared TPT levels + colliding `extended_fields` keys), survivor's value pre-selected, admin flips individual fields; every choice logged in `survivorship_log[]`, which is also what makes reversal's field restoration possible. Same-extension-type collisions resolve the same way; loser-only extensions carry over wholesale.
  - **BOLO Flags always carry over unmodified, both sides'** (user choice): each remains an independent governed record; a redundant pair triggers a post-merge review notification, and clearance goes through the standard Supervisor+/step-up gate — flag governance is never delegated to the merge flow or the merging admin.
  - **Reversal: restored Entity goes active immediately; post-merge references are admin-allocated, never defaulted** (user choice): references created during the merged period land on an allocation worklist and the reversal only reaches `reversal_completed` when every item is explicitly assigned to survivor or restored Entity — misattribution is precisely the harm reversal exists to fix. Interim state is honest and visible: both records operable, unallocated references still resolving to the survivor, flagged on both records. Chained merges reverse LIFO.
  - **Reference Field Registration** (new build-time catalog): every direct entity-reference field across every module registers itself, mirroring the platform's established registration discipline — this is what turns "redirect every reference across every module" from an aspiration into a mechanically enumerable operation, resolving the review's "implies a global registry of referencing columns that no doc specifies" gap. EntityAssociation rows need no registration (structurally enumerable).
  - **Post-redirect association collisions are auto-resolved and logged, surfaced in a post-merge review summary**: exact duplicates collapse (keep earliest); single-current-value kinds keep most-recent-active and soft-remove the older (the existing active/removed history mechanism absorbing the collision); a row made self-referential by the redirect is soft-removed and flagged — a useful late signal the "duplicates" may actually have been two related people.
  - General lesson: a mechanism described as "transactionally safe" across module boundaries in a CQRS platform almost always actually means "atomic switchover + convergent background repair + read-time indirection" — spec that shape explicitly rather than leaving "transactional" to be discovered as impossible during implementation.
  (See the expanded Deduplication, merge & reversal section of [entity-registry-core.md](0.5-master-records/entity-registry-core.md).)
- **Merge follow-up: explicit per-type mergeability (`is_mergeable`) and a no-new-storage Merged Records view — user's `merged_fields` JSON proposal refined rather than adopted verbatim.** The user proposed an "IMergeable" shape with a `merged_fields` JSONB column holding values displaced on merge. Resolution after engaging the tradeoff:
  - **Adopted outright — the interface framing.** Entity Type Registration gains `is_mergeable` (default true): a type registered false is skipped by dedup matching and can never be a merge survivor or loser, by contract. This formalizes what was previously implicit-by-omission (a type with no match signals just never flagged) and resolves Guard Tour's standing "Activity Registry should eventually document a high-volume opt-out" flag — Checkpoint Scan and Safety Check-in register false; Activity Registry #13 now requires every future extension to declare it explicitly. Also pre-answers the merge dimension of the pending "everything is an Activity" volume-valve defect for alarm-type extensions.
  - **Declined — the JSON column.** Displaced values already exist in two authoritative places: the tombstone (full loser record retained readable in place) and `survivorship_log[]` (which uniquely also covers displaced survivor-original values when the admin picked the loser's value). A third denormalized copy would drift and would put a second, ungoverned JSONB blob on the Entity root beside the platform's single deliberately-governed one (`extended_fields`).
  - **Adopted instead — the operational need behind the proposal**: a **Merged Records panel** on the survivor's profile, rendered live from tombstone lookup (`merged_into_ref` back-reference) plus survivorship log — displaced values visible on the record with zero new storage. If reporting ever needs to filter on former values without joining merge history, a derived cache may be added then, explicitly as a rebuild-on-reversal projection of the survivorship log, never a second source of truth.
  - General lesson: when a proposal is "add a field to hold X," first check whether X already exists authoritatively somewhere and the real need is a *view* of it — and separately check whether the proposal's framing (here, the per-type interface) is independently valuable even if its storage mechanism isn't.
  (See [entity-registry-core.md](0.5-master-records/entity-registry-core.md) #10a/#14a, [activity-registry.md](0.5-master-records/activity-registry.md) #13, and the closed-out flag in [guard-tour-checkpoint-verification.md](1-security-operations/guard-tour-checkpoint-verification.md).)
- **Correction: Tenant-Defined Types re-anchored to concrete types — the "no developer-built table at all" framing skewed the user's original intent.** The user's intent was always: "add a *kind* of Activity (or Item, or Document) I didn't foresee, give it custom fields through the extended_fields mechanism — but always built over a concrete type in the system." The earlier promotion drift let a tenant register a free-floating first-class Extension/Association Type (`concrete_schema_ref = null`, its own display-label strategy, its own dedup signals, the "Access Badge Log" example) — closer to a Salesforce custom-objects system than the intended mechanism, and inconsistent with the doc's own anti-EAV posture. Corrected throughout:
  - **Tenant-Defined Subtype** replaces Tenant-Defined Type: always declares a concrete developer-built **anchor** (a base type like Activity/Document, a concrete extension like Vehicle, or base EntityAssociation for relationship kinds). A subtype's records ARE real records of the anchor — real rows in the anchor's TPT chain — designated via a new nullable, concrete, filterable `tenant_subtype_ref` column on the Entity root (`entity_type` stays the anchor's; the subtype is a designation within the anchor, not a new discriminator value).
  - **Everything is inherited from the anchor because there is nothing new to treat**: lifecycle, display labels (anchor's strategy, subtype name available as a token — "Wildlife Encounter #217"), `is_mergeable` posture (a subtype can never widen mergeability beyond its anchor), offline write classes, timeline inclusion, queue participation. The former FRs granting a tenant-defined type its own label strategy and standalone dedup config are gone; a subtype may only *add* optional `extended_fields`-keyed match signals on top of the anchor's.
  - **Entity Type Registration is developer-only again**: `is_tenant_defined`/nullable-`concrete_schema_ref` removed — Tenant-Defined Subtypes live in their own catalog (in tenant-defined-types-custom-fields.md), anchored by reference to a registration, never rows within it.
  - **This is the generalization of a pattern the platform already used ad hoc everywhere** (Call Type Definitions, DAR Entry categories, Courtesy Patrol categories — all tenant-defined kinds of a concrete type), which is itself the tell that the anchored model was the right shape all along. The graduation path also gets cleaner: promoting a popular subtype means building a real concrete extension of the same anchor and migrating designation → extension rows, not conjuring a table for a previously tableless type.
  - General lesson: when generalizing a locally-drafted mechanism into a platform feature, re-check the generalization against the original motivating intent before widening it — "further generalized" can quietly cross from *the same idea, shared* into *a different, bigger product* (here: custom subtypes vs. custom objects), and the drift reads as progress until the original author looks at it.
  (See the corrected [tenant-defined-types-custom-fields.md](0-platform-core/tenant-defined-types-custom-fields.md), retrofitted [entity-registry-core.md](0.5-master-records/entity-registry-core.md) (Entity root gains `tenant_subtype_ref`; type registrations developer-only), and the updated load-bearing summary in `_RESUME.md`.)
- **Code-architecture guidance documented as a standing guidance doc, not a feature spec: `docs/architecture-guidance.md`.** Per the user's stated build philosophy (interfaces + abstract classes + inheritance as the primary pattern), the stance is recorded once, deliberately outside the requirements template since it has no actors/lifecycle/acceptance criteria — it governs how implementation code gets shaped. Core rule: **inherit what a thing IS** (the TPT identity taxonomy, mapped 1:1 to C# class hierarchies, abstract ancestors, structural depth fine / behavioral depth avoided, sealed by default); **interface what a thing CAN** (capabilities cross-cut the taxonomy — IMergeable, IDisplayLabeled — never pushed into base classes; memberless marker interfaces are registry metadata in costume); **compose what a thing USES** (services/workflows injected over capability interfaces, no template-method chains). Platform-specific constraint that outranks OO preference: **the runtime registry is authoritative and interfaces only mirror it**, because Tenant-Defined Subtypes have no code class and can never implement a compile-time interface — registration wins on any disagreement, with a recommended startup validation asserting developer-built types' interfaces match their registrations. Includes the earn-your-existence test (a class/interface must add fields, members, or behavior), with the named field-less EntityAssociation kinds recorded as a deliberate requirements-level exception whose physical mapping (TPT vs. TPH under a named C# type) stays a technical-spec choice. `_RESUME.md` updated to point implementation-adjacent sessions at it.
- **Contradiction-fix batch from the platform design review — five Master Records defects plus the air-gapped AI conflict, resolved (three elicited, three drafted):**
  - **Display labels required at instantiable levels only** (drafted): Entity Registry Core #26's "every level, from the most generic (Party)" contradicted Party Registry's own (correct) "Party declares no label — it always comes from the Person/Organization extension." Resolved in Entity Registry Core's favor of the Party doc: effectively-abstract levels (Party, the Entity root) are exempt and **delegate** to the most-specific extension's label — consistent with the architecture-guidance doc's abstract-class posture (abstract levels hold shared structure, not presentation).
  - **Organization gains its missing `status` field** (drafted): the active→inactive lifecycle was declared in States & Transitions with no field to hold it.
  - **Visitor extension carries no per-visit state** (drafted): `check_in_status` on a one-per-person Visitor row breaks on the second visit. Visitor is now a durable known-visitor profile (default purpose/host only); each actual **Visit** is anticipated as an Activity extension registered by Access Control (Module 4) with the Person as participant — the "everything trackable is an Activity" discipline applied to the record that was wrongly carrying occurrence state on a profile.
  - **Vehicle: absent ownership means unknown, never tenant-owned** (elicited): the old default silently read a visitor's unrecorded car as tenant property. Fleet vehicles now carry an explicit `ConveyanceOwnerAssociation` to the tenant's own Organization record — the modest setup cost of honest absence semantics. General lesson: never overload null with a substantive default on a liability-relevant field; absence of a record is information ("unrecorded"), not a value.
  - **Emergency contacts stay real Person entities, with two mitigations** (elicited): dedup pairs involving a contact-only Person (no extensions, referenced only via EmergencyContactAssociation) are low-priority suggestions, never mandatory review items (upgrading to normal priority if the stub gains an extension); and contact-only Persons are excluded from general person search, visible only through the referencing association. Keeps the cross-reference value (an employee's spouse may also be a visitor) without the review-queue flood or the searchable-contacts privacy exposure.
  - **Air-gapped AI resolved via the adaptor pattern, elevated platform-wide** (elicited — the user generalized beyond the question): AI providers become **provider adaptors** behind one interface (launch: Anthropic, OpenAI, Gemini, Azure OpenAI; a self-hosted inference adaptor is the designed-but-not-built future answer for air-gapped/DOE, mirroring GIS's self-hostable-only restriction for those tenants), with the launch limitation disclosed (AI unavailable in air-gapped deployments until that adaptor ships). Per explicit user direction, the principle is platform-wide: **anything pointing at an external provider — AI, maps, notification channels, SIEM export, even a logging sink — goes behind an adaptor so swapping/adding a provider is adaptor work, never feature rework.** GIS established the pattern; it is now recorded as the default for every provider-shaped dependency, and added to docs/architecture-guidance.md as a standing code-level rule.
  (See the retrofitted [entity-registry-core.md](0.5-master-records/entity-registry-core.md), [organization-registry.md](0.5-master-records/organization-registry.md), [person-registry.md](0.5-master-records/person-registry.md), [vehicle-conveyance-registry.md](0.5-master-records/vehicle-conveyance-registry.md), [ai-llm-services.md](0-platform-core/ai-llm-services.md), and docs/architecture-guidance.md.)
- **"Everything is an Activity" volume valve resolved: Signal Disposition spectrum + storm collapse, recorded in Activity Registry ahead of Module 19.** Closes the design review's last structural defect — the docs previously implied every physical-security signal (Module 19's intrusion panels, IoT sensors, fire panels) would become Entity+Activity+extension rows, all audit-tier, which an alarm storm would bloat. Per user elicitation, anchored by his framing worth keeping verbatim: *the Activity base type is about "what a dispatcher or supervisor needs to see, and doing so more efficiently" — not a union table of everything.*
  - **Signal Disposition is a tenant-configured spectrum, per integration/signal type**, four levels: `display_only` (live console passthrough, no storage in this platform at all — the source system owns its own log), `telemetry` (raw retention-windowed store outside the Entity hierarchy), `activity` (promotes to a real Activity extension with full standard treatment), `incident_dispatch` (promotes AND fires configured Domain Events automation — e.g., forced door on a SCIF auto-creates an Incident and dispatches armed guards; no new automation mechanism, standard trigger/effect split and automation constraints). Module 19 will own the configuration surface; Activity Registry owns the contract. Extends the precedent already set twice (GIS Position Records, access events as raw telemetry).
  - **Storm collapse: one real event = one record.** Repeated identical signals within a rolling window collapse into one promoted Activity with first/last timestamps and a live `occurrence_count` (400 firings from one faulty sensor = 1 row, count 400), raw sequence still queryable in telemetry. Write-time collapse at the promotion boundary, same family as Checkpoint Scan's debounce — distinct from identity dedup, which promoted signal Activities opt out of anyway via `is_mergeable = false`.
  - **Automation sets the default path, never a ceiling on human judgment**: manual promotion from any feed/telemetry level into an Activity (or straight to an Incident via the launch-point mechanism) is always available.
  - General lesson: a universal base mechanism ("everything trackable is an Activity") needs an explicit admission valve once machine-generated volume enters the picture — the discipline is *case-worthy occurrences are Activities; high-frequency signals are telemetry that can promote*, and it should be written down at the base-mechanism layer before the first high-volume producer module is specified, not discovered during it.
  (See the new "High-frequency signals & the promotion valve" section of [activity-registry.md](0.5-master-records/activity-registry.md).)
- **Strategic boundary recorded: Sentinel Suite is never the PSIM, VMS, alarm-management, or access-control system — it is the reporting engine those systems talk to best.** Per explicit user direction: "truly great ones" exist for physical-security control; the platform's entire value at that boundary is **escalation of alarm handling into documented incidents** — the systems of control feed the system of record. Supersedes the earlier phrasing in this log (under API & Messaging Layer) that Module 19 "owns hardware protocol adapters (BACnet, Modbus, Wiegand, RTSP)": the platform never speaks native device protocols. Consequences, applied now and binding on future elicitation:
  - **Module 19 reframed** (MODULES.md section header and pdd.md table/scope rewritten): an integration boundary with the platforms a site already runs — one adaptor per upstream system (Genetec/Lenel/Milestone-class, alarm receivers, IoT platforms) via each system's own APIs/event streams, per the platform-wide provider-adaptor pattern. Every ingested signal enters through Activity Registry's Signal Disposition valve; the `incident_dispatch` disposition IS the product at this boundary. Feature bullets like "alarm reset controls" or "remote gate controls" are read as invoking the upstream system's API where offered, never as owning the hardware.
  - **Module 3's future "Live Camera Feed Ingestion"** is read as embedding/deep-linking the VMS's own streams and playback, not RTSP/WebRTC ingestion infrastructure.
  - **Module 4 (Access Control)** is read as the records-and-workflow side (visitor management, clearance profiles, key custody, BOLO alerts — none of which the door-control vendors do well) plus consuming ACS events via Module 19 — never door/panel control itself.
  - **pdd.md Out of Scope** gains the boundary explicitly, alongside the existing 911-CAD exclusion — the two are the same instinct: integrate with the adjacent system of control, own the documentation/coordination layer.
  - General lesson: the scope-review finding that Module 19 was "a Genetec-class product, unbounded per-vendor work" is resolved not by cutting the module but by stating which side of the integration boundary the platform lives on — a module that looked like a person-decade of protocol work becomes a bounded set of upstream-system adaptors feeding an escalation pipeline that already exists.
  (See the reframed Module 19 sections in `docs/MODULES.md` and `docs/pdd.md`, pdd.md's expanded Out of Scope, and the touched-up valve section in [activity-registry.md](0.5-master-records/activity-registry.md).)

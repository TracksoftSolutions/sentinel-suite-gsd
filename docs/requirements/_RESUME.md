# Resume Prompt — paste this into a new session

We're mid-way through a structured requirements-elicitation exercise for Sentinel Suite. Before doing anything else:

1. Read `docs/requirements/_INDEX.md` — the ordered, checkbox-tracked list of every feature (from `docs/MODULES.md`, plus several features added during elicitation). `[x]` = doc written, `[ ]` = not started yet. Find the first unchecked item — that's where we continue.
2. Read `docs/requirements/_DECISIONS.md` in full — it's the accumulated log of every cross-cutting architectural decision made so far. This is not optional context; several decisions (see below) constrain how every future feature doc must be written. Do not contradict or re-litigate anything in it without flagging the conflict to the user first.
3. Read `docs/mvp.md` — the living MVP scope ledger (in/out, scope gates, change log). Elicitation and build order follow it; when a feature negotiation moves scope, update its Change Log in the same commit.
4. If the work touches code-level design (or you're advising on implementation), also read `docs/architecture-guidance.md` — the code-architecture guidance doc (inheritance for the identity taxonomy, interfaces for capabilities with the runtime registry authoritative, composition for services). It's guidance, not a feature spec, and defers to `_DECISIONS.md` where they touch.
5. Skim 2-3 of the already-written docs in `docs/requirements/0.5-master-records/` (e.g., `entity-registry-core.md` and `person-registry.md`) to internalize the template and level of depth expected: Overview, Actors & Roles, User Stories, Functional Requirements, Data Model/Fields, States & Transitions, Integrations, Permissions, Non-Functional/Constraints, Acceptance Criteria, Open Questions.

## Where things stand

- **Module 0 (Platform Core): complete** — 14 features, including six added during elicitation beyond the original 8: Domain Events, Command/Action Bus, Command Palette, CLI-Style Input (all four split out of Event & Command Bus Architecture), **Tenant-Defined Types & Custom Fields**, and **AI / LLM Services** (both promoted from patterns first drafted locally inside Module 1 features — see below).
- **Module 0.5 (Master Records): complete** — 12 features on a from-scratch Table-Per-Type entity/association architecture (Entity Registry Core, Party, Person, Organization, Item, Vehicle/Conveyance, Location, Activity, Document, Entity Relationships & History), replacing the original 5-feature MODULES.md list.
- **Module 1 (Security Operations): complete** — all 8 features (DAR, Shift Passdowns & Handover Notes, Guard Tour & Checkpoint Verification, Patrol Management, Courtesy Patrol, Tickets/Citations & Traffic Safety, Incident Reporting & Management, AI-Assisted Incident Report Writing). This module produced most of the session's reusable cross-cutting patterns — see below.
- **40 of 222 features done as of the Module 2 hand-off; +4 since** (Module 0 gap-review additions below) — always recount `_INDEX.md` directly (`grep -cE '^\- \[x\]'` vs `grep -cE '^\- \[[ x]\]'`), don't trust a running tally from conversation narration.
- **Module 2 (Dispatch/CAD): complete** — all 8 features (Call Intake & Logging, Unit Dispatch & Proximity Routing, Active Incident Queue/CAD Console, Status & State Monitors, Silent Mobile Dispatching, Multi-Incident Console, Active Call Alerts & Timers, Historical CAD Log Reconstruction). Highlights: Call supersedes Patrol Management's Patrol Request as the CAD front door; Dispatch's phase-timeline became the platform's canonical response timeline (superseding Module 1's interim Response Timeline Event); Active Incident Queue is a live Card/Feed board over Activity Registry (queue role registered per type, one-level nesting cap, Route Assignment as a rollup card, Dispatcher vs. Supervisor/EOC default presets); Status & State Monitors' Duration Watchdog is one generalized `(activity_type, watched_field, watched_value)` mechanism reused three times (Time-on-Scene, Pending Call Alarm, Enroute Timer) plus a unified Critical Event Escalation Policy; Historical CAD Log Reconstruction is DAR-shaped (not audit-log-sourced) with two deliberately opposite-grouped axes (dispatcher/shift strictly interleaved vs. activity unbounded-depth). Full rationale for every retrofit and refinement is in `_DECISIONS.md` — don't re-derive it from this summary.
- **Application framework decision firmed up: .NET/ASP.NET Core, loosely based on ABP (ASP.NET Boilerplate), clean architecture + DDD at its core.** No longer a pending "if ABP lands" hedge — see the `_DECISIONS.md` entry right after the original .NET backend-framework decision. This triggered a deliberate detour: rather than continuing straight into Module 3, we jumped back to **Module 0 (Platform Core)** and cross-referenced it against ABP's standard module set for gaps the platform had silently assumed rather than specified.
- **Module 0 gap review: 4 new Platform Core features added and elicited, all `[x]` in `_INDEX.md`.** In order: **Tenant Management** (provisioning self-service+admin, isolation tiers `shared`/`dedicated_db`/`on_prem`, Edition + à-la-carte Module Entitlements, lifecycle through offboarding→archive→purge, and — the big one — **Client Engagement**, the contractor↔client dual-tenancy model for contract security: the *client* is always the tenant of record, a Contractor's staff get an org-level contract-scoped grant layered on Auth's existing per-user Tenant Role Grant, client read-access to historical records survives the engagement ending, and an ended engagement leaves the contractor only a Frozen Engagement Copy of records their own staff authored); **Feature Management** (boolean + quota flag kinds, resolves Tenant Management's Edition/entitlements into runtime gating, hard platform rule that no quota cap may ever block an operational action — escalate, don't block); **Blob/File Storage** (adaptor pattern matching GIS/AI-LLM, tenant-scoped content-hash dedup — deliberately never cross-tenant, even for `shared` tier — malware scan-then-quarantine, baseline orphan cleanup independent of any feature's own retention logic; closes the gap Document Registry explicitly deferred); **Background Job Processing** (one registry for both one-off and recurring jobs, platform-enforced idempotency via registered dedup keys — not a per-job convention — retry-with-backoff to dead-letter, isolation-tier-aware execution placement so `on_prem` tenants' jobs never touch shared compute). All four cross-cutting decisions are logged in `_DECISIONS.md`; read them in full before touching any of these four docs or anything that references tenants/entitlements/files/background work.
- **Module 0 gap review, second pass: 2 more Platform Core features added, plus one targeted amendment to an already-`[x]` doc.** Prompted by re-checking Command Palette/CLI-Style Input's silent assumption of "universal search" and Tenant Management's silent gap of "tenant exists but has no legacy data": **Global Search & Data Indexing** (opt-in per record type, precomputed adaptor-pattern index, permission always re-checked live at query time — index is never a second source of truth for access control) and **Bulk Import & Data Migration** (mandatory dry-run validation, dedicated `bulk_import` permission distinct from ordinary create permission, duplicates routed through Entity Registry Core's existing dedup/merge review rather than reinvented). Also amended **Authentication & Authorization** (already `[x]`) with items 19a-19c: self-service password reset as a tenant-configurable channel catalog (email/SMS/supervisor-assisted), gated by the tenant's minimum required AAL exactly like login. All three decisions logged in `_DECISIONS.md`.
- **Module 3 (Command Center / Dashboard / EOC): complete — 9 of 9 features.**
  - **Unified Operational Picture (UOP) Map** `[x]` — a pure composition layer (no new mapping mechanism) over GIS & Mapping Services' Position Record/Geofence/Overlay Layer, Active Incident Queue's Card read-model, and Status & State Monitors' Unit State, delivered live via Real-Time Delivery's Live Update Channel. Ships as both a dedicated full-screen Command Center destination (with a site-summary-tile multi-site rollup mode) and the `map` type in Multi-Incident Console's Panel Registry (that doc's Map panel is retrofitted to embed this fuller composition rather than a bare GIS map view). New dedicated **UOP Map Viewer** permission, layered on top of per-pin ABAC.
  - **Command Center Wallboard View** `[x]` — confirms Multi-Incident Console's flagged second-consumer prediction, but only half-promotes the mechanism: the panel_type catalog/renderers (Map/Queue/Kanban/Unit Roster/Detail, plus a new `health` type) are now a shared cross-doc catalog, while *arrangement* stays split — Console Layout (personal/draggable) vs. the new **Display Profile** (admin-authored, static-or-rotating per-screen assignment). Introduces **Display Device** (non-interactive, admin-provisioned, one-time-token identity with a viewing scope *and* a separate max-sensitivity ceiling — resolves Real-Time Delivery's own flagged "unauthenticated display posture" gap), an Alarm Alert Banner that can never be silenced but *can* be set per-device to a non-blocking `overlay_banner` instead of `full_screen_takeover` (corrected mid-review — a Dispatcher working off a wallboard screen shouldn't lose their view to an unrelated unit's missed check-in), and **Health Signal Registration** (opt-in status/metric registry, graceful on unregistered integrations, `unknown` distinct from `degraded`/`down` on a failed callback).
  - **ICS Role Mapping & Visual Org Chart** `[x]` — completes EOC Activation's lifecycle (retrofit: promoted from Multi-Incident Console's lightweight record to a full Activity extension, `active` → `deactivated`). **Two-tiered ICS adoption, refined three times mid-review:** Full ICS (entire Section/Branch hierarchy) stands up only via EOC Activation, available/never forced; Limited ICS (small, tenant-customizable subset — typically IC/Communications/Support) assigns directly to **any Incident**, no EOC Activation prerequisite — for tenants (DOE national labs) with mandatory NIMS/ICS adoption. ICS Role Assignment is a heavy EntityAssociation Extension whose anchor (`entity_id_b`) is either an Incident (Limited) or an EOC Activation (Full), `position_ref`/`start`/`end`, same current-holder-plus-history shape as `CustodyAssociation` — **this structure is unchanged by any of the three refinements below; only the entry point narrowed.** A tenant-level **ICS Adoption Policy** (`optional`/`mandatory_limited`) governs enforcement, which now lives entirely at the point an officer is associated with an Incident, not a closure-time check: the creator automatically gets an ICS Role Assignment for Incident Commander (superseding Incident Reporting & Management's plain `reporting_officer` default for mandatory tenants — retrofit, FR #4), and every additional officer/unit must have an explicit Limited Position picked as part of that same association action — no silent `responding_unit` fallback. Because IC is filled by construction the moment any officer is attached, there's nothing left to check later (the earlier `Position.is_required` flag and Supervisor Review sign-off gate were both retired as unnecessary once enforcement moved to the source). A minor incident (a trespasser who left on challenge) gets its IC for free and needs nothing else. Incident Command Post = ordinary Location + a time-bound `Command Post Designation` association (Full-ICS-only), no new Location extension. Role holders are always real Person entities, inline-created if needed. Contributes `org_chart` as a third Panel Registry catalog entry (after Wallboard's `health`).
  - **Situation Reports (SITREPs)** `[x]` — mirrors ICS Role Mapping's two-tier split: **SITREP** (EOC Activation-anchored, per-ICS-Section structure, recurring) and **Incident Status Report** (Limited-ICS-anchored, no Section fields, ad hoc only) — separate report types, not one form with hidden fields. Both follow a **Draft → Publish** two-step (mirrors AI Draft Narrative's never-auto-published discipline) snapshotted into an immutable Document via Document Registry. Stitches together four previously-independent mechanisms: recurring generation is the third Background Job Processing consumer (tenant-default cadence + per-activation override, halted on EOC Activation deactivation — retrofit); Resources Committed auto-populates from live Unit State, snapshotted at publish; overdue SITREP is a new Duration Watchdog instance feeding Critical Event Escalation Policy, never blocking any operational action; AI-drafted narrative is the third AI/LLM Services "propose, human confirms" consumer (`sitrep_narrative` context) after Incident Narrative and CLI-assist. Authorship (`single_compiler`/`per_section_contribution`) is tenant-configurable with per-activation override.
  - **Live Camera Feed Ingestion** `[x]` — never speaks VMS protocols directly (no RTSP/WebRTC infrastructure); pure Command Center consumption/embed/deep-link layer. Camera reuses Guard Tour's Checkpoint **triad pattern**: **Camera Position** (Location extension, fixed mount point) + **Camera** (Item extension, full identity/dedup/custody/audit like Vehicle — per explicit user direction, not a lightweight stub) linked by a single-current-value **Camera Mount Association** — hardware replacement never severs a position's history. Feed display (`embed`/`deep_link`) and PTZ control are both adaptor-declared capabilities (Module 19's future VMS Camera Stream Ingestion owns the real device registry — this doc's VMS-reference fields are an explicit interim stand-in, DAR Shift Window-style). Auto-popup camera selection reuses the platform's explicit-beats-default resolution chain (Command/Action Bus parameter precedent) over three existing trigger sources (Signal Disposition, Geofence, Duration Watchdog) — no new matching logic. Contributes `camera` as Panel Registry's fourth cross-doc panel type; retrofits UOP Map with a Camera Positions pin layer.
  - **EOC Logistics Hub** `[x]` — reuses four established mechanisms instead of inventing resource-management infrastructure. Resource modeling is a hybrid (per explicit user direction): a Resource Type Definition's `is_individually_tracked` flag routes to either a real Item Registry instance (full custody/dedup/audit, like Vehicle/Camera) or a simple local quantity counter for bulk/consumable types. **Resource Request** is an Activity extension anchored to the EOC Activation, directly reusing Patrol Management's ad hoc Patrol Request request→assign→fulfill shape; assigning triggers an ordinary Item custody transfer; registers as a `card` Queue Role (retrofit to Active Incident Queue) for zero-new-mechanism Kanban board visibility in Multi-Incident Console. **Mutual Aid Organization** entries always resolve to a real Organization Party (inline-created if new, Agency Handoff Log's discipline) and — per explicit user direction — carry basic agreement fields (dates, scope) rather than a bare directory, still flagged for reconciliation with Module 5's future Mutual Aid Agreements Tracker. EOC-Activation-only scope (no Limited-ICS reach) since ordinary incidents already have Patrol Request for routine equipment needs.
  - **Alarm Panel Monitors & Panic Alerts** `[x]` — confirms Camera's triad pattern for a second physical-security concept (Alarm Zone = Location extension, Alarm Panel = Item extension, full custody/dedup like Vehicle/Camera), but adapts multiplicity honestly: one Panel commonly monitors many Zones, so the Mount Association constrains only the Zone side, not Camera's strict 1:1. Every Zone signal flows through Activity Registry's existing Signal Disposition valve unmodified; the real vendor adaptor is deferred to Module 19's future Intrusion Alarm IP Listeners/Fire Panel Watchdogs. **Panic Alerts split into two sources**: a panel-integrated panic button is just an ordinary Alarm Zone through Signal Disposition; a **mobile app SOS button** is fully platform-native with its own thin **SOS Alert** Activity extension — the platform's first deliberate, explicitly-flagged exception to the confirmation-gate default (a panic trigger must fire with zero hesitation), pairing with an ungated **Stand Down** action for false alarms. Triggering composes three existing mechanisms (GPS capture, Real-Time Delivery's persistent Alarm State, the launch-point Incident-creation pattern) with zero new escalation infrastructure. Contributes `alarm_monitor` as Panel Registry's fifth cross-doc panel type; retrofits UOP Map with an Alarm Zones pin layer.
  - **Environmental & Weather Map Overlays** `[x]` — retrofits GIS & Mapping Services' Overlay Layer with a `live_feed` source type (adaptor-driven, always-current, no versioning) rather than a second parallel overlay mechanism — weather radar, lightning, and traffic congestion all render through the same toggle-able Overlay Layer UI a static KML upload uses. **Environmental Sensor** confirms the Camera/Alarm-Panel triad a third time (Location extension + Item extension, full custody/dedup/audit, Camera's 1:1 mount shape), feeding Activity Registry's existing Signal Disposition telemetry tier unmodified. Per explicit user direction, the motivating root-cause-correlation use case (heat → HVAC fan → false motion trip) is noted but *not built* here — this doc only ensures the telemetry fidelity that makes it possible; the actual correlation view belongs to Module 19's future Alarm Pattern & Nuisance Analysis. Severe Weather Alert routes through Notifications Engine with an optional one-tap Activate EOC Response suggestion at a tenant-configured threshold, reusing the existing confirmation gate unmodified.
  - **Historical Playback Console** `[x]` — closes Module 3 as a pure time-scrub replay composition, no new position/event/video storage: three synchronized tracks (spatial, event, video) driven by one master scrub cursor. Spatial track reuses UOP Map's exact composition/layer set/clustering unmodified, reading GIS's Position Record history instead of the live feed; event track is Entity Relationships & History's Interaction Timeline windowed to the session; video track deep-links into a Camera's VMS adaptor's own recorded-footage viewer via a new adaptor-declared `historical_playback_supported` capability (retrofit to Live Camera Feed Ingestion), never storing/streaming footage. Unit State replay derives from Dispatch's `phase_timestamps` (reapplying Historical CAD Log Reconstruction's never-source-from-audit-trail discipline), with a manual-transition gap (e.g. a bare `out_of_service` toggle) honestly accepted rather than patched. Deliberately **view-only, no report/export** — a genuine divergence from the DAR/Passdown/Incident/Historical-CAD-Log "snapshot to Document" pattern, per explicit user direction. Retention is per-track and honestly gapped (each track renders to its own source's retention boundary with a visible indicator) rather than blocking the whole session. New dedicated **Historical Playback Viewer** permission, kept separate from UOP Map Viewer given the materially greater movement-surveillance exposure of a full historical trail versus a live position glance; opening a session is itself an audit-tier event for the same reason.
  - Full rationale for all nine in `_DECISIONS.md`.
- **Module 3 (Command Center / Dashboard / EOC) is now complete — 9/9.** Module order is itself the priority order (per explicit user direction) — proceed straight down `_INDEX.md`, no reordering or pulling features forward.
- **Pre-Registration Portal** `[x]` — first Module 4 doc. Pre-Registration is an Activity extension (real lifecycle, earns Activity treatment like Route Assignment did) authorizing one or more separate Visit occurrences (Visit itself is Visitor Kiosk App's, not built here) within a date-range window. Visitor always resolves to a real, dedup-checked Person+Visitor entity. A watchlist match never directly creates a BOLO Flag — it raises a lightweight Watchlist Match Alert a Supervisor+ must dismiss or escalate through Entity Registry Core's existing step-up-gated flow (automation proposes, human confirms, applied to a watchlist hit). Introduces the base **PIAM Adaptor Registration** (HID SAFE first) that later Module 4 docs extend; watchlist authority is configurable `sentinel_native`/`external`, but the Pre-Registration record's own core fields stay Sentinel-native (one-way push out) — the full bidirectional-conflict rule is deferred to Access Credential Management.
- **Visitor Kiosk App** `[x]` — builds Visit (the execution half deferred by Pre-Registration Portal). Introduces **Kiosk Device**, the platform's first write-capable unauthenticated physical device — its own registration, not an extension of Wallboard's read-only Display Device, since it needs a bounded write scope + declared peripherals (camera, signature pad, ID scanner, barcode scanner, badge printer) rather than a viewing scope. Walk-in self-registration reuses Pre-Registration Portal's watchlist/approval mechanism wholesale via a new `kiosk_walk_in` channel — never a parallel path. Printed badge is visual-only; real PACS credential encoding stays behind the PIAM/PACS adaptor boundary. Overdue Visit handling is independently-configurable alert + auto-checkout thresholds (Duration Watchdog reuse), auto-checkout leaving an honestly-distinguishable record. Retrofits Pre-Registration Portal with QR pass issuance (owned at approval time, consumed here) and Active Incident Queue with Visit as a `card` Queue Role.
- **Host Arrival Notifications** `[x]` — thin composition doc, no new delivery/inbound-message infrastructure. Host Arrival Acknowledgment reuses the zero-friction-tap receipt pattern (Safety Check-in, Dispatch Acknowledgment) rather than becoming a fourth Activity extension. Confirmation gating is tenant-configurable (`acknowledgment_only` FYI vs. `gated` lobby-hold, with an always-available Guard **Release Visitor** override honestly logged when it bypasses confirmation). "Reply via SMS" resolves to one tappable link identical across every channel — no inbound-SMS parsing. Multi-tier escalation reuses Visitor Kiosk App's independent-dual-threshold shape a second time; backup host resolution follows the standard explicit-beats-default chain, retrofitting Pre-Registration Portal and Person Registry's Employee level rather than inventing a distribution-list mechanism ahead of Module 17.
- **Access Credential Management** `[x]` — **sharpens the Module 4 framing**: flagship capability is multi-vendor PACS/credential ingestion (CCure, Safelok, HID SAFE, and others — an open vendor set, not just HID SAFE), native issuance is a lighter fallback, per explicit user correction ("not trying to be in the credential management business, the single package for all security operations, getting the data in") — the same integrate-don't-replace boundary Module 19 established for VMS/alarms, now extended to PACS/credential systems. Access Credential is an Item extension; CredentialAssignment is a new purpose-built EntityAssociation (not reused custody), deliberately access-content-agnostic (zones/doors deferred to Clearance Profiles, next up). Provisioning is gated (Credential Approval Policy); deprovisioning is immediate/ungated on termination (Domain Event, the platform's second confirmation-gate exception after SOS Alert). Both provisioning and revocation carry a pending-confirmation state — never silently claims a credential is active/revoked before a write-capable adaptor confirms. Recertification lapse handling is tenant-configurable (escalate-only vs. auto-suspend) — the fourth Module 4 doc running with "configurable policy, not fixed rule" as its recurring shape. Also closed a self-caught gap: MODULES.md's "Badge Print Layout Tool" got a **Badge Template** mechanism (configurable field layout, scoped by `applies_to`), with Visitor Kiosk App's visitor badge retrofitted to render through it instead of a fixed layout.
- **Clearance Profiles** `[x]` — corrects Access Credential Management's own field shape: what a person can access is decoupled from which credential they carry. `CredentialAssignment.clearance_profile_ref` (singular) is retired for a real, independent, many-to-many **Clearance Profile Assignment** (Person ↔ Clearance Profile) — one badge commonly carries several stacked access groups. Same multi-vendor ingestion-first pattern as Access Credential Management (`clearance_sync` joins `visitor_sync`/`credential_sync`). Zone boundaries reference Location Registry directly — no new place concept, no blocking wait on Module 9 (`include_descendants` defaults false, never silently cascades). Time restriction is a reusable named Schedule Definition. Temporary-clearance expiration reuses Background Job Processing's one-off job kind, executing with the same ungated urgency as termination-triggered revocation.
- **Key Ring Registry** `[x]` — opens the physical-mechanical-key cluster (Key Ring Registry → Key Custody & Auditing → Lock Core & Cylinder Tracking), no adaptor/vendor involved, confirming Item Registry's own explicit forward reference to Key Custody. Key and Key Ring are both real Item extensions (fifth confirmed custody-mechanism consumer). Master-keying hierarchy reuses `HierarchyAssociation` a third time, with a Grand Master's operable-lock set *derived* by rolling up subordinates' mappings, never duplicated. Owns the key side of key↔lock, forward-referencing Lock Core (next-next doc, the seam pattern from Access Credential Management → Clearance Profiles applied again). Replacement Schedule gets real recurring-alert automation (Background Job Processing) plus an auto-created lightweight Locksmith Work Order ledger — a deliberate departure from the doc's own recommended lighter default, per explicit user direction.
- **Key Custody & Auditing** `[x]` — generalizes Duration Watchdog twice rather than building parallel mechanisms: (1) watched-record kind broadens beyond Activity-only to also watch a registered EntityAssociation (`custody_association` — its first non-Activity consumer); (2) threshold gains a dynamic resolution mode (holder's DAR Shift Window end, with flat-duration fallback), matching MODULES.md's "before shift end" framing. Digital custody signatures are tenant-configurable by key privilege level (Key Custody Signature Policy), reusing Document Registry exactly as Pre-Registration Portal's NDA signing did. Lockbox/cabinet integration (Traka) gets its own dedicated Key Cabinet Adaptor family, separate from PIAM Adaptor Registration — cabinet events flow through Activity Registry's existing Signal Disposition valve like any other hardware-signal producer, auto-opening/closing custody on promotion. Retrofits Active Call Alerts & Timers' Duration Watchdog data model. 
- **Lock Core & Cylinder Tracking** `[x]` — confirms the triad pattern a fifth time (Lock Position + Lock Core + Lock Mount Association) and closes Key Ring Registry's forward reference. A full core swap is free history from the Mount Association; a re-pin (same core, new internal keying) gets its own explicit Re-pin Record since no hardware swap occurs. Locksmith Work Order (Key Ring Registry) is generalized to target Lock Position/Core too, but per explicit user pushback ("lock and key does need more specific security tracking than most work orders") a Lock/Core-targeting order requires witness + security verification before completing — a Key/Key Ring-targeting order is unaffected. Retrofits UOP Map with a Lock Positions pin layer. **Module 4's physical-key cluster is complete** — the platform's proven mechanisms (Item extension, triad pattern, HierarchyAssociation, CustodyAssociation, Duration Watchdog, Signal Disposition) scaled into a fully hardware-physical domain with zero new base mechanisms. 
- **BOLO & Trespass Alerts** `[x]` — turned out to be almost entirely a retrofit/closure doc: the BOLO Flag mechanism was already fully built (Entity Registry Core + Person Registry, two modules ago). Real content: **arrival-time re-screening** (retrofit Visitor Kiosk App — every check-in re-runs BOLO screening, not just trusting Pre-Registration's submission-time result, closing a real staleness gap for multi-day windows); **silent alarm as genuine safety design** (neutral kiosk message, persistent Alarm State to Dispatch/Lobby Guards server-side, auto-launched Incident on match — never tips off a potentially dangerous individual); **Check Watchlist** as a first-class Command/Action Bus action for the non-kiosk case; **Trespass Notice Default Expiration Policy** closing Person Registry's own long-flagged open question with a jurisdiction-aware default. 
- **Remote Gate & Barrier Controls** `[x]` — closes Module 4 (10/10). Sixth and seventh confirmed triad instances (Gate/Barrier, Intercom) — Intercom deliberately kept independent of Camera per explicit user direction (a call/buzz is functionally distinct from a video feed, even though the doc's own recommendation was to reuse Camera), with an incoming call instead registering as a fourth trigger source on Camera Auto-Popup Mapping. Barrier Confirmation Policy is tenant-configurable by `barrier_type` — a genuine departure from Camera PTZ's unconditional confirmation-gate stance, the first time this module's recurring configurable-policy shape gated a physical action's required friction rather than a process. `gate_control` extends PIAM Adaptor Registration rather than spawning a fourth adaptor family, unlike Key Cabinet. Retrofits Live Camera Feed Ingestion and UOP Map.

## Module 4 (Access Control) — complete, 10/10

Opened with a strategic framing decision (native "PIAM Lite" + first-class multi-vendor PACS integration — CCure/Safelok/HID SAFE/others, an open vendor set), then delivered the visitor lifecycle trio (Pre-Registration Portal → Visitor Kiosk App → Host Arrival Notifications), the credential/clearance pair (Access Credential Management → Clearance Profiles, corrected mid-module to decouple access from credential identity), the physical-mechanical-key cluster (Key Ring Registry → Key Custody & Auditing → Lock Core & Cylinder Tracking, the platform's proven mechanisms scaling into hardware with zero new base mechanisms), and closed with BOLO & Trespass Alerts (mostly retrofit/closure) and Remote Gate & Barrier Controls. The **triad pattern** (Location extension + Item extension + Mount Association) now has **seven confirmed instances platform-wide** (Checkpoint, Camera, Alarm Panel, Environmental Sensor, Lock Core, Gate/Barrier, Intercom) — a firmly established standing pattern, not a one-off. Full rationale for all ten features in `_DECISIONS.md`.

- **ICS Forms Engine (FEMA-aligned)** `[x]` — opens Module 5 (Emergency Management), generalizing two prior narrow mechanisms (Situation Reports' SITREP machinery, ICS Role Mapping's ICS-207 render) into one platform-wide **Form Definition/Form Instance** engine covering the FEMA/NIMS ICS 200-series catalog (201-215A, 15 day-one forms), per explicit user direction to absorb rather than leave parallel mechanisms. Introduces **Operational Period** as a new first-class Activity extension (Full ICS cycles explicit sequential periods; Limited ICS gets one implicit continuous period, the module's now-three-times-applied Full/Limited depth split). SITREP/Incident Status Report become the ICS-209 (full)/ICS-209-lite Form Definitions (kept as two distinct definitions, not one with conditional fields); ICS-207 becomes a zero-new-fields render of existing ICS Role Assignment data. Fixed FEMA catalog plus tenant-defined custom forms (Tenant-Defined Subtypes anchored on `Form Instance`, no new custom-form mechanism); per-form tier applicability (`full`/`limited`/`both`); auto-population via a named-resolver registry (same discipline as Health Signal Registration). Retrofits `situation-reports.md` (marked superseded, its field content stands as the ICS-209 schema) and `ics-role-mapping-visual-org-chart.md` (FR #15a). Full rationale in `_DECISIONS.md`.

- **Resource Logistics Catalog** `[x]` — builds the real tenant-wide resource catalog EOC Logistics Hub explicitly deferred. Tenant-wide standing inventory (not EOC-activation-scoped) with a full FEMA/NIMS Resource Typing taxonomy (Category → Kind → Type/Tier). Individually-tracked resources register as a new **Catalog Item** Item extension, exercising the platform's existing multi-extension allowance for the first time (an ambulance can be both `Vehicle` and `Catalog Item` on one entity). **Deployment Status auto-derives from Resource Request's phase** (Unit-State/Dispatch-phase precedent) — `decommissioned` is the one independent manual transition. **Reorder Warnings** introduce a new, deliberately narrow **Stock Threshold Alert** mechanism (the platform's first value-threshold alert, distinct from every duration-threshold Duration Watchdog instance), debounced against alert storms. Retrofits `eoc-logistics-hub.md` (marked superseded — `Resource Type Definition` replaced, Resource Request's own lifecycle otherwise unchanged). Full rationale in `_DECISIONS.md`.

- **After-Action Reports (AAR)** `[x]` — closes out an EOC Activation/Incident with Timeline Reconstruction (reuses Historical CAD Log Reconstruction's activity-axis mechanism directly), a full-lifecycle Improvement Action Plan, and a derived Executive AAR Briefing. **Note: the `AskUserQuestion` tool dropped mid-round on this feature** — two retries failed, and the user's actual answers only arrived via a screenshot; they diverged from the recommended option on two of four questions, both deliberate: **AAR is its own independent Activity extension** (Draft→Publish/Document Registry, NOT routed through ICS Forms Engine — that engine stays scoped to the periodic FEMA 200-series catalog), and **Improvement Action Plan gets the full tracked-task lifecycle here** (owner, target date, status, evidence — a real Activity extension that keeps evolving after the parent AAR publishes), a deliberate departure from this module's usual seam-to-next-doc pattern. Improvement Plan (IP) Tracking (next doc) is explicitly flagged as the likely promoter/aggregator of this mechanism across multiple AARs and future drill-sourced items — not resolved here. AAR anchors to EOC Activation + Incident, with a reserved `exercise` anchor type forward-referencing Exercise & Drill Planner. Full rationale in `_DECISIONS.md`.

- **Improvement Plan (IP) Tracking** `[x]` — resolves AAR's own flagged open question: Improvement Action's parent link widens from `aar_report_ref` to `source_type`/`source_ref` (`aar_report`, new `ad_hoc`, reserved `drill`), so a corrective task can be logged directly without a fabricated AAR. **Action Item Registry** is a pure CQRS read-model (no new storage) over every Improvement Action platform-wide, RBAC/ABAC re-checked live per record. **Task Deadlines are a genuinely new mechanism** — a lead-time Deadline Reminder Policy (fires *before* a deadline), deliberately kept separate from AAR's existing after-the-fact Duration Watchdog (fires *after*) rather than forcing both into one abstraction. Evidence stays optional, confirmed not gated. Retrofits `after-action-reports.md` FR #6a. Full rationale in `_DECISIONS.md`.

- **Exercise & Drill Planner (HSEEP-aligned)** `[x]` — resolves both AAR's `exercise` anchor and IP Tracking's `drill` source via the platform's second full plan/execution pair (Exercise Design → Exercise), scoped to HSEEP's three designed exercise types only (routine drill logging stays the next doc's separate mechanism). **Exercise Fidelity Mode is a real first-class axis, per explicit user direction** — `narrative_only` (self-contained log) and `live_simulation` (participants create real Incident/Dispatch/etc. records for full training realism) are both fully supported, not one a fallback for the other. Introduces a **new base-level Activity Registry field** (`training_exercise_ref`/derived `is_training`) — the platform's first "real vs. training" axis, with a hard, non-configurable rule that every live console must default-filter it out and show a mandatory, non-suppressible TRAINING banner when explicitly viewed. HSEEP form templates stay this doc's own structured plan fields, confirming AAR's earlier choice not to route through ICS Forms Engine a second time. A below-target Evaluation Score auto-suggests a draft Improvement Action requiring explicit human confirmation. Retrofits `activity-registry.md` #19, `after-action-reports.md` FR #1, `improvement-plan-ip-tracking.md` FR #1. **Not yet retrofitted** (flagged, not done in this pass): Active Incident Queue, UOP Map, Multi-Incident Console, and Command Center Wallboard View each need a light touch-up to actually implement the default `is_training` filter this doc's contract requires of them — read `exercise-drill-planner.md` FR #12 before touching any of those docs next. Full rationale in `_DECISIONS.md`.

- **Drill Compliance Logging** `[x]` — the lighter mechanism Exercise & Drill Planner deliberately deferred to it: regulatory fire/evacuation/shelter drills, not designed HSEEP exercises. **Caught and fixed a real naming collision while drafting**: Exercise's `activity_type` was `drill` (borrowed from Activity Registry's illustrative example list); renamed to `exercise` (retrofit, also now consistent with AAR's `anchor_type = exercise`), freeing `compliance_drill` for this doc's own record. Overdue-requirement tracking reuses Key Custody's dynamic-threshold Duration Watchdog mode a third time. Occupant Exit Reports are an honest aggregate-only interim stand-in, flagged for Module 6's future Muster Check-in App/Evacuation Roster Reconciliation. A failed Drill Component Check auto-suggests a draft Improvement Action (`source_type = compliance_drill`, IP Tracking's fourth source value), gated on explicit confirmation. Retrofits `activity-registry.md`, `exercise-drill-planner.md`, `improvement-plan-ip-tracking.md`. Full rationale in `_DECISIONS.md`.

- **Mutual Aid Agreements Tracker** `[x]` — builds the real contract/approval/renewal lifecycle EOC Logistics Hub explicitly deferred. **Note: elicitation was skipped on the first draft** — the design got heavily pattern-matched from EOC Logistics Hub's forward reference and Resource Logistics Catalog's immediately-prior precedent, without an `AskUserQuestion` round, exactly the failure mode `_RESUME.md`'s own process section already warned against. The user caught it directly. A follow-up round confirmed three of four drafted decisions but changed the fourth. **Mutual Aid Agreement is a rich EntityAssociation Extension** (tenant's self-designated Organization ↔ partner Organization Party, same shape as `CredentialAssignment`/`ICS Role Assignment`), not a bare directory row — renewal extends the same row, a substantive renegotiation terminates + creates a new row with `superseded_agreement_ref` (confirmed). Resource Directory capabilities reference Resource Logistics Catalog's real FEMA/NIMS taxonomy instead of free-text tags (confirmed). Review Reminders promoted IP Tracking's just-built Deadline Reminder Policy into a shared Approaching-Deadline Reminder mechanism in Active Call Alerts & Timers, sibling to (not merged with) Duration Watchdog (confirmed). **Changed**: going active is gated by a **tenant-configurable Mutual Aid Agreement Approval Policy** (`none`/`required`, not the fixed no-gate the first draft assumed) — `required` adds a formal Agreement Approval governance record, DAR Shift Review-shaped. Retrofits `eoc-logistics-hub.md`, `active-call-alerts-timers.md`, `improvement-plan-ip-tracking.md`. Full rationale in `_DECISIONS.md`.

- **EOC Activation Checklists** `[x]` — closes Module 5 (8/8) with a generic **Checklist Template/Run mechanism**, structurally the same "ordered steps, independently completed" shape as Guard Tour's Tour Definition → Checkpoint Scan, serving both Activation Checklists and System Checklists as two categories of one mechanism. Checklist tracking is **informational only, never gating Activate EOC Response** — skipping an item never requires justification. A Checklist Run **auto-creates on EOC Activation** via an ordinary Domain Events rule. Staff Call-up reaches a new interim **EOC Call-up Roster** (Persons and/or ICS Positions, not just current holders — flagged for Module 8 reconciliation) and reuses the zero-friction-tap acknowledgment pattern, staying firmly inside Notifications Engine's internal-only scope. Full rationale in `_DECISIONS.md`.

**Module 5 (Emergency Management) is complete — 8/8 features.**

- **Pre-Incident Plans (Preplans)** `[x]` — opens Module 6 (Emergency Planning). Corrected mid-elicitation: a Preplan is a **Document extension with a `PreplanLocationAssociation`**, not a Location extension (a Preplan is authored guidance *about* a place, not itself a place, unlike Checkpoint/Camera Position/Alarm Zone) — this also cleanly explains why a Location can carry more than one active Preplan (a Building's general profile plus Lab 311's own chemical-specific plan). **Preplans owns dispatch-context surfacing generally**, resolving `mvp.md`'s Module 7 slice now: a matching Call/Incident (by type + Location, reusing existing Call Type/Incident Category Definitions) auto-surfaces the relevant Preplan(s) — every match presented, never silently narrowed to one — and pushes a summary to responders' devices. **"Running" a matched plan is explicitly forward-referenced to Incident Action Checklists** (next doc) via a bare `suggested_checklist_template_ref` pointer; no runnable-checklist mechanics are built here. A new **Preplan Utility Point** partially closes Location Registry's own flagged "GIS-marked utility control points" deferral, landing as a sixth triad-pin-layer-convention UOP Map layer; the deeper utility-ops directory stays deferred to Module 9. Sensitivity is a three-tier ladder cloning Person Registry's emergency-aware-visibility pattern (no access → dispatch-broadened summary, reverts on close → full detail behind a dedicated Preplan Viewer permission). Full rationale in `_DECISIONS.md`.

Next up: **Incident Action Checklists** (Module 6, 2nd of 8) — straight down `_INDEX.md`. This doc owes Preplans a real answer for `suggested_checklist_template_ref` and the actual runnable checklist/field-task-assignment mechanism Preplans deliberately left to it. After that, Module 6 continues with Continuity of Operations Plans (COOP), then **Muster Check-in App** and **Evacuation Roster Reconciliation** — the two features Drill Compliance Logging's Occupant Exit Report explicitly flagged as needing reconciliation once built (read that doc's Open Questions when those two come up), and which Preplans' own Open Questions flagged as the likely place to reconcile its narrower "active emergency" trigger with Person Registry's still-open platform-wide concept.

## A note on tooling, continued

`AskUserQuestion` dropped three more times during Preplans' elicitation (two in the first round, one in a follow-up round) — consistent with the known intermittent failure, not a new issue. Each time, falling back to a plain-text numbered-options message in chat worked and got a real answer. Don't burn more than one immediate retry before falling back to plain text — retrying repeatedly wastes turns without fixing the underlying stream issue.
- **Module 4 framing, set before any feature elicitation: Module 4 is "PIAM Lite" — a native standalone Physical Identity & Access Management layer for every tenant, with a first-class two-way HID SAFE integration.** Per explicit user direction, modeled on HID SAFE's real architecture (a PIAM governance layer sitting *above* a tenant's actual PACS hardware, never replacing it, connected via HID Integration Service): every tenant gets a real native PIAM Lite regardless of whether they run HID SAFE (Visitor Manager ≈ Pre-Registration Portal/Visitor Kiosk App/Host Arrival Notifications; core PIAM ≈ Access Credential Management/Clearance Profiles; Watch List Manager ≈ BOLO & Trespass Alerts, reusing Entity Registry Core's existing BOLO Flag mechanism). The HID SAFE integration is a new, dedicated, full-production-quality **PIAM/Access-Governance Adaptor** owned by Module 4 (not an extension of Module 19's read-only ingestion), fully bidirectional (reads HID SAFE's identity/credential/visitor/watchlist state in; writes Sentinel-Suite-originated actions out so HID SAFE can drive real badge/PACS provisioning) — the platform's first write-capable, bidirectional provider adaptor, which will need its own conflict/authority-of-record rule when Access Credential Management is elicited. Full rationale in `_DECISIONS.md`.

## Process to follow (unchanged from the rest of the session)

- One feature at a time. Use `AskUserQuestion` in rounds — keep asking until nothing about the feature is ambiguous. Adaptive depth: simple registry-style features get 1-2 rounds, complex workflow/lifecycle features get more. **This applies even when a feature is heavily pre-shaped by earlier docs' forward references** (e.g., Status & State Monitors had a lot of context already established) — a real elicitation round is still required, not optional just because you can draft something plausible from existing context alone. **Caught twice now** — Status & State Monitors mid-session, then Mutual Aid Agreements Tracker (skipped entirely, drafted straight from EOC Logistics Hub's forward reference + Resource Logistics Catalog's precedent, user had to call it out directly: "Why did you skip elicitation and just assume you knew how I wanted this to work"). The pattern both times was the same: a *recent, similar-looking* doc made the next one feel obvious enough to skip asking. That feeling is exactly the trigger for this rule, not an exception to it — run the round every time, no matter how settled the shape looks from precedent alone.
- Write the doc to `docs/requirements/<module-slug>/<feature-slug>.md` using the template above.
- Mark the feature `[x]` in `_INDEX.md` immediately after writing its doc.
- If a feature reveals a cross-cutting decision (a new shared mechanism, a naming collision, a pattern that should generalize), log it in `_DECISIONS.md` — that file is what keeps 200+ features consistent with each other across sessions.
- When the user pushes back on an architectural choice, engage with the actual technical tradeoff — don't just defer or just agree. This session's biggest wins (NIEM grounding, TPT inheritance, the Entity/EntityAssociation split) came from genuine back-and-forth, not from taking the first design and running with it.

## Load-bearing decisions from `_DECISIONS.md` you must not contradict

- **.NET/ASP.NET Core backend** (FedRAMP/FIPS alignment) — GraphQL for internal frontend, REST for external integrations.
- **RBAC baseline + ABAC overlay** authorization model; every sensitive action gated by a **confirmation gate** (Command/Action Bus) and **step-up auth** (Authentication & Authorization) where flagged.
- **NIEM Core grounding discipline**: before inventing fields for any new entity-like concept, check the real NIEM Core type (`nc:PersonType`, `nc:ItemType`, `nc:ActivityType`, `nc:DocumentType`, `nc:EntityType`/Party, etc.) rather than guessing. This has repeatedly produced better field models than starting from scratch.
- **Entity/EntityAssociation TPT architecture** (Module 0.5): any future module that needs its own "thing with an ID that gets tracked/deduped/cross-referenced" should very likely register as an extension of one of the five base types (Party, Item, Location, Activity, Document) rather than inventing a parallel identity system. Relationships between things should very likely be a named `EntityAssociation` subtype, not a plain foreign-key field, when the relationship benefits from independent audit/history (ownership, custody, employment, etc. all do; a purely descriptive intrinsic field usually doesn't).
- **Every Entity has a display label** (`display_label_strategy`, template or computed) — a universal Entity Registry Core requirement, not per-feature.
- **Settings & Preferences** is a shared hierarchical config engine (location chain + identity chain, lockable per level) — any future tenant/site/user-configurable setting should register against it, not implement its own override logic.
- **Command system** (Command/Action Bus, Domain Events, Command Palette, CLI-Style Input) is an intentional platform differentiator — any new feature that exposes user-facing actions should register them as Command/Action Bus actions, not build bespoke buttons with no palette/CLI/automation reach.
- **"Report" = a filtered read over Activity Registry, snapshotted into an immutable Document.** Established by DAR, reused as-is by Passdown and Incident Reporting: the underlying data (DAR Entries, Incident Updates, etc.) stays live/mutable via ordinary Activities; "generating a report" is a point-in-time Document snapshot that never retroactively updates. Report *generation mode* (auto-on-approval / ad hoc-only / both) and, per Incident, report *scope mode* (one consolidated document vs. one per contributor) are both independent, tenant-configurable axes — don't assume one document per record is the only shape a future report-like feature needs.
- **A review/sign-off gate is its own governance record that never mutates what it judges** — DAR's Shift Review, Incident's Supervisor Review. Any future "did a human approve this set of records" need should follow this shape (reviewer, status, comments, scoped item list) rather than adding an approval field onto the records themselves, since those records are usually shared across other modules too.
- **Plan vs. execution: a reusable-definition hierarchy pairs one-to-one with an Activity-extension hierarchy that has a real lifecycle.** Guard Tour's Route → Tour Definition → Checkpoint (plan, versioned, no lifecycle beyond active/archived) is executed by Route Assignment → Patrol → Checkpoint Scan (Activity extensions, one per real occurrence). Plan edits are versioned; an in-progress/completed execution pins the plan version it started against — never a live FK reference, or a later edit silently rewrites what already happened. A negative outcome (a missed required occurrence) gets its own real row (Patrol's `missed` status), not a silent gap in the data.
- **Dedup/merge is for identity, not events.** Entity Registry Core's full dedup/merge/human-review workflow is for things with a real "duplicate identity" risk (a Person, an Incident). A routine timestamped event (Checkpoint Scan) doesn't need it — at most a narrow write-time debounce against an accidental double-read. Don't inherit the heavier mechanism by default onto a type it wasn't designed for.
- **`extended_fields` (Entity Registry Core) + Tenant-Defined Types & Custom Fields (Module 0)**: every `Entity`/`EntityAssociation` row carries one bounded, schema-governed JSONB escape hatch — concrete TPT columns remain the unconditional default; `extended_fields` is only for a handful of low-value optional fields or genuine tenant-defined custom fields/subtypes. **Tenant-Defined Subtypes are always anchored on a concrete developer-built type** (corrected mid-review from an earlier "registered type with no table at all" framing that skewed the user's intent): a subtype's records ARE real records of the anchor (designated via the Entity root's `tenant_subtype_ref`), inheriting the anchor's entire platform treatment — there is no free-floating tenant-defined type. Custom field keys are always `custom_`-prefixed (reserved namespace). Two non-entity carriers (Domain Events Rule, CLI Alias) already prove the mechanism generalizes beyond Master Records — any future "many small categories, each with a few optional fields" or "let a tenant self-serve a new kind of record" need should register against this, not reinvent it.
- **AI / LLM Services (Module 0)** owns all AI-provider plumbing platform-wide: SaaS-pooled tokens or tenant BYO API key (Anthropic/OpenAI/Gemini/Azure OpenAI), Prompt Templates with schema-validated `{placeholder}` substitution, and Custom Instructions resolved by a category × location lookup (a new two-axis scoping shape, distinct from Settings & Preferences' single chain). A consuming feature declares an **AI Context** (a `context_key` + its available placeholders) and never talks to a provider or assembles a prompt directly. **AI always proposes, a human always confirms** before generated content becomes part of any official record (Incident Update's `source = ai_generated` only commits on explicit approval) — this is a hard requirement for any future AI-generation feature, not a configurable default.
- **A launch-point pattern for spinning off a richer record from a lightweight one, with context pre-filled.** Checkpoint Scan → Patrol Finding/Incident, Incident escalation (`escalated_from_ref`) — any Activity that logs something in the moment should be a launch point for a fuller record via ordinary Command/Action Bus context-seeding, not a dead end.
- **Offline is a minimal capture subset, never 1:1 parity (revised decision — supersedes the hybrid CRDT + LWW design).** Offline writes follow a three-class contract: create new records (client UUID, locally editable until first sync), append rows to existing records' timelines (including sole-actor execution transitions like completing your own Patrol), and never mutate shared server-side records offline — Dispatcher on-behalf-of (radio relay) is the platform's primary disconnected workflow, elevated to a first-class cross-cutting principle. Conflicts are impossible by construction, so there is no conflict-resolution engine. When specifying any future feature, the question is "which of its Activities are append-capturable offline, if any" (default: none) — never "how does this feature work offline." Tripwire: a feature proposing offline co-editing of shared mutable state must be flagged before drafting.
- **A checked-out numbered range must fully reconcile before it's closed** (Tickets/Citations' Ticket Book: checkout → void-or-issue → reconcile, plus a `lost` terminal state) — reuse this shape for any future physical numbered-pad/tag tracking need, layered *alongside*, never instead of, a record's own digital offline-safe identity.
- **When two+ features independently need the same non-trivial capability, promote it to a shared Platform Core feature** rather than letting each build its own version — this happened four times this session (Domain Events/Command Bus/Palette split from Event & Command Bus Architecture; Tenant-Defined Types & Custom Fields promoted from a Courtesy Patrol-local field; AI/LLM Services promoted from an assumed dependency in AI-Assisted Incident Report Writing/CLI-Style Input). Treat a third instance of "I need this again" as a strong signal to check `_DECISIONS.md` before building locally.

## A note on tooling

`AskUserQuestion` intermittently dropped mid-session ("Tool permission stream closed before response received") — several times, not a one-off. When it fails, don't assume the user saw or answered it; say so plainly and either retry once or ask the user to answer in plain text/screenshot. Don't silently proceed on assumed answers.

## A note on working style

This session ran long and context got heavy, which is why we're handing off. Keep responses tight, avoid re-deriving things already settled in `_DECISIONS.md`, and don't be afraid to push back if something in this resume doc looks wrong once you're looking at the live repo state — verify before trusting, same as always.

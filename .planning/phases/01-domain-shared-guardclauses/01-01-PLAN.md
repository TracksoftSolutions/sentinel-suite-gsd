---
phase: 01-domain-shared-guardclauses
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SentinelSuite.Framework.Domain.Shared.Tests.csproj
  - SentinelSuite/global.json
  - SentinelSuite/SentinelSuite.slnx
autonomous: true
requirements: [PRIM-01]
tags: [guard-clauses, dotnet, domain-shared, test-infra]

must_haves:
  truths:
    - "SentinelSuite.Framework.Domain.Shared.Tests project builds and is part of the solution, ready to host guard-clause tests"
    - "dotnet test routes through Microsoft.Testing.Platform once global.json exists at the solution root"
  artifacts:
    - path: "SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SentinelSuite.Framework.Domain.Shared.Tests.csproj"
      provides: "MTP-native xUnit v3 test project scaffold, hand-authored (not dotnet new xunit)"
    - path: "SentinelSuite/global.json"
      provides: "Routes dotnet test through Microsoft.Testing.Platform on the .NET 10 SDK"
  key_links:
    - from: "SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SentinelSuite.Framework.Domain.Shared.Tests.csproj"
      to: "SentinelSuite/SentinelSuite.Framework.Domain.Shared/SentinelSuite.Framework.Domain.Shared.csproj"
      via: "ProjectReference element"
      pattern: "ProjectReference Include=.*Domain\\.Shared\\.csproj"
    - from: "SentinelSuite/SentinelSuite.slnx"
      to: "SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SentinelSuite.Framework.Domain.Shared.Tests.csproj"
      via: "solution membership (third Project Path entry)"
      pattern: "Domain\\.Shared\\.Tests\\.csproj"
---

<objective>
Stand up the MTP-native (Microsoft.Testing.Platform) xUnit v3 test project and solution-level scaffolding that every later Wave 2 guard-clause plan depends on to write and run tests. This is the phase's Wave 0/Wave 1 test-infrastructure work called out explicitly in 01-VALIDATION.md — no guard-clause source code is written in this plan.

Purpose: Guarantee `dotnet test`/`dotnet run --project ...Tests` works correctly on the very first task of the very first phase of this milestone, using the corrected (MTP-native, not VSTest) tooling RESEARCH.md identifies.
Output: A buildable, empty `SentinelSuite.Framework.Domain.Shared.Tests` project wired into `SentinelSuite.slnx`, plus `SentinelSuite/global.json` routing test execution through MTP.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-domain-shared-guardclauses/01-CONTEXT.md
@.planning/phases/01-domain-shared-guardclauses/01-RESEARCH.md
@.planning/phases/01-domain-shared-guardclauses/01-PATTERNS.md
@.planning/phases/01-domain-shared-guardclauses/01-VALIDATION.md
@SentinelSuite/SentinelSuite.slnx
@SentinelSuite/SentinelSuite.Framework.Domain.Shared/SentinelSuite.Framework.Domain.Shared.csproj
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hand-author the MTP-native xUnit v3 test project</name>
  <files>SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SentinelSuite.Framework.Domain.Shared.Tests.csproj</files>
  <read_first>
    - SentinelSuite/SentinelSuite.Framework.Domain.Shared/SentinelSuite.Framework.Domain.Shared.csproj (existing scaffold shape: Sdk, TargetFramework, ImplicitUsings, Nullable)
    - .planning/phases/01-domain-shared-guardclauses/01-RESEARCH.md, section "xUnit v3 test project (.csproj), hand-authored to guarantee MTP + no VSTest packages" and "Pitfall 5: The SDK's built-in dotnet new xunit template is not xUnit v3 / MTP" and "Package Legitimacy Audit"
    - .planning/phases/01-domain-shared-guardclauses/01-PATTERNS.md, section "SentinelSuite.Framework.Domain.Shared.Tests/* (test project + test files)"
  </read_first>
  <action>
    Create the new project directory SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/ containing SentinelSuite.Framework.Domain.Shared.Tests.csproj using Sdk="Microsoft.NET.Sdk". Set TargetFramework to net10.0, ImplicitUsings enable, Nullable enable, IsPackable false, OutputType Exe, UseMicrosoftTestingPlatformRunner true, TestingPlatformDotnetTestSupport true. Add a package reference for xunit.v3 pinned to version 3.2.2 exactly (matches RESEARCH.md's Package Legitimacy Audit verdict — do not use a floating "*" or "latest" range). Add a package reference for coverlet.mtp pinned to version 10.0.1 exactly — the MTP-native coverage collector; RESEARCH.md Pitfall 4 documents that the VSTest-era coverage collector this project's CLAUDE.md originally recommended does not work with MTP-hosted projects, so use the MTP-native package instead. Add a ProjectReference to ../SentinelSuite.Framework.Domain.Shared/SentinelSuite.Framework.Domain.Shared.csproj. Do not add a Program.cs — the MTP runner property generates the test host entry point automatically. Do not run `dotnet new xunit` to scaffold this project — per Pitfall 5, the SDK's built-in template produces a VSTest-based (pre-MTP) project shape even on a fresh .NET 10 SDK install; hand-author the file directly instead.
  </action>
  <acceptance_criteria>
    - File exists at SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SentinelSuite.Framework.Domain.Shared.Tests.csproj
    - Contains a package reference for xunit.v3 version 3.2.2 exactly
    - Contains a package reference for coverlet.mtp version 10.0.1 exactly
    - Does NOT contain a package reference for the VSTest-era coverage collector (coverlet.collector)
    - Contains UseMicrosoftTestingPlatformRunner set to true
    - Contains a ProjectReference pointing at SentinelSuite.Framework.Domain.Shared.csproj
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'coverlet.collector' SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SentinelSuite.Framework.Domain.Shared.Tests.csproj | grep -x 0 && grep -c 'coverlet.mtp' SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SentinelSuite.Framework.Domain.Shared.Tests.csproj | grep -qv '^0$' && grep -c 'UseMicrosoftTestingPlatformRunner' SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SentinelSuite.Framework.Domain.Shared.Tests.csproj | grep -qv '^0$'</automated>
  </verify>
  <done>csproj created with pinned MTP-native package versions (xunit.v3 3.2.2, coverlet.mtp 10.0.1) and a correct ProjectReference; no VSTest-era coverage package present.</done>
</task>

<task type="auto">
  <name>Task 2: Wire global.json + slnx, confirm solution-wide build</name>
  <files>SentinelSuite/global.json, SentinelSuite/SentinelSuite.slnx</files>
  <read_first>
    - SentinelSuite/SentinelSuite.slnx (current 2-project content, exact `&lt;Project Path=.../&gt;` element shape to replicate)
    - .planning/phases/01-domain-shared-guardclauses/01-RESEARCH.md, section "global.json — required at the solution root for dotnet test to route through MTP on SDK 10+" and "Environment Availability"
    - .planning/phases/01-domain-shared-guardclauses/01-PATTERNS.md, sections "SentinelSuite/global.json (config, net-new)" and "SentinelSuite.slnx (modified, additive)"
  </read_first>
  <action>
    Create SentinelSuite/global.json at the solution root (next to SentinelSuite.slnx) with a "test" object whose "runner" key is "Microsoft.Testing.Platform" — this routes `dotnet test` through MTP on the .NET 10 SDK. Edit SentinelSuite/SentinelSuite.slnx to add a third Project Path element pointing at SentinelSuite.Framework.Domain.Shared.Tests/SentinelSuite.Framework.Domain.Shared.Tests.csproj, following the exact element shape of the two existing entries — do not reformat or remove the existing two entries. After both edits, run a solution-wide build to confirm all three projects (including the test project from Task 1) compile together under the .NET 10 SDK.
  </action>
  <acceptance_criteria>
    - SentinelSuite/global.json exists and is valid JSON with test.runner equal to "Microsoft.Testing.Platform"
    - SentinelSuite/SentinelSuite.slnx contains exactly 3 Project Path entries
    - The two pre-existing Project Path entries are unmodified (same paths, same element shape)
    - `dotnet build` succeeds for the whole solution with exit code 0
  </acceptance_criteria>
  <verify>
    <automated>cd SentinelSuite && dotnet build SentinelSuite.slnx && grep -c '&lt;Project Path=' SentinelSuite.slnx | grep -x 3</automated>
  </verify>
  <done>global.json present and routes MTP; slnx references all 3 projects; whole solution builds successfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Developer machine / CI build ↔ public NuGet registry | Test-project-only package installs (xunit.v3, coverlet.mtp) pull compiled binaries from an external registry into the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|------------|----------|--------------|------------------|
| T-1-01 | Tampering | NuGet package installs (xunit.v3, coverlet.mtp) in the test project | high | mitigate | Both packages were independently verified against the NuGet v3 flatcontainer registry during research (RESEARCH.md Package Legitimacy Audit — verdict OK, no ASSUMED/SUS/SLOP findings). Task 1 pins the exact audited versions (3.2.2 / 10.0.1) rather than a floating range, preventing a silent future upgrade to an unaudited version. |
| T-1-02 | Repudiation | Test project scaffolding (no runtime surface) | low | accept | Build-time-only scaffolding with no user-facing or persisted data; no audit trail is meaningful at this layer. |

</threat_model>

<verification>
Run `cd SentinelSuite && dotnet build SentinelSuite.slnx` — expect the whole 3-project solution to build with zero errors. Confirm `SentinelSuite/global.json` and `SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SentinelSuite.Framework.Domain.Shared.Tests.csproj` both exist on disk.
</verification>

<success_criteria>
- Solution builds with the new test project included, zero errors.
- Test project uses xunit.v3 3.2.2 + coverlet.mtp 10.0.1 (not VSTest packages).
- global.json routes `dotnet test` through Microsoft.Testing.Platform.
- Wave 2 plans (01-03 through 01-06) can immediately add test files under SentinelSuite.Framework.Domain.Shared.Tests/Guards/ and run them via `dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter <ClassName>`.
</success_criteria>

## Artifacts This Phase Produces

- `SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SentinelSuite.Framework.Domain.Shared.Tests.csproj` — new MTP-native xUnit v3 test project
- `SentinelSuite/global.json` — new, routes `dotnet test` through Microsoft.Testing.Platform
- `SentinelSuite/SentinelSuite.slnx` — modified, adds the third project entry

<output>
Create `.planning/phases/01-domain-shared-guardclauses/01-01-SUMMARY.md` when done
</output>

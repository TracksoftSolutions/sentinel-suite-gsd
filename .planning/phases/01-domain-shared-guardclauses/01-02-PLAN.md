---
phase: 01-domain-shared-guardclauses
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/IGuardClause.cs
  - SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/Guard.cs
autonomous: true
requirements: [PRIM-01]
tags: [guard-clauses, dotnet, domain-shared]

must_haves:
  truths:
    - "Guard.Against is a valid call-site expression that type-checks as IGuardClause, ready for extension methods to attach to"
    - "Domain.Shared remains free of third-party package references after this plan"
    - "A future module can add its own Guard.Against.X(...) guard without editing Domain.Shared, per the documented naming convention"
  artifacts:
    - path: "SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/IGuardClause.cs"
      provides: "Zero-member extensibility marker interface (not a domain-capability marker)"
    - path: "SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/Guard.cs"
      provides: "Guard.Against static entry point + documented per-module naming convention"
  key_links:
    - from: "SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/Guard.cs"
      to: "SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/IGuardClause.cs"
      via: "Guard : IGuardClause, Against property returns IGuardClause"
      pattern: "class Guard\\s*:\\s*IGuardClause"
---

<objective>
Build the two foundational, zero-behavior types every guard method in this phase (and every future guard-clause extension in the kernel) attaches to: the `IGuardClause` extensibility marker interface and the `Guard.Against` static entry point (D-01, D-05, D-10, D-11). Document the per-module guard-extension naming convention (D-06) and the rationale distinguishing `IGuardClause` from the domain-capability marker-interface anti-pattern `docs/architecture-guidance.md` warns against (RESEARCH.md Pitfall 6), so a future architecture review doesn't misflag it.

Purpose: Give Wave 2's guard-family plans (01-03 through 01-06) a stable, already-compiling `IGuardClause`/`Guard` foundation to extend, and give every one of the milestone's 26 future modules a documented, precedent-setting extension pattern.
Output: `IGuardClause.cs` and `Guard.cs` compiling in `SentinelSuite.Framework.Domain.Shared/Guards/`, with inline documentation covering D-06 and Pitfall 6.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-domain-shared-guardclauses/01-CONTEXT.md
@.planning/phases/01-domain-shared-guardclauses/01-RESEARCH.md
@.planning/phases/01-domain-shared-guardclauses/01-PATTERNS.md
@docs/architecture-guidance.md
@SentinelSuite/SentinelSuite.Framework.Domain.Shared/SentinelSuite.Framework.Domain.Shared.csproj
</context>

<tasks>

<task type="auto">
  <name>Task 1: IGuardClause marker interface + Pitfall-6 rationale doc</name>
  <files>SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/IGuardClause.cs</files>
  <read_first>
    - .planning/phases/01-domain-shared-guardclauses/01-CONTEXT.md, decisions D-05 and D-06
    - .planning/phases/01-domain-shared-guardclauses/01-RESEARCH.md, "Pitfall 6: IGuardClause's empty-marker-interface shape superficially resembles a pattern docs/architecture-guidance.md warns against — it is not the same category"
    - .planning/phases/01-domain-shared-guardclauses/01-PATTERNS.md, section "Guards/IGuardClause.cs (interface, extensibility marker)"
    - docs/architecture-guidance.md, line 27: "A marker interface with no members is a smell" (the rule this file's doc comment must distinguish itself from)
  </read_first>
  <action>
    Create namespace SentinelSuite.Framework.Domain.Shared.Guards. Define public interface IGuardClause with zero members (a pure extensibility/dispatch anchor for guard-clause extension methods, per D-05). Add an XML doc summary comment on the interface that explicitly distinguishes it from the domain-capability marker-interface pattern docs/architecture-guidance.md warns against: state that this interface has no registry, no runtime capability query, and no domain concept behind it — it exists purely so any assembly can add a Guard.Against.X(...) extension method with zero changes to Domain.Shared. This doc comment is the required rebuttal artifact for a future architecture review that might otherwise flag D-05 against the marker-interface smell rule.
  </action>
  <acceptance_criteria>
    - File exists at SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/IGuardClause.cs
    - Declares `public interface IGuardClause` with no members between its braces
    - Namespace declaration is exactly SentinelSuite.Framework.Domain.Shared.Guards
    - The XML doc comment on the interface references both the concept of a marker interface and the fact that it has no registry/no runtime capability query, to prove the Pitfall-6 rebuttal is present
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'interface IGuardClause' SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/IGuardClause.cs | grep -qv '^0$' &amp;&amp; grep -c 'namespace SentinelSuite.Framework.Domain.Shared.Guards' SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/IGuardClause.cs | grep -qv '^0$'</automated>
  </verify>
  <done>IGuardClause.cs compiles as an empty marker interface with the Pitfall-6 rationale documented inline.</done>
</task>

<task type="auto">
  <name>Task 2: Guard static entry point + D-06 naming-convention doc</name>
  <files>SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/Guard.cs</files>
  <read_first>
    - .planning/phases/01-domain-shared-guardclauses/01-CONTEXT.md, decisions D-01, D-06, D-10, D-11
    - .planning/phases/01-domain-shared-guardclauses/01-RESEARCH.md, "Architecture Patterns" system diagram and "Guard.cs (static entry point)" code
    - .planning/phases/01-domain-shared-guardclauses/01-PATTERNS.md, section "Guards/Guard.cs (static entry point)"
    - SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/IGuardClause.cs (from Task 1 — the interface Guard implements and returns)
    - SentinelSuite/SentinelSuite.Framework.Domain.Shared/SentinelSuite.Framework.Domain.Shared.csproj (confirm it stays untouched/dependency-free after this task)
  </read_first>
  <action>
    In namespace SentinelSuite.Framework.Domain.Shared.Guards, define public sealed class Guard : IGuardClause with a private parameterless constructor and one public static property named Against of type IGuardClause, returning a single lazily-constructed instance of Guard (matching D-01/D-11's literal `Guard.Against.X` call-site shape — call sites never construct Guard directly). Add an XML remarks doc block on the Guard class documenting the naming convention for guard-extension classes (D-06): framework-level guards in this assembly use the pattern GuardAgainst{Concept}Extensions (one static class per concern, for example GuardAgainstNullExtensions); any downstream module adding its own domain-specific guard must name its extension class using a {Module}GuardExtensions pattern (for example a hypothetical MultiTenancyGuardExtensions) and implement it as an extension method on IGuardClause, requiring zero edits to Domain.Shared. Confirm SentinelSuite.Framework.Domain.Shared.csproj still declares zero third-party package references after adding this file — the project must remain dependency-free.
  </action>
  <acceptance_criteria>
    - File exists at SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/Guard.cs
    - Declares `public sealed class Guard : IGuardClause` with a private constructor and a public static Against property of type IGuardClause
    - The XML doc remarks mention both the GuardAgainst{Concept}Extensions convention and the {Module}GuardExtensions convention for downstream modules
    - SentinelSuite.Framework.Domain.Shared.csproj still declares zero third-party package references
    - `dotnet build` of Domain.Shared succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd SentinelSuite &amp;&amp; dotnet build SentinelSuite.Framework.Domain.Shared/SentinelSuite.Framework.Domain.Shared.csproj &amp;&amp; grep -c 'PackageReference' SentinelSuite.Framework.Domain.Shared/SentinelSuite.Framework.Domain.Shared.csproj | grep -x 0</automated>
  </verify>
  <done>Guard.cs compiles, exposes Guard.Against as IGuardClause, naming convention documented inline, Domain.Shared remains dependency-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A this plan | Pure static scaffolding types with no external/untrusted input surface; real input-validation threats are introduced in Wave 2 (01-03 through 01-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|------------|----------|--------------|------------------|
| T-1-03 | Tampering | Guard.cs / IGuardClause.cs (foundation types, no runtime input yet) | low | accept | Pure static scaffolding with no runtime input surface at this layer; the actual input-validation guard logic (and its threat mitigations) lands in Wave 2 plans 01-03 through 01-06. |
| T-1-04 | Elevation of Privilege / design-drift | Convention bypass — a future contributor hardcodes ad hoc validation instead of extending via IGuardClause | low | accept | Enforced by the documented naming convention (D-06) and ordinary code review discipline; bypassing the convention produces inconsistent code style, not a runtime-exploitable vulnerability. |

</threat_model>

<verification>
Run `cd SentinelSuite && dotnet build SentinelSuite.Framework.Domain.Shared/SentinelSuite.Framework.Domain.Shared.csproj` — expect zero errors. Confirm the .csproj still has no PackageReference elements.
</verification>

<success_criteria>
- IGuardClause.cs and Guard.cs both compile in Domain.Shared with zero third-party package references.
- Guard.Against.* is a syntactically valid, type-checked expression ready for Wave 2's extension methods.
- D-06's naming convention and Pitfall 6's marker-interface rebuttal are both documented inline in code.
</success_criteria>

## Artifacts This Phase Produces

- `IGuardClause` — empty marker interface, `SentinelSuite.Framework.Domain.Shared.Guards` namespace
- `Guard` — sealed class implementing `IGuardClause`, exposes `static IGuardClause Against { get; }`

<output>
Create `.planning/phases/01-domain-shared-guardclauses/01-02-SUMMARY.md` when done
</output>

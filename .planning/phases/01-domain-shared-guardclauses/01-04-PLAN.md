---
phase: 01-domain-shared-guardclauses
plan: 4
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/GuardAgainstRangeExtensions.cs
  - SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/Guards/GuardAgainstRangeTests.cs
autonomous: true
requirements: [PRIM-01]
tags: [guard-clauses, dotnet, domain-shared, tdd]

must_haves:
  truths:
    - "Guard.Against.OutOfRange returns valid in-range (inclusive-boundary) input unchanged and throws ArgumentOutOfRangeException/ArgumentException correctly for out-of-range input and invalid range definitions"
    - "Guard.Against.EnumOutOfRange returns defined enum values unchanged and throws InvalidEnumArgumentException for undefined values"
  artifacts:
    - path: "SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/GuardAgainstRangeExtensions.cs"
      provides: "OutOfRange<T>, EnumOutOfRange<T>"
    - path: "SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/Guards/GuardAgainstRangeTests.cs"
      provides: "Pass/throw unit test coverage for the Range guard family"
  key_links:
    - from: "SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/Guards/GuardAgainstRangeTests.cs"
      to: "SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/GuardAgainstRangeExtensions.cs"
      via: "test calls Guard.Against.OutOfRange / EnumOutOfRange"
      pattern: "Guard\\.Against\\.(OutOfRange|EnumOutOfRange)"
---

<objective>
Implement the range and enum-membership guard family, directly satisfying ROADMAP.md's Phase 1 success criterion #3 ("Passing xUnit tests cover empty/range/enum-membership guard scenarios, both pass and throw paths").

Purpose: Give downstream types (e.g., a future multi-tenancy isolation-tier enum, numeric quantity ranges) a correct, generic way to guard both comparable-range and enum-membership invariants.
Output: `GuardAgainstRangeExtensions.cs` compiling with `OutOfRange<T>` and `EnumOutOfRange<T>`; a fully green `GuardAgainstRangeTests.cs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-domain-shared-guardclauses/01-CONTEXT.md
@.planning/phases/01-domain-shared-guardclauses/01-RESEARCH.md
@.planning/phases/01-domain-shared-guardclauses/01-PATTERNS.md
@.planning/phases/01-domain-shared-guardclauses/01-VALIDATION.md
</context>

<interfaces>
<!-- From 01-02-PLAN.md (Wave 1) — the foundation this plan's extension methods attach to. -->
Namespace SentinelSuite.Framework.Domain.Shared.Guards:
- `public interface IGuardClause` — zero-member extensibility marker.
- `public sealed class Guard : IGuardClause` — exposes `public static IGuardClause Against { get; }`.
Every method in this plan is an extension method: `this IGuardClause guardClause` as the first parameter, invoked as `Guard.Against.MethodName(...)`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OutOfRange&lt;T&gt;</name>
  <files>SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/GuardAgainstRangeExtensions.cs, SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/Guards/GuardAgainstRangeTests.cs</files>
  <read_first>
    - .planning/phases/01-domain-shared-guardclauses/01-RESEARCH.md, "Range and enum guards" code example and "Pitfall 3: Two of Ardalis's methods don't fit this project's constraints — exclude them from the rounded-out list"
    - .planning/phases/01-domain-shared-guardclauses/01-PATTERNS.md, section "Guards/GuardAgainstRangeExtensions.cs (OutOfRange, EnumOutOfRange)"
    - SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/IGuardClause.cs (from Plan 01-02)
  </read_first>
  <behavior>
    - Test: Guard.Against.OutOfRange(5, 1, 10) returns 5 unchanged (in-range).
    - Test: Guard.Against.OutOfRange(1, 1, 10) returns 1 (lower boundary is inclusive).
    - Test: Guard.Against.OutOfRange(10, 1, 10) returns 10 (upper boundary is inclusive).
    - Test: Guard.Against.OutOfRange(0, 1, 10) throws ArgumentOutOfRangeException with ParamName equal to the captured input expression.
    - Test: Guard.Against.OutOfRange(11, 1, 10) throws ArgumentOutOfRangeException.
    - Test: Guard.Against.OutOfRange(5, 10, 1) (rangeFrom greater than rangeTo — an invalid range definition) throws ArgumentException, not ArgumentOutOfRangeException.
  </behavior>
  <action>
    In namespace SentinelSuite.Framework.Domain.Shared.Guards, create static class GuardAgainstRangeExtensions with OutOfRange&lt;T&gt;(this IGuardClause guardClause, T input, T rangeFrom, T rangeTo, parameterName via CallerArgumentExpression targeting input), constrained where T : IComparable, IComparable&lt;T&gt;. If rangeFrom compares greater than rangeTo, throw a new ArgumentException whose message references only the rangeFrom/rangeTo parameter names (never a data value). If input compares less than rangeFrom or greater than rangeTo, throw a new ArgumentOutOfRangeException using parameterName, with a message that interpolates only parameterName, never the actual input value. Otherwise return input. Create GuardAgainstRangeTests.cs under the test project's Guards/ folder implementing all 6 behavior-block cases as xUnit v3 [Fact] methods.
  </action>
  <acceptance_criteria>
    - GuardAgainstRangeExtensions.cs declares OutOfRange&lt;T&gt; constrained to IComparable and IComparable&lt;T&gt;
    - Throws ArgumentException when rangeFrom is greater than rangeTo, ArgumentOutOfRangeException when input is outside [rangeFrom, rangeTo]
    - Boundary values (rangeFrom, rangeTo themselves) pass through without throwing
    - All 6 behavior-block test cases pass
    - The file does not implement any SQL-Server-specific or persistence-flavored range guard
  </acceptance_criteria>
  <verify>
    <automated>cd SentinelSuite &amp;&amp; dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter GuardAgainstRangeTests</automated>
  </verify>
  <done>OutOfRange&lt;T&gt; implemented, inclusive-boundary semantics correct, invalid-range-definition case distinguished from out-of-bounds case.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EnumOutOfRange&lt;T&gt; + persistence-guard exclusion note</name>
  <files>SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/GuardAgainstRangeExtensions.cs, SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/Guards/GuardAgainstRangeTests.cs</files>
  <read_first>
    - .planning/phases/01-domain-shared-guardclauses/01-RESEARCH.md, "EnumOutOfRange" code example and "Pitfall 3" (rationale for excluding the SQL-Server-specific range guard)
    - .planning/phases/01-domain-shared-guardclauses/01-PATTERNS.md, "Explicitly exclude OutOfSQLDateRange" note
    - SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/GuardAgainstRangeExtensions.cs (file from Task 1 — add to it)
  </read_first>
  <behavior>
    - Test: define a small test-fixture enum with three defined members; Guard.Against.EnumOutOfRange with a defined member value returns that value unchanged.
    - Test: Guard.Against.EnumOutOfRange with a value cast from an integer not defined on the enum throws System.ComponentModel.InvalidEnumArgumentException.
    - Test: the thrown InvalidEnumArgumentException correctly carries the captured parameter name.
  </behavior>
  <action>
    Add EnumOutOfRange&lt;T&gt;(this IGuardClause guardClause, T input, parameterName via CallerArgumentExpression targeting input) to GuardAgainstRangeExtensions, constrained where T : struct, Enum. If the value is not defined on the enum type, throw a new System.ComponentModel.InvalidEnumArgumentException using parameterName, the integer-converted input, and the enum's type (this is a BCL type, no package needed on net10.0). Otherwise return input. Add a one-line code comment above the class noting that a SQL-Server-specific date-range guard is deliberately excluded from this file: it bakes a persistence-engine-specific assumption into a persistence-agnostic Domain.Shared kernel, violating Clean Architecture's dependency direction (only Infrastructure may know a concrete database engine exists) — this is a permanent exclusion, not a placeholder for a future phase. Add the 3 behavior-block test cases above to GuardAgainstRangeTests.cs, including a small private test-fixture enum declared in the test file.
  </action>
  <acceptance_criteria>
    - GuardAgainstRangeExtensions.cs declares EnumOutOfRange&lt;T&gt; constrained where T : struct, Enum
    - Throws System.ComponentModel.InvalidEnumArgumentException for an undefined enum value, returns the value unchanged for a defined one
    - A code comment documents the deliberate exclusion of the SQL-Server-specific range guard from this Domain.Shared kernel
    - All 3 behavior-block test cases pass
  </acceptance_criteria>
  <verify>
    <automated>cd SentinelSuite &amp;&amp; dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter GuardAgainstRangeTests</automated>
  </verify>
  <done>EnumOutOfRange&lt;T&gt; implemented and green; SQL-specific guard exclusion documented inline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Any future caller (numeric quantities, tenant isolation-tier enums, etc.) → Guard.Against.OutOfRange/EnumOutOfRange | Guards against untrusted numeric/enum values before they reach a downstream constructor |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|------------|----------|--------------|------------------|
| T-1-01 | Tampering | GuardAgainstRangeExtensions.cs (OutOfRange, EnumOutOfRange) | high | mitigate | Fails fast on out-of-bounds numeric input and undefined enum values cast from external/untrusted sources (e.g., a tenant isolation-tier value later deserialized from an external payload) before the invalid value reaches a downstream constructor. |
| T-1-02 | Information Disclosure | ArgumentOutOfRangeException/ArgumentException messages | high | mitigate | Messages interpolate only parameterName/rangeFrom/rangeTo parameter names, never the actual out-of-range input value. |

</threat_model>

<verification>
Run `cd SentinelSuite && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter GuardAgainstRangeTests` — expect every test to pass with zero failures.
</verification>

<success_criteria>
- ROADMAP.md Phase 1 success criterion #3 (range/enum-membership portion) satisfied.
- OutOfRange&lt;T&gt; and EnumOutOfRange&lt;T&gt; correctly implemented with inclusive boundaries and BCL-only exceptions.
- SQL-Server-specific range guard permanently excluded and documented.
</success_criteria>

## Artifacts This Phase Produces

- `GuardAgainstRangeExtensions` static class — `OutOfRange<T>`, `EnumOutOfRange<T>`
- `GuardAgainstRangeTests` — xUnit v3 test class covering pass/throw paths for the Range guard family

<output>
Create `.planning/phases/01-domain-shared-guardclauses/01-04-SUMMARY.md` when done
</output>

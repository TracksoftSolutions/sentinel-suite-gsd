---
phase: 01-domain-shared-guardclauses
plan: 3
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/GuardAgainstNullExtensions.cs
  - SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/Guards/GuardAgainstNullTests.cs
autonomous: true
requirements: [PRIM-01]
tags: [guard-clauses, dotnet, domain-shared, tdd]

must_haves:
  truths:
    - "Guard.Against.Null returns valid reference-type and nullable-value-type input unchanged, and throws ArgumentNullException with the correct captured parameter name on null input"
    - "Guard.Against.NullOrEmpty/NullOrWhiteSpace reject empty/whitespace-only input with ArgumentException, never leaking the raw rejected value in the exception message"
    - "D-02: every guard method in this family captures its parameter name via CallerArgumentExpression, not an explicit nameof(x) argument; D-03: every guard method returns the validated input value unchanged on success, enabling inline assignment"
  artifacts:
    - path: "SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/GuardAgainstNullExtensions.cs"
      provides: "Null<T> (2 overloads: class-constrained, struct-constrained), NullOrEmpty (2 overloads: string, IEnumerable<T>), NullOrWhiteSpace"
    - path: "SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/Guards/GuardAgainstNullTests.cs"
      provides: "Pass/throw unit test coverage for the entire Null guard family"
  key_links:
    - from: "SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/Guards/GuardAgainstNullTests.cs"
      to: "SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/GuardAgainstNullExtensions.cs"
      via: "test calls Guard.Against.Null / NullOrEmpty / NullOrWhiteSpace"
      pattern: "Guard\\.Against\\.(Null|NullOrEmpty|NullOrWhiteSpace)"
---

<objective>
Implement the null-argument guard family — this phase's most load-bearing and easiest-to-get-subtly-wrong guard methods, per RESEARCH.md's two-overload `Null<T>` pattern and the `CallerArgumentExpression`/`[NotNull]` mechanics. This directly satisfies ROADMAP.md's Phase 1 success criterion #2 ("Passing xUnit tests cover null-argument guard scenarios, both pass and throw paths").

Purpose: Give every future `Entity`/`EntityAssociation` constructor (222 features deep) a correct, non-null-inferred way to guard reference and nullable-value-type arguments.
Output: `GuardAgainstNullExtensions.cs` compiling with `Null<T>` (both overloads), `NullOrEmpty` (both overloads), `NullOrWhiteSpace`; a fully green `GuardAgainstNullTests.cs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-domain-shared-guardclauses/01-CONTEXT.md
@.planning/phases/01-domain-shared-guardclauses/01-RESEARCH.md
@.planning/phases/01-domain-shared-guardclauses/01-PATTERNS.md
@.planning/phases/01-domain-shared-guardclauses/01-VALIDATION.md
</context>

<interfaces>
<!-- From 01-02-PLAN.md (Wave 1) — the foundation this plan's extension methods attach to. -->
Namespace SentinelSuite.Framework.Domain.Shared.Guards:
- `public interface IGuardClause` — zero-member extensibility marker.
- `public sealed class Guard : IGuardClause` — exposes `public static IGuardClause Against { get; }`.
Every method in this plan is an extension method: `this IGuardClause guardClause` as the first parameter, invoked as `Guard.Against.MethodName(...)`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Null&lt;T&gt; (two overloads) + NullOrWhiteSpace</name>
  <files>SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/GuardAgainstNullExtensions.cs, SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/Guards/GuardAgainstNullTests.cs</files>
  <read_first>
    - .planning/phases/01-domain-shared-guardclauses/01-RESEARCH.md, "Pattern 1: The two-overload Null&lt;T&gt; shape", "Pattern 2: CallerArgumentExpression applied to an ordinary argument, never the this receiver", "Pitfall 1", "Pitfall 2: [return: NotNull] is not what makes Ardalis's actual pattern work", and "Full guard method with two overloads (Null)" code example
    - .planning/phases/01-domain-shared-guardclauses/01-PATTERNS.md, section "Guards/GuardAgainstNullExtensions.cs (Null, NullOrEmpty, NullOrWhiteSpace)" including "Critical correctness notes (do not deviate)"
    - .planning/phases/01-domain-shared-guardclauses/01-CONTEXT.md, decisions D-01 through D-04, D-12
    - SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/IGuardClause.cs (the interface these methods extend — from Plan 01-02)
  </read_first>
  <behavior>
    - Test: Guard.Against.Null("value") with a non-null reference-type input returns the same string instance unchanged.
    - Test: Guard.Against.Null&lt;string&gt;(null) throws ArgumentNullException whose ParamName equals the captured call-site expression name (no explicit nameof passed, via CallerArgumentExpression).
    - Test: Guard.Against.Null&lt;int?&gt;(5) (a nullable value-type input) returns the unwrapped int 5.
    - Test: Guard.Against.Null&lt;int?&gt;(null) throws ArgumentNullException.
    - Test: Guard.Against.NullOrWhiteSpace("hello") returns "hello" unchanged.
    - Test: Guard.Against.NullOrWhiteSpace("   ") (whitespace-only) throws ArgumentException (not ArgumentNullException), and the exception message is a fixed string that does not contain the rejected whitespace value.
    - Test: Guard.Against.NullOrWhiteSpace(null) throws ArgumentNullException (delegates to Null first).
  </behavior>
  <action>
    In namespace SentinelSuite.Framework.Domain.Shared.Guards, create static class GuardAgainstNullExtensions with two Null&lt;T&gt; overloads on `this IGuardClause guardClause`: one constrained `where T : class` returning T, one constrained `where T : struct` accepting a nullable T and returning the unwrapped value. Decorate the `input` parameter (never the `this` receiver — see Pitfall 1) with the NotNull attribute on both overloads, and give both a `parameterName` parameter defaulted via CallerArgumentExpression targeting `input`. Do not add a return-value NotNull attribute on either overload — RESEARCH.md Pitfall 2 confirms Ardalis's real source omits it, and adding one triggers a compiler warning on the reference-type overload whose return type is already non-nullable; the non-nullable return type T combined with the NotNull attribute on the input parameter is the complete, sufficient mechanism. Both overloads throw a new ArgumentNullException using the captured parameterName when input is null. Add method NullOrWhiteSpace(this IGuardClause guardClause, string? input, parameterName via CallerArgumentExpression): first call Guard.Against.Null(input, parameterName), then if the string is null-or-whitespace throw a new ArgumentException with a fixed message string ("Required input was empty or whitespace.") and the parameterName — the message must never interpolate the actual whitespace/empty value being rejected (Information-Disclosure mitigation per RESEARCH.md Security Domain). Return input when valid. Then create GuardAgainstNullTests.cs under the test project's Guards/ folder implementing every case in the behavior block above as a distinct xUnit v3 [Fact], asserting both the returned value (pass path) and the thrown exception's type plus ParamName (throw path).
  </action>
  <acceptance_criteria>
    - GuardAgainstNullExtensions.cs declares exactly two methods named Null (two Null&lt;T&gt; overloads: one `where T : class`, one `where T : struct`)
    - Both Null&lt;T&gt; overloads carry the NotNull attribute on the input parameter and CallerArgumentExpression targeting input for parameterName
    - NullOrWhiteSpace calls Guard.Against.Null internally before its whitespace check
    - No return-value NotNull attribute appears anywhere in the file
    - All 7 behavior-block test cases pass when run
    - The exception message thrown by NullOrWhiteSpace on invalid input is a fixed string literal, not a string interpolation of the input value
  </acceptance_criteria>
  <verify>
    <automated>cd SentinelSuite &amp;&amp; dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter GuardAgainstNullTests</automated>
  </verify>
  <done>Null&lt;T&gt; (both overloads) and NullOrWhiteSpace implemented per Ardalis's exact mechanics; all pass/throw test cases green; no return-value-NotNull regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NullOrEmpty (string + IEnumerable&lt;T&gt; overloads)</name>
  <files>SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/GuardAgainstNullExtensions.cs, SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/Guards/GuardAgainstNullTests.cs</files>
  <read_first>
    - .planning/phases/01-domain-shared-guardclauses/01-PATTERNS.md, note "NullOrEmpty follows the same shape as NullOrWhiteSpace... string + IEnumerable&lt;T&gt; variants"
    - .planning/phases/01-domain-shared-guardclauses/01-CONTEXT.md, decision D-08 (confirms NullOrEmpty is part of the confirmed floor)
    - SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/GuardAgainstNullExtensions.cs (file from Task 1 — add to it, do not overwrite Null/NullOrWhiteSpace)
  </read_first>
  <behavior>
    - Test: Guard.Against.NullOrEmpty("value") returns "value" unchanged.
    - Test: Guard.Against.NullOrEmpty("") (empty string) throws ArgumentException.
    - Test: Guard.Against.NullOrEmpty((string?)null) throws ArgumentNullException.
    - Test: Guard.Against.NullOrEmpty(a non-empty List&lt;int&gt;) returns the same collection instance unchanged.
    - Test: Guard.Against.NullOrEmpty(an empty List&lt;int&gt;) throws ArgumentException.
    - Test: Guard.Against.NullOrEmpty((IEnumerable&lt;int&gt;?)null) throws ArgumentNullException.
  </behavior>
  <action>
    Add two NullOrEmpty overloads to the existing GuardAgainstNullExtensions static class: a string overload that calls Guard.Against.Null first, then throws a new ArgumentException with a fixed message ("Required input was empty.") and the parameterName if the string's Length is 0; and a generic NullOrEmpty&lt;T&gt; overload accepting IEnumerable&lt;T&gt; that calls Guard.Against.Null first, then throws the same-shaped ArgumentException if the sequence has no elements. Both return input unchanged when valid. Add the 6 behavior-block test cases above to GuardAgainstNullTests.cs as additional [Fact] methods.
  </action>
  <acceptance_criteria>
    - GuardAgainstNullExtensions.cs now declares two NullOrEmpty overloads (string and IEnumerable&lt;T&gt;)
    - Both delegate to Guard.Against.Null before the emptiness check
    - All 6 behavior-block test cases pass
  </acceptance_criteria>
  <verify>
    <automated>cd SentinelSuite &amp;&amp; dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter GuardAgainstNullTests</automated>
  </verify>
  <done>NullOrEmpty (string + IEnumerable&lt;T&gt;) implemented and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Any future caller (Entity/EntityAssociation constructors, UseCases validators, module code) → Guard.Against.* | This is the kernel's first real input-validation surface — every downstream constructor across 222 planned features will call through here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|------------|----------|--------------|------------------|
| T-1-01 | Tampering | GuardAgainstNullExtensions.cs (Null, NullOrEmpty, NullOrWhiteSpace) | high | mitigate | Every call fails fast with a specific BCL exception before a null/empty/whitespace value can propagate into a downstream constructor; verified by the pass/throw unit test pairs in both tasks. |
| T-1-02 | Information Disclosure | Exception messages thrown by NullOrWhiteSpace/NullOrEmpty | high | mitigate | Messages are fixed string literals referencing only parameterName (captured via CallerArgumentExpression), never the raw invalid input value — confirmed by acceptance criteria requiring a static string, not an interpolation. |

</threat_model>

<verification>
Run `cd SentinelSuite && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter GuardAgainstNullTests` — expect every test to pass with zero failures.
</verification>

<success_criteria>
- ROADMAP.md Phase 1 success criterion #2 satisfied: passing xUnit tests cover null-argument guard scenarios (both pass and throw paths).
- Null&lt;T&gt;, NullOrEmpty, NullOrWhiteSpace all correctly implemented per Ardalis's exact mechanics (two-overload Null, no return-value NotNull).
</success_criteria>

## Artifacts This Phase Produces

- `GuardAgainstNullExtensions` static class — `Null<T>` (2 overloads), `NullOrEmpty` (2 overloads: `string`, `IEnumerable<T>`), `NullOrWhiteSpace`
- `GuardAgainstNullTests` — xUnit v3 test class covering pass/throw paths for the full Null guard family

<output>
Create `.planning/phases/01-domain-shared-guardclauses/01-03-SUMMARY.md` when done
</output>

---
phase: 01-domain-shared-guardclauses
plan: 5
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/GuardAgainstNumericExtensions.cs
  - SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/Guards/GuardAgainstNumericTests.cs
autonomous: true
requirements: [PRIM-01]
tags: [guard-clauses, dotnet, domain-shared, tdd]

must_haves:
  truths:
    - "Guard.Against.Negative/NegativeOrZero/Zero correctly reject invalid numeric signs per D-08, with correct zero-boundary handling for each of the three"
    - "Guard.Against.Default rejects an uninitialized/default-value struct (e.g. an unset Guid or DateTime) generically, not per-type"
    - "D-02: every guard method in this family captures its parameter name via CallerArgumentExpression, not an explicit nameof(x) argument; D-03: every guard method returns the validated input value unchanged on success, enabling inline assignment"
  artifacts:
    - path: "SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/GuardAgainstNumericExtensions.cs"
      provides: "Negative<T>, NegativeOrZero<T>, Zero<T>, Default<T>"
    - path: "SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/Guards/GuardAgainstNumericTests.cs"
      provides: "Pass/throw unit test coverage for the Numeric guard family"
  key_links:
    - from: "SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/Guards/GuardAgainstNumericTests.cs"
      to: "SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/GuardAgainstNumericExtensions.cs"
      via: "test calls Guard.Against.Negative / NegativeOrZero / Zero / Default"
      pattern: "Guard\\.Against\\.(Negative|NegativeOrZero|Zero|Default)"
---

<objective>
Implement the numeric-sign and default-value guard family confirmed in CONTEXT.md decision D-08 as a broader-than-minimum floor (D-07): `Negative`, `NegativeOrZero`, `Zero` (numeric sign guards), and `Default<T>` (guards against an uninitialized/default-value struct such as an unset `Guid` or `DateTime`).

Purpose: Give future `Entity`/`EntityAssociation` constructors a consistent way to guard quantity/identity-shaped struct arguments (a negative quantity, an unset identity Guid) without each module reinventing the check.
Output: `GuardAgainstNumericExtensions.cs` compiling with all four guards; a fully green `GuardAgainstNumericTests.cs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-domain-shared-guardclauses/01-CONTEXT.md
@.planning/phases/01-domain-shared-guardclauses/01-RESEARCH.md
@.planning/phases/01-domain-shared-guardclauses/01-PATTERNS.md
@.planning/phases/01-domain-shared-guardclauses/01-VALIDATION.md
</context>

<interfaces>
<!-- From 01-02-PLAN.md (Wave 1) — the foundation this plan's extension methods attach to. -->
Namespace SentinelSuite.Framework.Domain.Shared.Guards:
- `public interface IGuardClause` — zero-member extensibility marker.
- `public sealed class Guard : IGuardClause` — exposes `public static IGuardClause Against { get; }`.
Every method in this plan is an extension method: `this IGuardClause guardClause` as the first parameter, invoked as `Guard.Against.MethodName(...)`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Negative, NegativeOrZero, Zero</name>
  <files>SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/GuardAgainstNumericExtensions.cs, SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/Guards/GuardAgainstNumericTests.cs</files>
  <read_first>
    - .planning/phases/01-domain-shared-guardclauses/01-CONTEXT.md, decision D-08 (confirmed numeric-sign guards)
    - .planning/phases/01-domain-shared-guardclauses/01-PATTERNS.md, section "Guards/GuardAgainstNumericExtensions.cs (Negative, NegativeOrZero, Zero, Default&lt;T&gt;)"
    - SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/IGuardClause.cs (from Plan 01-02)
  </read_first>
  <behavior>
    - Test: Guard.Against.Negative(5) returns 5.
    - Test: Guard.Against.Negative(-1) throws ArgumentException.
    - Test: Guard.Against.Negative(0) returns 0 (zero is not negative).
    - Test: Guard.Against.NegativeOrZero(5) returns 5.
    - Test: Guard.Against.NegativeOrZero(0) throws ArgumentException.
    - Test: Guard.Against.NegativeOrZero(-1) throws ArgumentException.
    - Test: Guard.Against.Zero(5) returns 5.
    - Test: Guard.Against.Zero(0) throws ArgumentException.
  </behavior>
  <action>
    In namespace SentinelSuite.Framework.Domain.Shared.Guards, create static class GuardAgainstNumericExtensions with three methods, each `this IGuardClause guardClause, T input, parameterName via CallerArgumentExpression targeting input`, constrained where T : struct, IComparable&lt;T&gt;: Negative&lt;T&gt; throws a new ArgumentException with a fixed message referencing only parameterName when input compares less than default(T); NegativeOrZero&lt;T&gt; throws the analogous message when input compares less than or equal to default(T); Zero&lt;T&gt; throws the analogous message when input compares equal to default(T). All three return input when the check passes. Exception messages must reference only parameterName, never the numeric input value itself. Create GuardAgainstNumericTests.cs under the test project's Guards/ folder implementing all 8 behavior-block cases as xUnit v3 [Fact] methods.
  </action>
  <acceptance_criteria>
    - GuardAgainstNumericExtensions.cs declares Negative&lt;T&gt;, NegativeOrZero&lt;T&gt;, Zero&lt;T&gt;, all constrained where T : struct, IComparable&lt;T&gt;
    - Each compares input against default(T) via CompareTo, not a hardcoded numeric type
    - All 8 behavior-block test cases pass
  </acceptance_criteria>
  <verify>
    <automated>cd SentinelSuite &amp;&amp; dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter GuardAgainstNumericTests</automated>
  </verify>
  <done>Negative/NegativeOrZero/Zero implemented, boundary semantics (zero handling) correct per D-08 for each of the three guards.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Default&lt;T&gt;</name>
  <files>SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/GuardAgainstNumericExtensions.cs, SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/Guards/GuardAgainstNumericTests.cs</files>
  <read_first>
    - .planning/phases/01-domain-shared-guardclauses/01-CONTEXT.md, decision D-08 ("Default&lt;T&gt; guards against an uninitialized/default-value struct, e.g. an unset Guid or DateTime")
    - .planning/phases/01-domain-shared-guardclauses/01-PATTERNS.md, "Guards/GuardAgainstNumericExtensions.cs — Default&lt;T&gt; shape"
    - SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/GuardAgainstNumericExtensions.cs (file from Task 1 — add to it)
  </read_first>
  <behavior>
    - Test: Guard.Against.Default(a freshly generated non-empty Guid) returns the same Guid unchanged.
    - Test: Guard.Against.Default(Guid.Empty) throws ArgumentException.
    - Test: Guard.Against.Default(DateTime.Now) returns the same non-default DateTime unchanged.
    - Test: Guard.Against.Default(default(DateTime)) throws ArgumentException.
  </behavior>
  <action>
    Add Default&lt;T&gt;(this IGuardClause guardClause, T input, parameterName via CallerArgumentExpression targeting input) to GuardAgainstNumericExtensions, constrained where T : struct, IEquatable&lt;T&gt;. Throw a new ArgumentException with a fixed message referencing only parameterName when input equals default(T); otherwise return input. This guard is deliberately generic over any IEquatable&lt;T&gt; struct (Guid, DateTime, int, etc.), not hand-written per concrete type. Add the 4 behavior-block test cases above to GuardAgainstNumericTests.cs, exercising both a Guid fixture and a DateTime fixture to prove the generic constraint is genuinely type-agnostic.
  </action>
  <acceptance_criteria>
    - GuardAgainstNumericExtensions.cs now declares Default&lt;T&gt; constrained where T : struct, IEquatable&lt;T&gt;
    - Works correctly against both Guid and DateTime fixture values
    - All 4 behavior-block test cases pass
  </acceptance_criteria>
  <verify>
    <automated>cd SentinelSuite &amp;&amp; dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter GuardAgainstNumericTests</automated>
  </verify>
  <done>Default&lt;T&gt; implemented and green against multiple struct types.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Any future caller (Entity identity Guids, quantity/count fields) → Guard.Against.Negative/NegativeOrZero/Zero/Default | Guards struct-shaped identity and quantity invariants before they reach a downstream constructor |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|------------|----------|--------------|------------------|
| T-1-01 | Tampering | GuardAgainstNumericExtensions.cs (Negative, NegativeOrZero, Zero, Default&lt;T&gt;) | high | mitigate | Rejects invalid numeric signs and uninitialized-struct sentinels (e.g., an unset Guid identity, a zero/negative quantity) before they reach a downstream Entity/EntityAssociation constructor in a later phase — directly protects the identity/quantity invariants those future types will depend on. |
| T-1-02 | Information Disclosure | Exception messages across all four numeric guards | high | mitigate | All four messages interpolate only parameterName, never the actual numeric/struct input value, verified per acceptance criteria. |

</threat_model>

<verification>
Run `cd SentinelSuite && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter GuardAgainstNumericTests` — expect every test to pass with zero failures.
</verification>

<success_criteria>
- D-08's confirmed numeric-sign and default-value guard floor is fully implemented and green.
- Default&lt;T&gt; proven type-agnostic across at least two distinct struct types.
</success_criteria>

## Artifacts This Phase Produces

- `GuardAgainstNumericExtensions` static class — `Negative<T>`, `NegativeOrZero<T>`, `Zero<T>`, `Default<T>`
- `GuardAgainstNumericTests` — xUnit v3 test class covering pass/throw paths for the Numeric guard family

<output>
Create `.planning/phases/01-domain-shared-guardclauses/01-05-SUMMARY.md` when done
</output>

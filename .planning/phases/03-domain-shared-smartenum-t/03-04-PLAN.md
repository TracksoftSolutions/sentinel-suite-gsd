---
phase: 03-domain-shared-smartenum-t
plan: 4
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumComparableTests.cs
  - SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumOperatorTests.cs
  - SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumGenericValueTests.cs
autonomous: true
requirements: [PRIM-03]
tags: [smartenum, dotnet, domain-shared, tests]

must_haves:
  truths:
    - "Derived SmartEnum instances sort correctly via IComparable<T>, for both the int-backed and the generic-value (string-backed) forms (D-07, D-08 amended)"
    - "==, !=, <, <=, >, >= operators behave consistently with Equals/CompareTo, including null-safe comparisons (D-10 amended, full six-operator set)"
    - "A string-backed SmartEnum<TEnum,string> fixture exercises discovery, lookup, and comparison end-to-end (D-01's second value-type form)"
  artifacts:
    - path: "SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumComparableTests.cs"
      provides: "IComparable<T> sort-order test coverage for both value-type forms"
    - path: "SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumOperatorTests.cs"
      provides: "six-operator (==,!=,<,<=,>,>=) test coverage"
    - path: "SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumGenericValueTests.cs"
      provides: "string-backed SmartEnum<TEnum,string> end-to-end test coverage"
  key_links:
    - from: "SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumGenericValueTests.cs"
      to: "SentinelSuite/SentinelSuite.Framework.Domain.Shared/SmartEnum/SmartEnum{TEnum,TValue}.cs"
      via: "string-backed fixture declares SmartEnum<TEnum,string> directly (not the int-backed SmartEnum<TEnum> wrapper), exercising the generic-value form from Plan 03-01"
      pattern: "SmartEnum<\\w+, ?string>"
    - from: "SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumComparableTests.cs"
      to: "SentinelSuite/SentinelSuite.Framework.Domain.Shared/SmartEnum/SmartEnum{TEnum,TValue}.cs"
      via: "exercises CompareTo from Plan 03-01 for both int-backed and string-backed forms"
      pattern: "CompareTo"
---

<objective>
Verify SmartEnum's comparison, equality-operator, and generic-value-form surface from Plan 03-01: `IComparable<T>` sort ordering for both value-type forms (D-07, D-08 amended), the full six-operator set (D-10 amended), and an end-to-end string-backed `SmartEnum<TEnum,string>` fixture (D-01's second form).

Purpose: Prove derived enums sort correctly and support natural comparison-operator syntax out of the box for both the int-backed and generic-value-backed shapes, and that the string-backed form works end-to-end (discovery, lookup, comparison) exactly like the int-backed form.
Output: Three new xUnit v3 test files under `SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/`, all green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-domain-shared-smartenum-t/03-CONTEXT.md
@.planning/phases/03-domain-shared-smartenum-t/03-RESEARCH.md
@.planning/phases/03-domain-shared-smartenum-t/03-PATTERNS.md
@.planning/phases/03-domain-shared-smartenum-t/03-VALIDATION.md
</context>

<interfaces>
<!-- From 03-01-PLAN.md (Wave 1) — this plan's tests exercise this surface; do not modify it. -->
Namespace SentinelSuite.Framework.Domain.Shared.SmartEnum:
- `public abstract class SmartEnum&lt;TEnum, TValue&gt; : IComparable&lt;SmartEnum&lt;TEnum, TValue&gt;&gt;` — `CompareTo` delegates to `Value.CompareTo(other.Value)`; operators `==`, `!=` (null-safe, delegate to `Equals`), `&lt;`, `&lt;=`, `&gt;`, `&gt;=` (delegate to `CompareTo`); static `List`, `FromValue`, `FromName`.
- `public abstract class SmartEnum&lt;TEnum&gt; : SmartEnum&lt;TEnum, int&gt;` — int-backed convenience form.
- A string-backed derived type declares directly against the two-parameter base: `internal sealed class MyCode : SmartEnum&lt;MyCode, string&gt;` with `private MyCode(string value, string name) : base(name, value)`.
An int-backed fixture is declared as: `internal sealed class MyFixture : SmartEnum&lt;MyFixture&gt;` with `public static readonly` instances and `private MyFixture(int value, string name) : base(name, value)`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: SmartEnumComparableTests + SmartEnumOperatorTests</name>
  <files>SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumComparableTests.cs, SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumOperatorTests.cs</files>
  <read_first>
    - .planning/phases/03-domain-shared-smartenum-t/03-CONTEXT.md — decisions D-07, D-08 (amended), D-10 (amended)
    - .planning/phases/03-domain-shared-smartenum-t/03-RESEARCH.md — Pattern 4 (IComparable/equality/operators, lines 310-337), Open Question 2 (full six-operator set rationale)
    - .planning/phases/03-domain-shared-smartenum-t/03-PATTERNS.md — "Test files" section (naming convention)
    - SentinelSuite/SentinelSuite.Framework.Domain.Shared/SmartEnum/SmartEnum{TEnum,TValue}.cs (from Plan 03-01 — exact `CompareTo`/operator implementations)
  </read_first>
  <action>
    Create SmartEnumComparableTests.cs in namespace SentinelSuite.Framework.Domain.Shared.Tests.SmartEnum. Declare an internal sealed fixture ComparableFixtureStatus : SmartEnum&lt;ComparableFixtureStatus&gt; (int-backed) with three static instances declared out of value order — Medium = new(2, nameof(Medium)), Low = new(1, nameof(Low)), High = new(3, nameof(High)) — and a private constructor forwarding (value, name) to base. Also declare an internal sealed fixture ComparableFixtureCode : SmartEnum&lt;ComparableFixtureCode, string&gt; (string-backed) with two static instances Beta = new("beta", nameof(Beta)), Alpha = new("alpha", nameof(Alpha)), and a private constructor forwarding (value, name) to base(name, value). Write [Fact] tests asserting: (1) sorting a List&lt;ComparableFixtureStatus&gt; built as { High, Low, Medium } via .OrderBy(x =&gt; x) (uses IComparable&lt;T&gt;) produces the ascending-Value sequence [Low, Medium, High]; (2) sorting a List&lt;ComparableFixtureCode&gt; built as { Beta, Alpha } via .OrderBy(x =&gt; x) produces the ascending-string sequence [Alpha, Beta] (D-08 amended — string-backed form is comparable too); (3) Low.CompareTo(Low) returns 0 (self-comparison); (4) Low.CompareTo(High) returns a negative number; (5) High.CompareTo(Low) returns a positive number.

    Then create SmartEnumOperatorTests.cs with fixture OperatorFixtureStatus : SmartEnum&lt;OperatorFixtureStatus&gt; declaring Low = new(1, nameof(Low)) and High = new(2, nameof(High)). Write [Fact] tests asserting: Low == Low is true (same static-field reference); Low == High is false and Low != High is true; a both-null comparison — ((SmartEnum&lt;OperatorFixtureStatus, int&gt;?)null) == ((SmartEnum&lt;OperatorFixtureStatus, int&gt;?)null) — is true; Low &lt; High is true and High &lt; Low is false; Low &lt;= Low is true and Low &lt;= High is true and High &lt;= Low is false; High &gt; Low is true and Low &gt; High is false; High &gt;= High is true and High &gt;= Low is true and Low &gt;= High is false.
  </action>
  <acceptance_criteria>
    - Sorting a shuffled `List&lt;ComparableFixtureStatus&gt;` via `OrderBy(x => x)` produces ascending `Value` order (int-backed form, D-07)
    - Sorting a shuffled `List&lt;ComparableFixtureCode&gt;` via `OrderBy(x => x)` produces ascending string order (generic-value form, D-08 amended)
    - `CompareTo` returns `0` for self-comparison, negative for a lesser `Value`, positive for a greater `Value`
    - All six operators (`==`, `!=`, `<`, `<=`, `>`, `>=`) on `OperatorFixtureStatus` behave consistently with `Equals`/`CompareTo`, including a both-null `==` comparison returning `true`
    - `cd SentinelSuite && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter SmartEnumComparableTests` passes with zero failures
    - `cd SentinelSuite && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter SmartEnumOperatorTests` passes with zero failures
  </acceptance_criteria>
  <verify>
    <automated>cd SentinelSuite && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter SmartEnumComparableTests && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter SmartEnumOperatorTests</automated>
  </verify>
  <done>SmartEnumComparableTests.cs and SmartEnumOperatorTests.cs exist and pass, confirming D-07/D-08 (IComparable for both value-type forms) and D-10 amended (full six-operator set).</done>
</task>

<task type="auto">
  <name>Task 2: SmartEnumGenericValueTests (string-backed form, end-to-end)</name>
  <files>SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumGenericValueTests.cs</files>
  <read_first>
    - .planning/phases/03-domain-shared-smartenum-t/03-CONTEXT.md — decision D-01 (both value-type forms)
    - .planning/phases/03-domain-shared-smartenum-t/03-RESEARCH.md — "Fixture-style derived enum, both value-type forms (D-01)" code example (lines 415-438), the `TenantIsolationTier` string-backed example specifically
    - .planning/phases/03-domain-shared-smartenum-t/03-PATTERNS.md — "Fixture requirement" note on needing both int-backed and string-backed fixtures across the test suite
    - SentinelSuite/SentinelSuite.Framework.Domain.Shared/SmartEnum/SmartEnum{TEnum,TValue}.cs (from Plan 03-01 — confirms the generic-value form shares the identical member surface as the int-backed form)
  </read_first>
  <action>
    Create SmartEnumGenericValueTests.cs in namespace SentinelSuite.Framework.Domain.Shared.Tests.SmartEnum. Declare an internal sealed fixture GenericValueFixtureCode : SmartEnum&lt;GenericValueFixtureCode, string&gt; with two static instances Shared = new("shared", nameof(Shared)) and DedicatedDb = new("dedicated_db", nameof(DedicatedDb)) — mirroring RESEARCH.md's TenantIsolationTier example — and a private constructor GenericValueFixtureCode(string value, string name) : base(name, value). Write [Fact] tests asserting: (1) GenericValueFixtureCode.List.Count equals 2 and List contains both Shared and DedicatedDb (discovery works for the generic-value form); (2) GenericValueFixtureCode.FromValue("shared") returns Shared; (3) GenericValueFixtureCode.FromName("DedicatedDb") returns DedicatedDb; (4) GenericValueFixtureCode.TryFromValue("missing", out var result) returns false with result null; (5) sorting a List&lt;GenericValueFixtureCode&gt; built as { Shared, DedicatedDb } via .OrderBy(x =&gt; x) orders DedicatedDb ("dedicated_db") before Shared ("shared") — alphabetical string comparison via CompareTo.
  </action>
  <acceptance_criteria>
    - `GenericValueFixtureCode.List.Count` equals `2` and `List` contains both declared instances
    - `FromValue("shared")` returns `Shared`; `FromName("DedicatedDb")` returns `DedicatedDb`
    - `TryFromValue("missing", out var result)` returns `false` with `result` `null`
    - Sorting `[Shared, DedicatedDb]` via `OrderBy(x => x)` orders `DedicatedDb` before `Shared` (alphabetical string comparison)
    - `cd SentinelSuite && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter SmartEnumGenericValueTests` passes with zero failures
  </acceptance_criteria>
  <verify>
    <automated>cd SentinelSuite && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter SmartEnumGenericValueTests</automated>
  </verify>
  <done>SmartEnumGenericValueTests.cs exists and passes, confirming D-01's string-backed generic-value form works end-to-end (discovery, lookup, comparison) exactly like the int-backed form.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new — this plan adds only in-process test code (comparison, sorting, and a second value-type form) exercising Plan 03-01's already-mitigated surface | No new trust boundary is introduced; no lookup-miss/exception path is newly exercised here (that is Plan 03-03's concern) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-3-01 | Information Disclosure | `SmartEnumNotFoundException` (Plan 03-01) | low | accept | Inherited from Plan 03-01; this plan's `TryFromValue("missing", ...)` assertion in `SmartEnumGenericValueTests.cs` uses the non-throwing path, so no exception message/`AttemptedLookup` disclosure is exercised here. |
| T-3-02 | Denial of Service (theoretical) | `GetAllOptions()` reflection scan (Plan 03-01) | low | accept | Inherited from Plan 03-01; this plan additionally confirms the reflection scan and per-closed-generic-type `Lazy<T>` caching work correctly for the generic-value (`TValue = string`) closed generic instantiation, not just the int-backed one — no new threat, broader confirmation of the existing mitigation's correctness. |

</threat_model>

<verification>
Run each new test class individually:
`cd SentinelSuite && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter SmartEnumComparableTests`
`cd SentinelSuite && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter SmartEnumOperatorTests`
`cd SentinelSuite && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter SmartEnumGenericValueTests`

All three must pass with zero failures. Once all Wave 2 plans (03-02, 03-03, 03-04) have landed, run `cd SentinelSuite && dotnet test` from the solution root — expect zero failures across the full suite, confirming ROADMAP.md's Phase 3 success criteria 1-4 are all satisfied together.
</verification>

<success_criteria>
- `IComparable<T>` sort ordering (D-07, D-08 amended) and the full six-operator set (D-10 amended) are covered by passing, automated xUnit v3 tests for both value-type forms.
- The string-backed `SmartEnum<TEnum,string>` generic-value form (D-01) is proven end-to-end: discovery, lookup, and comparison.
</success_criteria>

## Artifacts This Phase Produces

- `SmartEnumComparableTests` — xUnit v3 test class; fixtures `ComparableFixtureStatus`, `ComparableFixtureCode`
- `SmartEnumOperatorTests` — xUnit v3 test class; fixture `OperatorFixtureStatus`
- `SmartEnumGenericValueTests` — xUnit v3 test class; fixture `GenericValueFixtureCode`

<output>
Create `.planning/phases/03-domain-shared-smartenum-t/03-04-SUMMARY.md` when done
</output>

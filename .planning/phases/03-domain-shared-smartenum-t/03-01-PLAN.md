---
phase: 03-domain-shared-smartenum-t
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - SentinelSuite/SentinelSuite.Framework.Domain.Shared/SmartEnum/ISmartEnum.cs
  - SentinelSuite/SentinelSuite.Framework.Domain.Shared/SmartEnum/SmartEnumNotFoundException.cs
  - SentinelSuite/SentinelSuite.Framework.Domain.Shared/SmartEnum/SmartEnum{TEnum,TValue}.cs
  - SentinelSuite/SentinelSuite.Framework.Domain.Shared/SmartEnum/SmartEnum{TEnum}.cs
autonomous: true
requirements: [PRIM-03]
tags: [smartenum, dotnet, domain-shared]

must_haves:
  truths:
    - "SmartEnum<TEnum> and SmartEnum<TEnum,TValue> compile in Domain.Shared with zero third-party NuGet package references (ROADMAP Phase 3 Success Criterion 1)"
    - "SmartEnumNotFoundException is a single, dedicated exception type (D-05/D-06) carrying the attempted lookup value/name and target type for both FromValue-miss and FromName-miss"
    - "Derived SmartEnum types are comparable via IComparable<T> and support the full six comparison/equality operators (D-07, D-10 amended)"
    - "Every SmartEnum call site can write MyStatus.FromValue(1) / MyStatus.FromName(\"Active\") (throwing), MyStatus.TryFromValue(1, out var status) (non-throwing), MyStatus.FromValue(1, MyStatus.Default) (default-on-miss, D-14), and MyStatus.List to enumerate every defined value"
  artifacts:
    - path: "SentinelSuite/SentinelSuite.Framework.Domain.Shared/SmartEnum/ISmartEnum.cs"
      provides: "zero-member marker interface ISmartEnum"
    - path: "SentinelSuite/SentinelSuite.Framework.Domain.Shared/SmartEnum/SmartEnumNotFoundException.cs"
      provides: "sealed SmartEnumNotFoundException with TargetType/AttemptedLookup properties"
    - path: "SentinelSuite/SentinelSuite.Framework.Domain.Shared/SmartEnum/SmartEnum{TEnum,TValue}.cs"
      provides: "core generic base: discovery/caching, lookup API, equality, comparison, ToString, operators"
    - path: "SentinelSuite/SentinelSuite.Framework.Domain.Shared/SmartEnum/SmartEnum{TEnum}.cs"
      provides: "int-backed convenience wrapper SmartEnum<TEnum> : SmartEnum<TEnum,int>"
  key_links:
    - from: "SentinelSuite/SentinelSuite.Framework.Domain.Shared/SmartEnum/SmartEnum{TEnum}.cs"
      to: "SentinelSuite/SentinelSuite.Framework.Domain.Shared/SmartEnum/SmartEnum{TEnum,TValue}.cs"
      via: "SmartEnum<TEnum> inherits SmartEnum<TEnum,int> — TValue=int specialization"
      pattern: "SmartEnum<TEnum, int>"
    - from: "SentinelSuite/SentinelSuite.Framework.Domain.Shared/SmartEnum/SmartEnum{TEnum,TValue}.cs"
      to: "SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/GuardAgainstNullExtensions.cs"
      via: "FromName/constructor reuse Guard.Against.NullOrWhiteSpace from Phase 1"
      pattern: "Guard\\.Against\\.NullOrWhiteSpace"
    - from: "SentinelSuite/SentinelSuite.Framework.Domain.Shared/SmartEnum/SmartEnum{TEnum,TValue}.cs"
      to: "SentinelSuite/SentinelSuite.Framework.Domain.Shared/SmartEnum/SmartEnumNotFoundException.cs"
      via: "FromValue/FromName throw SmartEnumNotFoundException on a miss"
      pattern: "throw new SmartEnumNotFoundException"
---

<objective>
Build the hand-rolled `SmartEnum<TEnum>` / `SmartEnum<TEnum,TValue>` type-safe enumeration base (Ardalis.SmartEnum-equivalent, zero NuGet dependency) in `Domain.Shared` — the complete production surface for PRIM-03: reflection-based lazy-cached instance discovery, the full lookup API (throwing, Try-variants, and default-on-miss per D-14), comparison/equality/ToString, and the six-operator comparison set (D-10 amended).

Purpose: Give the kernel and every future module a dependency-free type-safe enumeration base, matching Ardalis.SmartEnum's proven shape exactly (per RESEARCH.md's verbatim-sourced reference) without ever taking the package itself as a dependency.
Output: `ISmartEnum.cs`, `SmartEnumNotFoundException.cs`, `SmartEnum{TEnum,TValue}.cs`, `SmartEnum{TEnum}.cs` — all compiling cleanly in `SentinelSuite.Framework.Domain.Shared` with zero third-party `PackageReference` entries.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-domain-shared-smartenum-t/03-CONTEXT.md
@.planning/phases/03-domain-shared-smartenum-t/03-RESEARCH.md
@.planning/phases/03-domain-shared-smartenum-t/03-PATTERNS.md
@.planning/phases/03-domain-shared-smartenum-t/03-VALIDATION.md
</context>

<interfaces>
<!-- Pre-existing, from Phase 1 (Guards) — reused directly by Task 2's FromName/TryFromName and by the class constructor. -->
Namespace SentinelSuite.Framework.Domain.Shared.Guards:
- `Guard.Against.NullOrWhiteSpace(string? input, [CallerArgumentExpression] string? parameterName = null)` — throws `ArgumentNullException`/`ArgumentException` on null/whitespace input, otherwise returns the validated string unchanged.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ISmartEnum, SmartEnumNotFoundException, and SmartEnum&lt;TEnum,TValue&gt;'s structural backbone</name>
  <files>SentinelSuite/SentinelSuite.Framework.Domain.Shared/SmartEnum/ISmartEnum.cs, SentinelSuite/SentinelSuite.Framework.Domain.Shared/SmartEnum/SmartEnumNotFoundException.cs, SentinelSuite/SentinelSuite.Framework.Domain.Shared/SmartEnum/SmartEnum{TEnum,TValue}.cs</files>
  <read_first>
    - .planning/phases/03-domain-shared-smartenum-t/03-CONTEXT.md — decisions D-01, D-02, D-03, D-05, D-06, D-07, D-08 (amended), D-09, D-11, D-12, D-13
    - .planning/phases/03-domain-shared-smartenum-t/03-RESEARCH.md — Pattern 2 (per-closed-generic-type Lazy&lt;T&gt; caching, lines 214-273), Pattern 4 (IComparable/equality/operators/ToString, lines 310-337), Pitfall 2 (ConcurrentDictionary anti-pattern), Pitfall 3 (Lazy&lt;T&gt; exception-caching semantics), Pitfall 4 (CRTP self-referencing-generic limitation)
    - .planning/phases/03-domain-shared-smartenum-t/03-PATTERNS.md — sections for `ISmartEnum.cs` and `SmartEnum{TEnum,TValue}.cs`
    - SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/Guard.cs (sealed static-entry-point + inline-remarks-documented-convention precedent, D-06-style)
    - SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/IGuardClause.cs (zero-member marker interface precedent, exact XML-doc shape to mirror)
    - SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/GuardAgainstNullExtensions.cs (`Guard.Against.NullOrWhiteSpace` signature, reused in the constructor)
  </read_first>
  <action>
    In namespace SentinelSuite.Framework.Domain.Shared.SmartEnum, create three files.

    (1) ISmartEnum.cs: a zero-member public interface ISmartEnum, XML-doc'd as a non-generic marker letting code detect "is this a SmartEnum" without knowing TEnum/TValue — mirror IGuardClause.cs's &lt;remarks&gt; block distinguishing this from the architecture-guidance capability-marker-interface smell (this is a framework-internal type-detection anchor, not a tenant-defined capability marker).

    (2) SmartEnumNotFoundException.cs: a sealed public class SmartEnumNotFoundException : Exception with four constructors — parameterless; (string message); (string message, Exception innerException); and (string message, Type targetType, object? attemptedLookup) — the fourth sets two get-only properties, Type? TargetType and object? AttemptedLookup, populated internally by FromValue/FromName misses (added in Task 2). This diverges from the Ardalis reference's non-sealed modifier per this project's sealed-by-default kernel-type convention (Guard, and Phase 2's Result/Result&lt;T&gt; precedent). Add an XML-doc &lt;remarks&gt; block flagging: a future API/Application-layer phase must not surface .Message or .AttemptedLookup directly to an external HTTP response without review — this is T-3-01 (Information Disclosure), accepted-for-now at this Domain.Shared-only phase, mirroring Phase 2's identical flag on Result.CriticalError's Exception property.

    (3) SmartEnum{TEnum,TValue}.cs: public abstract class SmartEnum&lt;TEnum, TValue&gt; implementing IComparable&lt;SmartEnum&lt;TEnum, TValue&gt;&gt;, IEquatable&lt;SmartEnum&lt;TEnum, TValue&gt;&gt;, ISmartEnum, with generic constraints "where TEnum : SmartEnum&lt;TEnum, TValue&gt;" and "where TValue : IEquatable&lt;TValue&gt;, IComparable&lt;TValue&gt;" (D-08 as amended in CONTEXT.md — broadened from the original literal wording to match the Ardalis reference exactly, per RESEARCH.md Pitfall 1/Open Question 1; this amendment is already locked, not open).

    Add: protected constructor SmartEnum(string name, TValue value) that sets Name via Guard.Against.NullOrWhiteSpace(name) and Value directly; public string Name { get; } and public TValue Value { get; }.

    Add four private static fields exactly matching RESEARCH.md Pattern 2: static readonly Lazy&lt;List&lt;TEnum&gt;&gt; _enumOptions = new(GetAllOptions, LazyThreadSafetyMode.ExecutionAndPublication); static readonly Lazy&lt;Dictionary&lt;string, TEnum&gt;&gt; _fromName built via _enumOptions.Value.ToDictionary(item =&gt; item.Name); static readonly Lazy&lt;Dictionary&lt;string, TEnum&gt;&gt; _fromNameIgnoreCase built the same way with StringComparer.OrdinalIgnoreCase; static readonly Lazy&lt;Dictionary&lt;TValue, TEnum&gt;&gt; _fromValue built via a first-wins-on-duplicate-Value loop (skip a null Value, skip if the dictionary already contains the key) exactly as RESEARCH.md Pattern 2 documents.

    Add private static List&lt;TEnum&gt; GetAllOptions() using Assembly.GetAssembly(typeof(TEnum))!.GetTypes().Where(t =&gt; typeof(TEnum).IsAssignableFrom(t)).SelectMany(t =&gt; GetFieldsOfType&lt;TEnum&gt;(t)).OrderBy(t =&gt; t.Name).ToList() — and a private static List&lt;TFieldType&gt; GetFieldsOfType&lt;TFieldType&gt;(Type type) helper declared as a private static method on this same class (not a separate TypeExtensions.cs file — this keeps the file set matching RESEARCH.md's Recommended Project Structure tree exactly, which lists only ISmartEnum.cs, SmartEnum{TEnum}.cs, SmartEnum{TEnum,TValue}.cs, and SmartEnumNotFoundException.cs), using type.GetFields(BindingFlags.Public | BindingFlags.Static | BindingFlags.FlattenHierarchy).Where(p =&gt; type.IsAssignableFrom(p.FieldType)).Select(pi =&gt; (TFieldType)pi.GetValue(null)!).ToList().

    Add public static IReadOnlyCollection&lt;TEnum&gt; List =&gt; _enumOptions.Value.

    Add equality/comparison/representation members exactly matching RESEARCH.md Pattern 4: public override string ToString() =&gt; Name; public override int GetHashCode() =&gt; Value.GetHashCode(); public override bool Equals(object? obj); public virtual bool Equals(SmartEnum&lt;TEnum, TValue&gt;? other) (ReferenceEquals-first, then Value.Equals(other.Value)); public virtual int CompareTo(SmartEnum&lt;TEnum, TValue&gt;? other) =&gt; Value.CompareTo(other!.Value); and the full six-operator set per CONTEXT.md D-10 (amended): ==, !=, &lt;, &lt;=, &gt;, &gt;=. == and != delegate to Equals with the reference's null-safe shape ("left is null ? right is null : left.Equals(right)"); &lt;, &lt;=, &gt;, &gt;= each delegate to CompareTo (e.g. left is null ? right is not null : left.CompareTo(right) &lt; 0 for the &lt; operator).

    Add a class-level &lt;remarks&gt; XML-doc block documenting two conventions inline, matching Guard.cs's D-06 inline-documentation precedent: (a) D-13 — every concrete derived SmartEnum type is expected/documented to be sealed; this base class itself must remain abstract (not sealed) to be inheritable, but every leaf derived type should be; (b) RESEARCH.md Pitfall 4's CRTP caveat, verbatim intent — "always inherit as SmartEnum&lt;TSelf&gt; where TSelf is the declaring type itself; the C# compiler cannot fully enforce this via the generic constraint alone."

    Do NOT add FromName, TryFromName, FromValue, TryFromValue, or the FromValue-with-default overload in this task, and do NOT create SmartEnum{TEnum}.cs — both are Task 2.
  </action>
  <acceptance_criteria>
    - ISmartEnum.cs declares `public interface ISmartEnum` with zero members, namespace SentinelSuite.Framework.Domain.Shared.SmartEnum
    - SmartEnumNotFoundException.cs declares `public sealed class SmartEnumNotFoundException : Exception` with 4 constructors and `TargetType` (Type?) / `AttemptedLookup` (object?) get-only properties
    - SmartEnum{TEnum,TValue}.cs declares `public abstract class SmartEnum<TEnum, TValue>` with constraints `where TEnum : SmartEnum<TEnum, TValue>` and `where TValue : IEquatable<TValue>, IComparable<TValue>`
    - Exactly four `static readonly Lazy<` field declarations exist in the file (`_enumOptions`, `_fromName`, `_fromNameIgnoreCase`, `_fromValue`)
    - `GetAllOptions` and `GetFieldsOfType` are both private static methods declared inside SmartEnum{TEnum,TValue}.cs — no separate TypeExtensions.cs file is created
    - `List` static property, `Equals(object?)`, `Equals(SmartEnum<TEnum,TValue>?)`, `GetHashCode()`, `CompareTo`, `ToString()`, and all six operators (`==`,`!=`,`<`,`<=`,`>`,`>=`) are present
    - Class-level XML remarks document both the D-13 sealed-by-default convention and the Pitfall 4 CRTP caveat
    - `cd SentinelSuite && dotnet build SentinelSuite.Framework.Domain.Shared/SentinelSuite.Framework.Domain.Shared.csproj` succeeds with zero errors
  </acceptance_criteria>
  <verify>
    <automated>cd SentinelSuite && dotnet build SentinelSuite.Framework.Domain.Shared/SentinelSuite.Framework.Domain.Shared.csproj</automated>
  </verify>
  <done>ISmartEnum, SmartEnumNotFoundException, and SmartEnum&lt;TEnum,TValue&gt;'s structural backbone (discovery/caching, equality, comparison, ToString, all six operators) compile with zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Lookup API surface + SmartEnum&lt;TEnum&gt; int-backed wrapper</name>
  <files>SentinelSuite/SentinelSuite.Framework.Domain.Shared/SmartEnum/SmartEnum{TEnum,TValue}.cs, SentinelSuite/SentinelSuite.Framework.Domain.Shared/SmartEnum/SmartEnum{TEnum}.cs</files>
  <read_first>
    - .planning/phases/03-domain-shared-smartenum-t/03-CONTEXT.md — decisions D-04, D-05, D-06, D-14
    - .planning/phases/03-domain-shared-smartenum-t/03-RESEARCH.md — Pattern 1 (int-backed wrapper, lines 195-212), Pattern 3 (FromName/TryFromName/FromValue/TryFromValue signatures, lines 275-308), Open Question 3 / D-14's FromValue-with-default addition
    - .planning/phases/03-domain-shared-smartenum-t/03-PATTERNS.md — "Lookup surface (RESEARCH.md Pattern 3)" and "Cross-file reuse note" sections
    - SentinelSuite/SentinelSuite.Framework.Domain.Shared/SmartEnum/SmartEnum{TEnum,TValue}.cs (just created in Task 1 of this plan — the file this task extends)
    - SentinelSuite/SentinelSuite.Framework.Domain.Shared/Guards/GuardAgainstNullExtensions.cs (`Guard.Against.NullOrWhiteSpace` signature to call from FromName)
  </read_first>
  <action>
    Add to SmartEnum{TEnum,TValue}.cs (do not create a new file for these methods):

    public static TEnum FromName(string name, bool ignoreCase = false) — call Guard.Against.NullOrWhiteSpace(name) first, select _fromNameIgnoreCase.Value or _fromName.Value based on ignoreCase, and on a lookup miss throw new SmartEnumNotFoundException($"No {typeof(TEnum).Name} with Name \"{name}\" found.", typeof(TEnum), name) using Task 1's fourth constructor to populate TargetType/AttemptedLookup.

    public static bool TryFromName(string name, bool ignoreCase, out TEnum? result) — return false with result set to null (no throw, no Guard call) when name is null or empty via string.IsNullOrEmpty; otherwise look up via the same dictionaries as FromName without throwing on a miss.

    public static TEnum FromValue(TValue value) — look up via _fromValue.Value, throw new SmartEnumNotFoundException($"No {typeof(TEnum).Name} with Value {value} found.", typeof(TEnum), value) on a miss.

    public static bool TryFromValue(TValue value, out TEnum? result) — non-throwing equivalent via _fromValue.Value.TryGetValue.

    public static TEnum FromValue(TValue value, TEnum defaultValue) — three-argument overload per CONTEXT.md D-14: returns defaultValue instead of throwing when _fromValue.Value has no entry for value; returns the actual matching instance when value is found (defaultValue is ignored on a hit).

    Then create SmartEnum{TEnum}.cs: public abstract class SmartEnum&lt;TEnum&gt; : SmartEnum&lt;TEnum, int&gt; where TEnum : SmartEnum&lt;TEnum, int&gt;, with a single protected constructor SmartEnum(string name, int value) : base(name, value) — matching RESEARCH.md Pattern 1 verbatim; this wrapper class contains no other members.
  </action>
  <acceptance_criteria>
    - `FromName(string name, bool ignoreCase = false)` calls `Guard.Against.NullOrWhiteSpace` before dictionary lookup and throws `SmartEnumNotFoundException` (with `TargetType`/`AttemptedLookup` populated) on a miss
    - `TryFromName(string name, bool ignoreCase, out TEnum? result)` returns `false`/`null` for a null or empty `name` without throwing
    - `FromValue(TValue value)` throws `SmartEnumNotFoundException` (`TargetType`/`AttemptedLookup` populated) on a miss; `TryFromValue(TValue value, out TEnum? result)` returns `false`/`null` on a miss without throwing
    - A three-argument `FromValue(TValue value, TEnum defaultValue)` overload exists and returns `defaultValue` on a miss, and the actual matching instance on a hit (D-14)
    - SmartEnum{TEnum}.cs declares `public abstract class SmartEnum<TEnum> : SmartEnum<TEnum, int>` with exactly one `protected` constructor forwarding `(name, value)` to the base
    - `cd SentinelSuite && dotnet build SentinelSuite.Framework.Domain.Shared/SentinelSuite.Framework.Domain.Shared.csproj` succeeds with zero errors
    - `grep -c "PackageReference" SentinelSuite.Framework.Domain.Shared/SentinelSuite.Framework.Domain.Shared.csproj` returns `0` (zero third-party NuGet dependencies — ROADMAP Phase 3 Success Criterion 1)
  </acceptance_criteria>
  <verify>
    <automated>cd SentinelSuite && dotnet build SentinelSuite.Framework.Domain.Shared/SentinelSuite.Framework.Domain.Shared.csproj && test "$(grep -c PackageReference SentinelSuite.Framework.Domain.Shared/SentinelSuite.Framework.Domain.Shared.csproj)" -eq 0</automated>
  </verify>
  <done>Full lookup API surface (FromName/TryFromName/FromValue/TryFromValue/FromValue-with-default) and the int-backed SmartEnum&lt;TEnum&gt; wrapper compile with zero errors and zero third-party package references.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None — `Domain.Shared` is a pure in-memory library with no I/O, network, or persistence boundary this phase | The reflection scan reads only the hosting process's own loaded assembly metadata; no external input crosses a trust boundary here yet |
| Future call site (arbitrary caller-supplied lookup value/name) → `FromValue`/`FromName`/`TryFromValue`/`TryFromName` | The one place caller-supplied data flows into this phase's code — today the caller is always in-process kernel/test code, not an external actor |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-3-01 | Information Disclosure | `SmartEnumNotFoundException.Message` / `AttemptedLookup` property | low | accept | Raw attempted lookup value/name is interpolated into the exception message and exposed via the `AttemptedLookup` property (matches the Ardalis reference exactly). Acceptable at this Domain.Shared-only phase since no external-facing API boundary exists yet — mirrors Phase 2's identical acceptance of `Result.CriticalError`'s `Exception` property (T-2-01). Mitigation: XML-doc remarks on `SmartEnumNotFoundException` (Task 1) flag that a future API/Application-layer phase must not surface `.Message`/`.AttemptedLookup` directly to an external response without review. |
| T-3-02 | Denial of Service (theoretical) | `GetAllOptions()` assembly-wide reflection scan | low | accept | An assembly-wide type/field scan is a theoretical startup-latency concern only if many `SmartEnum`-derived types exist and are eagerly enumerated. D-03's lazy-per-first-access design (Task 1's `Lazy<T>` fields) already mitigates the worst case — cost is paid once per derived type on its first real use, not eagerly at process start. No host/bootstrapper exists yet in this milestone (Module System is explicitly scoped down per `PROJECT.md`), so no startup path triggers this eagerly. Flag for whichever future phase adds a host/bootstrapper. |
| T-3-SC | Tampering (supply-chain) | npm/pip/cargo/NuGet installs | n/a | accept | This phase installs zero packages (RESEARCH.md Package Legitimacy Audit confirms zero-package scope) — no install checkpoint required. |

</threat_model>

<verification>
Run `cd SentinelSuite && dotnet build SentinelSuite.Framework.Domain.Shared/SentinelSuite.Framework.Domain.Shared.csproj` — expect zero errors and zero warnings related to the new `SmartEnum/` files.

Confirm zero third-party `PackageReference` entries were added: `grep -c "PackageReference" SentinelSuite.Framework.Domain.Shared/SentinelSuite.Framework.Domain.Shared.csproj` returns `0`.

This plan has no automated test coverage of its own (tests land in 03-02/03-03/03-04, Wave 2) — its own verification is build-success plus the source-level acceptance criteria above. Once Wave 2 plans land, `cd SentinelSuite && dotnet test` should pass with zero failures across the full suite, confirming this plan's production surface is correct.
</verification>

<success_criteria>
- `ISmartEnum`, `SmartEnumNotFoundException`, `SmartEnum<TEnum,TValue>`, and `SmartEnum<TEnum>` all compile with zero errors and zero third-party `PackageReference` entries (ROADMAP Phase 3 Success Criterion 1).
- The full lookup API (`FromName`, `TryFromName`, `FromValue`, `TryFromValue`, `FromValue`-with-default) and comparison/equality/operator/`ToString` surface exist exactly as specified in D-01 through D-14.
- D-13's sealed-by-default convention and Pitfall 4's CRTP caveat are documented inline in the base class's XML remarks.
</success_criteria>

## Artifacts This Phase Produces

- `ISmartEnum` — zero-member marker interface
- `SmartEnumNotFoundException` — sealed exception class; constructors: parameterless, `(string)`, `(string, Exception)`, `(string, Type, object?)`; properties `TargetType` (Type?), `AttemptedLookup` (object?)
- `SmartEnum<TEnum, TValue>` — abstract generic base class; members: `Name` (string), `Value` (TValue), static `List`, static `FromName`, static `TryFromName`, static `FromValue` (two overloads), static `TryFromValue`, `Equals(object?)`, `Equals(SmartEnum<TEnum,TValue>?)`, `GetHashCode()`, `CompareTo`, `ToString()`, operators `==`, `!=`, `<`, `<=`, `>`, `>=`; private static `GetAllOptions()`, private static `GetFieldsOfType<TFieldType>(Type)`
- `SmartEnum<TEnum>` — abstract int-backed wrapper class; one `protected` constructor

<output>
Create `.planning/phases/03-domain-shared-smartenum-t/03-01-SUMMARY.md` when done
</output>
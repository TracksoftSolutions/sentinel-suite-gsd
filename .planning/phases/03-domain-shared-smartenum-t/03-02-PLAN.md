---
phase: 03-domain-shared-smartenum-t
plan: 2
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumEqualityTests.cs
  - SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumListTests.cs
  - SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumToStringTests.cs
  - SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumDiscoveryFailureTests.cs
autonomous: true
requirements: [PRIM-03]
tags: [smartenum, dotnet, domain-shared, tests]

must_haves:
  truths:
    - "Two SmartEnum<T>-derived fixture instances with the same underlying value are equal and share a hash code (ROADMAP Phase 3 Success Criterion 2)"
    - "Every derived SmartEnum type exposes a static List enumerating all its defined instances (D-09)"
    - "ToString() returns the instance's Name (D-11)"
    - "A badly-formed SmartEnum-derived fixture (duplicate Name across two static fields) throws a specific, catchable exception on first lookup access, consistently on repeated access, without corrupting lookups on an unrelated SmartEnum-derived type (RESEARCH.md Pitfall 3)"
  artifacts:
    - path: "SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumEqualityTests.cs"
      provides: "value-equality and hash-code test coverage"
    - path: "SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumListTests.cs"
      provides: "static List enumeration test coverage (D-09)"
    - path: "SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumToStringTests.cs"
      provides: "ToString override test coverage (D-11)"
    - path: "SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumDiscoveryFailureTests.cs"
      provides: "Lazy<T> exception-caching / no-cross-type-corruption test coverage (Pitfall 3)"
  key_links:
    - from: "SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumEqualityTests.cs"
      to: "SentinelSuite/SentinelSuite.Framework.Domain.Shared/SmartEnum/SmartEnum{TEnum,TValue}.cs"
      via: "test fixtures inherit SmartEnum<TEnum> and exercise Equals/GetHashCode from Plan 03-01"
      pattern: "SmartEnum<\\w+>"
    - from: "SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumDiscoveryFailureTests.cs"
      to: "SentinelSuite/SentinelSuite.Framework.Domain.Shared/SmartEnum/SmartEnumNotFoundException.cs"
      via: "duplicate-Name fixture's FromName call surfaces the underlying ToDictionary ArgumentException, not SmartEnumNotFoundException"
      pattern: "ArgumentException"
---

<objective>
Verify SmartEnum's structural backbone from Plan 03-01: value equality/hash-code semantics (Success Criterion 2), the static `List` enumeration surface (D-09), `ToString()` (D-11), and the `Lazy<T>` exception-caching/no-cross-type-corruption behavior on a badly-formed derived type (RESEARCH.md Pitfall 3).

Purpose: Prove the reflection-based discovery, equality, and representation surface built in Plan 03-01 behaves exactly as the Ardalis.SmartEnum reference does, including its less-obvious failure-mode guarantees.
Output: Four new xUnit v3 test files under `SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/`, all green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-domain-shared-smartenum-t/03-CONTEXT.md
@.planning/phases/03-domain-shared-smartenum-t/03-RESEARCH.md
@.planning/phases/03-domain-shared-smartenum-t/03-PATTERNS.md
@.planning/phases/03-domain-shared-smartenum-t/03-VALIDATION.md
</context>

<interfaces>
<!-- From 03-01-PLAN.md (Wave 1) — this plan's tests exercise this surface; do not modify it. -->
Namespace SentinelSuite.Framework.Domain.Shared.SmartEnum:
- `public interface ISmartEnum` — zero-member marker.
- `public abstract class SmartEnum&lt;TEnum, TValue&gt; : IComparable&lt;SmartEnum&lt;TEnum, TValue&gt;&gt;, IEquatable&lt;SmartEnum&lt;TEnum, TValue&gt;&gt;, ISmartEnum` — `Name` (string), `Value` (TValue), static `List` (`IReadOnlyCollection&lt;TEnum&gt;`), `Equals`/`GetHashCode`/`CompareTo`/`ToString` overrides, operators `==`,`!=`,`&lt;`,`&lt;=`,`&gt;`,`&gt;=`, static `FromName`/`TryFromName`/`FromValue`/`TryFromValue`/`FromValue`(with default).
- `public abstract class SmartEnum&lt;TEnum&gt; : SmartEnum&lt;TEnum, int&gt;` — `protected SmartEnum(string name, int value)`.
- `public sealed class SmartEnumNotFoundException : Exception` — `TargetType` (Type?), `AttemptedLookup` (object?).
A derived fixture type is declared as: `internal sealed class MyFixture : SmartEnum&lt;MyFixture&gt;` with `public static readonly` instances and a `private MyFixture(int value, string name) : base(name, value)` constructor.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: SmartEnumEqualityTests + SmartEnumListTests</name>
  <files>SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumEqualityTests.cs, SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumListTests.cs</files>
  <read_first>
    - .planning/phases/03-domain-shared-smartenum-t/03-PATTERNS.md — "Test files" section (imports pattern, core test pattern, naming convention)
    - .planning/phases/03-domain-shared-smartenum-t/03-RESEARCH.md — "Fixture-style derived enum" code example (lines 415-438), Pattern 2's "Note on `_fromValue`'s 'multiple instances, same value' behavior" (equality-by-value vs. canonical-lookup-instance distinction)
    - SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/Guards/GuardAgainstNullTests.cs (xUnit v3 `[Fact]` style, `Given_When_Then` naming convention, no test-base-class machinery)
    - SentinelSuite/SentinelSuite.Framework.Domain.Shared/SmartEnum/SmartEnum{TEnum,TValue}.cs (from Plan 03-01 — exact member names to assert against)
  </read_first>
  <action>
    Create SmartEnumEqualityTests.cs in namespace SentinelSuite.Framework.Domain.Shared.Tests.SmartEnum. Declare an internal sealed fixture EqualityFixtureStatus : SmartEnum&lt;EqualityFixtureStatus&gt; with two static readonly instances Active = new(1, nameof(Active)) and Inactive = new(2, nameof(Inactive)), and a private constructor EqualityFixtureStatus(int value, string name) : base(name, value). Write [Fact] tests named per the {MethodUnderTest}_When{Condition}_{ExpectedOutcome} convention: (1) two accesses of the same static field (EqualityFixtureStatus.Active read twice) are Equals-true with matching GetHashCode(); (2) EqualityFixtureStatus.FromValue(1) returns an instance Equals-true to EqualityFixtureStatus.Active (Success Criterion 2 — proves lookup-returned and field-referenced instances of the same Value are equal); (3) EqualityFixtureStatus.Active and EqualityFixtureStatus.Inactive (different Value) are not Equals; (4) EqualityFixtureStatus.Active.Equals((SmartEnum&lt;EqualityFixtureStatus, int&gt;?)null) returns false.

    Then create SmartEnumListTests.cs with fixture ListFixtureStatus : SmartEnum&lt;ListFixtureStatus&gt; declaring three static instances (First = new(1, nameof(First)), Second = new(2, nameof(Second)), Third = new(3, nameof(Third))). Write [Fact] tests asserting: ListFixtureStatus.List.Count equals 3; ListFixtureStatus.List contains each of First, Second, Third (Assert.Contains per instance); and ListFixtureStatus.List's static type is IReadOnlyCollection&lt;ListFixtureStatus&gt; (no public mutator surface — assert via `is not ICollection&lt;ListFixtureStatus&gt;` or simply that the declared property type is IReadOnlyCollection&lt;ListFixtureStatus&gt;).
  </action>
  <acceptance_criteria>
    - SmartEnumEqualityTests.cs asserts two references to the same static field are `Equals`-true with matching `GetHashCode()`
    - SmartEnumEqualityTests.cs asserts `FromValue(1)` returns an instance `Equals`-true to the fixture's `Active` field (ROADMAP Phase 3 Success Criterion 2)
    - SmartEnumEqualityTests.cs asserts two differently-valued instances are not `Equals`, and `Equals(null)` returns `false`
    - SmartEnumListTests.cs asserts `List.Count` equals the fixture's declared instance count and `List` contains every declared static instance (D-09)
    - `cd SentinelSuite && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter SmartEnumEqualityTests` passes with zero failures
    - `cd SentinelSuite && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter SmartEnumListTests` passes with zero failures
  </acceptance_criteria>
  <verify>
    <automated>cd SentinelSuite && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter SmartEnumEqualityTests && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter SmartEnumListTests</automated>
  </verify>
  <done>SmartEnumEqualityTests.cs and SmartEnumListTests.cs exist and pass, confirming Success Criterion 2 (value equality) and D-09 (static List).</done>
</task>

<task type="auto">
  <name>Task 2: SmartEnumToStringTests + SmartEnumDiscoveryFailureTests</name>
  <files>SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumToStringTests.cs, SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumDiscoveryFailureTests.cs</files>
  <read_first>
    - .planning/phases/03-domain-shared-smartenum-t/03-RESEARCH.md — Pitfall 3 ("Lazy<T> exception-caching semantics differ from a static constructor's") in full, including its "How to avoid" and "Warning signs" subsections
    - .planning/phases/03-domain-shared-smartenum-t/03-PATTERNS.md — "Test files" section exception-assertion pattern (`Assert.Throws<...>`)
    - SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/Guards/GuardAgainstStringTests.cs (exception-throwing assertion shape to mirror)
    - SentinelSuite/SentinelSuite.Framework.Domain.Shared/SmartEnum/SmartEnum{TEnum,TValue}.cs (from Plan 03-01 — `_fromName` built via `ToDictionary(item => item.Name)`, which throws `ArgumentException` on a duplicate key)
  </read_first>
  <action>
    Create SmartEnumToStringTests.cs with fixture ToStringFixtureStatus : SmartEnum&lt;ToStringFixtureStatus&gt; declaring one static instance Active = new(1, nameof(Active)). Write a [Fact] asserting ToStringFixtureStatus.Active.ToString() equals the literal string "Active" (D-11).

    Then create SmartEnumDiscoveryFailureTests.cs declaring two fixtures: (a) DuplicateNameFixtureStatus : SmartEnum&lt;DuplicateNameFixtureStatus&gt; with two static instances sharing the identical Name string "Same" but different Value — First = new(1, "Same"), Second = new(2, "Same"); (b) an unrelated, correctly-formed WorkingFixtureStatus : SmartEnum&lt;WorkingFixtureStatus&gt; with one static instance Only = new(1, nameof(Only)). Write [Fact] tests asserting: (1) DuplicateNameFixtureStatus.FromName("Same") throws ArgumentException — this is the duplicate-key failure surfacing from the `_fromName` Lazy&lt;T&gt;'s internal ToDictionary call, per RESEARCH.md Pitfall 3, not a SmartEnumNotFoundException; (2) calling DuplicateNameFixtureStatus.FromName("Same") a second time also throws ArgumentException — confirming Lazy&lt;T&gt;'s default exception-caching/rethrow semantics rather than a crash or a different failure mode on repeated access; (3) after both failing calls above, WorkingFixtureStatus.FromValue(1) still succeeds and returns the Only instance unaffected — proving the broken fixture's reflection/dictionary-build failure does not corrupt lookups on an unrelated SmartEnum-derived type (RESEARCH.md Pitfall 3's exact concern, and this phase's per-closed-generic-type Lazy&lt;T&gt; isolation guarantee from Pattern 2).
  </action>
  <acceptance_criteria>
    - `ToStringFixtureStatus.Active.ToString()` returns exactly `"Active"`
    - `DuplicateNameFixtureStatus.FromName("Same")` throws `ArgumentException` on both a first and a second call (Lazy exception-caching semantics verified)
    - `WorkingFixtureStatus.FromValue(1)` succeeds and returns the `Only` instance after `DuplicateNameFixtureStatus`'s failures — no cross-type corruption
    - `cd SentinelSuite && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter SmartEnumToStringTests` passes with zero failures
    - `cd SentinelSuite && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter SmartEnumDiscoveryFailureTests` passes with zero failures
  </acceptance_criteria>
  <verify>
    <automated>cd SentinelSuite && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter SmartEnumToStringTests && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter SmartEnumDiscoveryFailureTests</automated>
  </verify>
  <done>SmartEnumToStringTests.cs and SmartEnumDiscoveryFailureTests.cs exist and pass, confirming D-11 (ToString returns Name) and RESEARCH.md Pitfall 3's Lazy&lt;T&gt; exception-caching / no-cross-type-corruption behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new — this plan adds only in-process test code exercising Plan 03-01's already-mitigated surface | No new trust boundary is introduced; test fixtures are hard-coded in-repo, not caller-supplied external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-3-01 | Information Disclosure | `SmartEnumNotFoundException.Message`/`AttemptedLookup` (Plan 03-01) | low | accept | Inherited from Plan 03-01; this plan's tests do not exercise the throwing lookup path directly (that is Plan 03-03's concern) — no new exposure introduced. |
| T-3-02 | Denial of Service (theoretical) | `GetAllOptions()` reflection scan (Plan 03-01) | low | accept | Inherited from Plan 03-01. `SmartEnumDiscoveryFailureTests.cs` in this plan deliberately exercises a reflection-time-adjacent failure path (duplicate-Name dictionary build), confirming the failure is scoped to the one broken derived type and does not degrade or corrupt any other `SmartEnum`-derived type's discovery — this is a direct test of D-03's lazy-per-type isolation design, not a new threat. |

</threat_model>

<verification>
Run each new test class individually:
`cd SentinelSuite && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter SmartEnumEqualityTests`
`cd SentinelSuite && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter SmartEnumListTests`
`cd SentinelSuite && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter SmartEnumToStringTests`
`cd SentinelSuite && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter SmartEnumDiscoveryFailureTests`

All four must pass with zero failures. Once all Wave 2 plans (03-02, 03-03, 03-04) have landed, run `cd SentinelSuite && dotnet test` from the solution root — expect zero failures across the full suite.
</verification>

<success_criteria>
- Value equality (Success Criterion 2), static `List` (D-09), `ToString()` (D-11), and the Lazy&lt;T&gt; exception-caching/no-cross-type-corruption guarantee (Pitfall 3) are all covered by passing, automated xUnit v3 tests.
</success_criteria>

## Artifacts This Phase Produces

- `SmartEnumEqualityTests` — xUnit v3 test class; fixture `EqualityFixtureStatus`
- `SmartEnumListTests` — xUnit v3 test class; fixture `ListFixtureStatus`
- `SmartEnumToStringTests` — xUnit v3 test class; fixture `ToStringFixtureStatus`
- `SmartEnumDiscoveryFailureTests` — xUnit v3 test class; fixtures `DuplicateNameFixtureStatus`, `WorkingFixtureStatus`

<output>
Create `.planning/phases/03-domain-shared-smartenum-t/03-02-SUMMARY.md` when done
</output>

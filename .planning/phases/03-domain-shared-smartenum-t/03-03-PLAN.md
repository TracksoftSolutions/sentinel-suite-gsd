---
phase: 03-domain-shared-smartenum-t
plan: 3
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumFromValueFromNameTests.cs
  - SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumNotFoundTests.cs
  - SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumFromValueDefaultTests.cs
autonomous: true
requirements: [PRIM-03]
tags: [smartenum, dotnet, domain-shared, tests]

must_haves:
  truths:
    - "FromValue/FromName lookups succeed for valid inputs (ROADMAP Phase 3 Success Criterion 3)"
    - "FromValue/FromName fail predictably via a specific, catchable SmartEnumNotFoundException for invalid inputs (ROADMAP Phase 3 Success Criterion 4)"
    - "TryFromValue/TryFromName return false instead of throwing for invalid or null/empty inputs (D-04)"
    - "FromValue(value, defaultValue) returns the caller-supplied default instead of throwing on a miss, and the actual match on a hit (D-14)"
  artifacts:
    - path: "SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumFromValueFromNameTests.cs"
      provides: "FromValue/FromName success-path test coverage"
    - path: "SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumNotFoundTests.cs"
      provides: "throwing-miss and Try-variant-miss test coverage"
    - path: "SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumFromValueDefaultTests.cs"
      provides: "FromValue-with-default overload test coverage (D-14)"
  key_links:
    - from: "SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumNotFoundTests.cs"
      to: "SentinelSuite/SentinelSuite.Framework.Domain.Shared/SmartEnum/SmartEnumNotFoundException.cs"
      via: "asserts TargetType/AttemptedLookup are populated correctly on a lookup miss"
      pattern: "SmartEnumNotFoundException"
    - from: "SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumFromValueFromNameTests.cs"
      to: "SentinelSuite/SentinelSuite.Framework.Domain.Shared/SmartEnum/SmartEnum{TEnum,TValue}.cs"
      via: "exercises FromValue/FromName success paths from Plan 03-01 Task 2"
      pattern: "FromValue|FromName"
---

<objective>
Verify SmartEnum's full lookup API surface from Plan 03-01 Task 2: throwing `FromValue`/`FromName` success paths (Success Criterion 3), throwing-on-miss and non-throwing Try-variant behavior (Success Criterion 4, D-04), and the three-argument default-on-miss overload (D-14).

Purpose: Prove every lookup mode CONTEXT.md locked in — throw, Try, and default — behaves correctly for both hit and miss cases, and that `SmartEnumNotFoundException` carries the attempted lookup value/name and target type as D-06 requires.
Output: Three new xUnit v3 test files under `SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/`, all green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-domain-shared-smartenum-t/03-CONTEXT.md
@.planning/phases/03-domain-shared-smartenum-t/03-RESEARCH.md
@.planning/phases/03-domain-shared-smartenum-t/03-PATTERNS.md
@.planning/phases/03-domain-shared-smartenum-t/03-VALIDATION.md
</context>

<interfaces>
<!-- From 03-01-PLAN.md (Wave 1) — this plan's tests exercise this surface; do not modify it. -->
Namespace SentinelSuite.Framework.Domain.Shared.SmartEnum:
- `public static TEnum FromName(string name, bool ignoreCase = false)` — throws `SmartEnumNotFoundException` on a miss.
- `public static bool TryFromName(string name, bool ignoreCase, out TEnum? result)` — `false`/`null` on a miss or a null/empty `name`, never throws.
- `public static TEnum FromValue(TValue value)` — throws `SmartEnumNotFoundException` on a miss.
- `public static bool TryFromValue(TValue value, out TEnum? result)` — `false`/`null` on a miss, never throws.
- `public static TEnum FromValue(TValue value, TEnum defaultValue)` — returns `defaultValue` on a miss (D-14), the actual match on a hit.
- `public sealed class SmartEnumNotFoundException : Exception` — `TargetType` (Type?), `AttemptedLookup` (object?), plus the standard `(string message)` constructor; message shape is `No {TEnum.Name} with Name "{name}" found.` or `No {TEnum.Name} with Value {value} found.`
A derived fixture type is declared as: `internal sealed class MyFixture : SmartEnum&lt;MyFixture&gt;` with `public static readonly` instances and a `private MyFixture(int value, string name) : base(name, value)` constructor.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: SmartEnumFromValueFromNameTests (success paths)</name>
  <files>SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumFromValueFromNameTests.cs</files>
  <read_first>
    - .planning/phases/03-domain-shared-smartenum-t/03-RESEARCH.md — Pattern 3 (FromName/FromValue signatures, lines 275-308)
    - .planning/phases/03-domain-shared-smartenum-t/03-PATTERNS.md — "Test files" section (naming convention, imports pattern)
    - SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/Guards/GuardAgainstNullTests.cs (xUnit v3 `[Fact]` style, naming convention)
    - SentinelSuite/SentinelSuite.Framework.Domain.Shared/SmartEnum/SmartEnum{TEnum,TValue}.cs (from Plan 03-01 — exact `FromName`/`FromValue` signatures)
  </read_first>
  <action>
    Create SmartEnumFromValueFromNameTests.cs in namespace SentinelSuite.Framework.Domain.Shared.Tests.SmartEnum. Declare an internal sealed fixture LookupFixtureStatus : SmartEnum&lt;LookupFixtureStatus&gt; with two static instances Active = new(1, nameof(Active)) and Inactive = new(2, nameof(Inactive)), and a private constructor LookupFixtureStatus(int value, string name) : base(name, value). Write [Fact] tests named per the {MethodUnderTest}_When{Condition}_{ExpectedOutcome} convention: (1) LookupFixtureStatus.FromValue(1) returns the Active instance; (2) LookupFixtureStatus.FromName("Active") returns the Active instance using an exact-case match with the default ignoreCase: false; (3) LookupFixtureStatus.FromName("active", ignoreCase: true) returns the Active instance, exercising the `_fromNameIgnoreCase` case-insensitive dictionary path.
  </action>
  <acceptance_criteria>
    - `LookupFixtureStatus.FromValue(1)` returns the `Active` instance (ROADMAP Phase 3 Success Criterion 3)
    - `LookupFixtureStatus.FromName("Active")` returns the `Active` instance with the default `ignoreCase: false`
    - `LookupFixtureStatus.FromName("active", ignoreCase: true)` returns the `Active` instance (case-insensitive path)
    - `cd SentinelSuite && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter SmartEnumFromValueFromNameTests` passes with zero failures
  </acceptance_criteria>
  <verify>
    <automated>cd SentinelSuite && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter SmartEnumFromValueFromNameTests</automated>
  </verify>
  <done>SmartEnumFromValueFromNameTests.cs exists and passes, confirming ROADMAP Phase 3 Success Criterion 3.</done>
</task>

<task type="auto">
  <name>Task 2: SmartEnumNotFoundTests (throw + Try-miss) + SmartEnumFromValueDefaultTests (D-14)</name>
  <files>SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumNotFoundTests.cs, SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/SmartEnum/SmartEnumFromValueDefaultTests.cs</files>
  <read_first>
    - .planning/phases/03-domain-shared-smartenum-t/03-CONTEXT.md — decisions D-04, D-05, D-06, D-14
    - .planning/phases/03-domain-shared-smartenum-t/03-PATTERNS.md — "Exception-assertion pattern to reuse for SmartEnumNotFoundTests.cs" section
    - SentinelSuite/SentinelSuite.Framework.Domain.Shared.Tests/Guards/GuardAgainstStringTests.cs (`Assert.Throws<...>` shape to mirror)
    - SentinelSuite/SentinelSuite.Framework.Domain.Shared/SmartEnum/SmartEnumNotFoundException.cs (from Plan 03-01 — exact `TargetType`/`AttemptedLookup` property names and the 4-constructor shape)
  </read_first>
  <action>
    Create SmartEnumNotFoundTests.cs with fixture NotFoundFixtureStatus : SmartEnum&lt;NotFoundFixtureStatus&gt; declaring one static instance Active = new(1, nameof(Active)). Write [Fact] tests asserting: (1) NotFoundFixtureStatus.FromValue(999) throws SmartEnumNotFoundException whose TargetType equals typeof(NotFoundFixtureStatus) and whose AttemptedLookup equals 999 (D-06); (2) NotFoundFixtureStatus.FromName("Missing") throws SmartEnumNotFoundException whose AttemptedLookup equals "Missing"; (3) NotFoundFixtureStatus.FromName("active") (case mismatch, default ignoreCase: false) also throws SmartEnumNotFoundException; (4) NotFoundFixtureStatus.TryFromValue(999, out var result) returns false with result null; (5) NotFoundFixtureStatus.TryFromName("Missing", false, out var result2) returns false with result2 null; (6) NotFoundFixtureStatus.TryFromName(null!, false, out var result3) returns false with result3 null, without throwing (D-04's non-throwing path for a null name); (7) NotFoundFixtureStatus.TryFromName("", false, out var result4) returns false with result4 null, without throwing.

    Then create SmartEnumFromValueDefaultTests.cs with fixture DefaultFixtureStatus : SmartEnum&lt;DefaultFixtureStatus&gt; declaring one static instance Active = new(1, nameof(Active)). Write [Fact] tests asserting: (1) DefaultFixtureStatus.FromValue(1, DefaultFixtureStatus.Active) returns the actual matching instance on a hit (the supplied default argument is ignored when the value is found); (2) DefaultFixtureStatus.FromValue(999, DefaultFixtureStatus.Active) returns the supplied DefaultFixtureStatus.Active default instead of throwing, on a miss (D-14).
  </action>
  <acceptance_criteria>
    - `FromValue(999)` throws `SmartEnumNotFoundException` with `TargetType == typeof(NotFoundFixtureStatus)` and `AttemptedLookup` equal to `999`
    - `FromName("Missing")` and `FromName("active")` (case mismatch, default `ignoreCase: false`) both throw `SmartEnumNotFoundException`
    - `TryFromValue(999, out var result)` returns `false` with `result` `null`; `TryFromName("Missing", false, out var result2)` returns `false` with `result2` `null`
    - `TryFromName(null!, false, out var result3)` and `TryFromName("", false, out var result4)` both return `false` without throwing (D-04 non-throwing path)
    - `FromValue(1, DefaultFixtureStatus.Active)` returns the actual matching instance (hit path ignores the supplied default)
    - `FromValue(999, DefaultFixtureStatus.Active)` returns the supplied default instance instead of throwing (D-14 miss path)
    - `cd SentinelSuite && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter SmartEnumNotFoundTests` passes with zero failures
    - `cd SentinelSuite && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter SmartEnumFromValueDefaultTests` passes with zero failures
  </acceptance_criteria>
  <verify>
    <automated>cd SentinelSuite && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter SmartEnumNotFoundTests && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter SmartEnumFromValueDefaultTests</automated>
  </verify>
  <done>SmartEnumNotFoundTests.cs and SmartEnumFromValueDefaultTests.cs exist and pass, confirming ROADMAP Phase 3 Success Criterion 4, D-04's Try-variant contract, D-06's exception-carries-context requirement, and D-14's default-on-miss overload.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test code (hard-coded lookup values) → `FromValue`/`FromName` throwing path | This plan directly exercises the T-3-01 boundary flagged in Plan 03-01 — attempted lookup values are asserted to appear in `AttemptedLookup`/`Message`, confirming the accepted (not mitigated) disclosure shape rather than introducing a new one |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-3-01 | Information Disclosure | `SmartEnumNotFoundException.Message`/`AttemptedLookup` | low | accept | This plan's `SmartEnumNotFoundTests.cs` asserts `AttemptedLookup` is populated with the raw rejected value/name — confirming the already-accepted disclosure shape from Plan 03-01 (mirrors Phase 2's `Result.CriticalError` T-2-01 acceptance) is implemented correctly, not introducing a new exposure. No external-facing boundary exists at this Domain.Shared-only phase. |

</threat_model>

<verification>
Run each new test class individually:
`cd SentinelSuite && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter SmartEnumFromValueFromNameTests`
`cd SentinelSuite && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter SmartEnumNotFoundTests`
`cd SentinelSuite && dotnet run --project SentinelSuite.Framework.Domain.Shared.Tests --filter SmartEnumFromValueDefaultTests`

All three must pass with zero failures. Once all Wave 2 plans (03-02, 03-03, 03-04) have landed, run `cd SentinelSuite && dotnet test` from the solution root — expect zero failures across the full suite.
</verification>

<success_criteria>
- FromValue/FromName success paths (Success Criterion 3), throwing-miss and Try-variant-miss paths (Success Criterion 4, D-04), and the default-on-miss overload (D-14) are all covered by passing, automated xUnit v3 tests.
- `SmartEnumNotFoundException` is confirmed to carry `TargetType`/`AttemptedLookup` correctly (D-06).
</success_criteria>

## Artifacts This Phase Produces

- `SmartEnumFromValueFromNameTests` — xUnit v3 test class; fixture `LookupFixtureStatus`
- `SmartEnumNotFoundTests` — xUnit v3 test class; fixture `NotFoundFixtureStatus`
- `SmartEnumFromValueDefaultTests` — xUnit v3 test class; fixture `DefaultFixtureStatus`

<output>
Create `.planning/phases/03-domain-shared-smartenum-t/03-03-SUMMARY.md` when done
</output>
